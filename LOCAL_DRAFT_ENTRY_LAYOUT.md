# 融合后的物理形态 · 本地执行模型自拟草案 v0.1（待反驳）

> **性质**：本地执行模型自拟，**未经评审**。`ISSUES.md` 第 2 轮题目（N1~N6）以本文为待反驳基线。
> **本轮范围**：只谈**目录/仓库/文件放在哪**，不谈概念层（概念归属已由第 1 轮定，见 `VERDICTS.md` 与 DEC-13~15）。
> 用户的原始诉求一句话：**把三个项目变成一个项目，并且他不想手工管文件。**

## 1. 现状盘点（实测）

| 层 | 现在的物理位置 | 可写性 |
|---|---|---|
| Obsidian 容器库 | `D:\Work\`，**各子库独立 git** | 人写 |
| 三套系统 | `D:\Work\AI自成长引擎\`、`D:\Work\自适应工作流引擎\`、（元智能规范在事实层仓 `30_元智能/`） | 各自执行真源 |
| 记忆库 | `D:\记忆\`（独立 vault，含 `调度大脑记忆/` 工具记忆） | 人+Agent 写 |
| 事实层（AI 间共享） | `D:\meta-intelligence-facts\`，含 `10_ / 20_ / 30_` 三个子目录 | 本地执行模型写，推公开仓 |
| 状态类产物 | 记忆库 `STATE`（≤60 行）、两个 `01-任务看板.md`、`runtime/*.drift.json`、`CURRENT_STATE.md` | **四处各自可写，互不同步** |

**病灶**：不是"文件夹太多"，是**同一类事实有四个可写处**，且没有任何一处写着"我们到底在追求什么"。

## 2. 我的方案：合"看的那一层"，不合"跑的那一层"

```
D:\Work\元智能\                 ← 唯一入口，新项目，独立 git
  00_首页.md                    ← 机器生成，人只看这一页（Human View）
  需求\                         ← Demand 台账：全系统唯一"人可写"的地方
    D-001_全机AI资产治理.md
    D-002_送审链路可用性.md
    D-003_三系统融合.md
  状态\                         ← 机器生成，只读；人改它即视为污染
    CURRENT_STATE.md
  证据\                         ← 三块，全是指针，不复制正文
    自成长.md    → D:\Work\AI自成长引擎
    工作流.md    → D:\Work\自适应工作流引擎
    规范.md      → D:\meta-intelligence-facts\30_元智能
```

三套引擎**原地不动**（代码、技能、契约、探针、看板一律不搬）。理由：搬进 Obsidian 会让"看的地方"变成"写的地方"，违背既有规则"Obsidian 只解释与索引，不反向充当运行事实源"；且批量改路径正是本机 946 条死链的成因。

## 3. 数据流（谁生成谁）

```
需求\（人写） → 生成器 → 状态\CURRENT_STATE.md → 00_首页.md
                     ↑
   两个看板 + runtime/drift + DECISIONS/VERDICTS（只读输入）
```

- 生成器只读上面这些，**唯一写出** `状态\` 与 `00_首页.md`。
- 首页只放四类：现在在哪 / 下一步 / 风险 / 等你决定。路径、JSON、日志、重试细节默认隐藏。
- 视图与原生事实冲突时，判视图过期并重生成；**禁止反向修改真源去配平视图**。

## 4. 我不确定的三点（就是要问的）

1. **指针 vs 复制**：Obsidian 对 junction / symlink 的实际行为我没测过 —— 全文搜索、反链面板、关系图谱到底跟不跟进去？如果不跟，"证据层"在 Obsidian 里就是死链，那还不如放一份**生成出来的索引页**（列路径与状态，不复制正文）。
2. **Demand 台账放哪**：`D:\Work\元智能\需求\` 是给人看的，但 AI 间共享真源在事实层仓。同一份内容两个位置就又是双写。要么台账真源在事实层、Obsidian 里是投影；要么相反。**我倾向真源在事实层**（Agent 读写都走它，Obsidian 只渲染给人看），但这条与 DEC-13"元智能层持有 Demand"怎么落地我没想清楚。
3. **谁来触发重生成**：手动跑脚本？定时？还是 Agent 每次收尾必须跑？触发不了的话，首页就会长期显示过期状态——那比现在更危险，因为它看起来像真的。

## 5. 我认为的"合上了"判据（请评审可否机器化）

改一次 Demand 的 `version`，十分钟内三件事自动成立：受影响的 Task/Experiment 被重新判断、`状态\CURRENT_STATE.md` 变了、`00_首页.md` 变了。做不到就是叠了层皮，不是融合。
