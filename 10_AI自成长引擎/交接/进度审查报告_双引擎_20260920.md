# 进度审查报告 · 微内核路由底座 + 双引擎收口 · 2026-09-20

> **性质**：对外部执行方回报的「架构与环境配置状态」做实证审查。**结论一律以文件系统、git 状态、hash 比对为准，不以自述为准。**
> **审查时间**：2026-09-20
> **审查人**：Loomy（立项与交接方）
> **总评**：**交代的收口任务全部通过（14/14），但发现 7 个实质问题，其中 2 个严重、1 个是我方契约错误、1 个是 Work 库 510 MB 重复目录 + 24 个文件未提交删除。**

---

## 一、交接任务验收：14/14 通过

| # | 验收项 | 核验方式 | 结果 |
|---|---|---|---|
| 1 | L1~L10 十处 `草稿区\` 断链修复 | `grep 草稿区` 于 `D:\Work\自适应工作流引擎\` | **PASS**（零命中） |
| 2 | U1 `docs\00-项目总览.md` L28/74/138/157 L3 落点改口径 | grep `Loomy skills` | **PASS**（L157 已含「程序技能目录只放已安装生效项，项目产出先存本项目技能库」） |
| 3 | U2 `README.md` 边界段口径 | grep | **PASS** |
| 4 | U3 `docs\03-方案构想.md` §2.3 + 示例输出 | grep | **PASS** |
| 5 | U4 `docs\02-原始想法记录.md` **只增不改** | 读文件 | **PASS**（原 A2 行未动，页末追加「口径更正」段）★ 最易违规项，守住了 |
| 6 | R1 新增决策项 D7（`_inbox` 归属，含双面理由） | 读任务看板 §五 | **PASS**（结果栏写「待定（未表态前保持现状不动）」，未替用户拍板） |
| 7 | R2 上游复盘记录 | 读 §七 | **PASS** |
| 8 | R3 下游决策日志记归并 | 读 §六 L96 | **PASS** |
| 9 | skill 未被违规安装进程序目录 | 列 `D:\记忆\_Skills\loomy-learned*` | **PASS**（仍 3 个，无新增） |
| 10 | `evalB_ext.js` 只标注不改逻辑 | `git diff --stat` | **PASS**（`24 insertions(+)`，0 删除，纯插入） |
| 11 | 草稿区三级目录建好 | 列目录 | **PASS**（`L1_经验` / `L2_SOP` / `L3_Skill`，内容为空） |
| 12 | `经验沉淀SOP.md` 落盘 | 读文件（2094B） | **PASS**（含三套模板 + 行为守则） |
| 13 | 全局路由配置落盘 | Test-Path + SHA256 | **PASS**（`AGENTS.md` 与 `.claude\CLAUDE.md` 均存在，09-20 08:32/08:33） |
| 14 | 未建 git 仓、未登记 projects.yaml（守约束） | Test-Path | **PASS**（两项目均无 `.git`） |

> 收口质量评价：**高于预期**。尤其第 5 项（只增不改）和第 6 项（不替用户拍板）是最容易偷懒的两处，都守住了。

---

## 二、七个问题（按严重度排序）

### 问题 1【严重】P2 路由是哑的：指向一个不存在的文件

**证据**

`C:\Users\郭永涛\AGENTS.md` 第 10 行：

> | **P2: 复杂多步工作流** | 任务涉及 >=3 个异构工具顺序链条，且存在强数据依赖 | 读取 `D:\Work\自适应工作流引擎\docs\00-自适应工作流总纲.md` | 按契约执行状态机，遇漂移自适应降级 |

实测：

```
[缺失]  D:\Work\自适应工作流引擎\docs\00-自适应工作流总纲.md
[存在]  D:\Work\自适应工作流引擎\docs\00-项目总览.md      (12211B)
[存在]  D:\Work\自适应工作流引擎\docs\03-方案构想.md      (14074B)
```

而进度汇报第 2 条自称「**挂载总纲至** `…\00-自适应工作流总纲.md`」—— **该文件从未被创建**。

**后果**：任何会话命中 P2 时，加载动作失败。而"按需加载"的失败是**静默**的（Agent 读不到就当没有），于是"契约执行状态机 / 遇漂移自适应降级"整条能力形同虚设。这正是本项目最反对的失效类型：**不报错，但功能是假的**。

**附带问题**：若真按汇报去新建 `00-自适应工作流总纲.md`，会与既有 `00-项目总览.md` 形成**同目录双 `00-` 编号撞车**，破坏 handbook 的 docs 编号惯例（00 总览 / 01 看板 / 02-03 专题），也会让两个项目的 README 里「先读 docs\00」变成歧义指令。

**建议修法（一行，不新建文件）**：把 P2 的加载路径改为既有文件

```
读取 `D:\Work\自适应工作流引擎\docs\00-项目总览.md`（架构与四级闸门）
      + `D:\Work\自适应工作流引擎\docs\03-方案构想.md`（契约/探针/自愈执行序）
```

---

### 问题 2【严重】全局禁令与本项目核心闸门直接矛盾

**证据**

- `AGENTS.md` 硬约束 2：「**禁止主动加戏反问**：即便完成了 P1 沉淀，也仅在后台静默写入草稿，明面上只回答用户的业务问题，**绝不主动询问"是否需要为您保存为 Skill"**」
- `经验沉淀SOP.md` §三.2：「**不加戏**：在回答用户时只汇报正常的任务收尾…**不主动向用户炫耀"我已为您总结了一条经验"**」

**冲突对象**：`D:\Work\AI自成长引擎\docs\00-项目总览.md` §四 闸门交互 —— 会话末产出「建议固化清单」，用户回复编号（`1,3` / `all` / `none`）才执行；且 §四.1 明写 **L2 生成草稿待确认、L3 必须用户点头 + dry-run**。

**后果**：全局路由是**所有会话**的行为底座，优先级高于项目文档。它一旦生效：

| 层级 | 原设计 | 被全局禁令改写后 |
|---|---|---|
| L1 经验条目 | 自动写入可撤销 | ✅ 一致，无冲突 |
| L2 工作流 SOP | 出草稿 → **等确认** | ❌ 变成静默落盘，没人看 |
| L3 skill / 脚本 | **必须点头 + dry-run** | ❌ 人工确认环节被掐掉 |

而 L3 闸门是本项目**防幻觉污染的唯一防线**（`00-项目总览` §七 风险表第 4 行「过度自动化 → 我失去掌控，产物不敢用 → 对策：L3 必须人工确认」）。同时 `技能库\README.md` §四 的五步安装闸门第 4 步就是"郭老师点头"。

**需要你裁定（二选一）**

- **甲**：把 AGENTS.md 硬约束 2 与 SOP §三.2 的适用范围**限定为 L1**，明确写出「L2/L3 仍须走建议固化清单与人工点头」。→ 保住闸门，代价是会话末偶尔多一段询问
- **乙**：接受 L2/L3 也静默落盘到草稿区，靠**人工定期翻草稿区**来把关。→ 干净安静，代价是草稿区不翻就等于没有闸门

我的建议是**甲**：草稿区目前无任何 git 追踪（见问题 5），静默落盘 + 无人翻阅 + 无法回溯，三者叠加风险最高。

---

### 问题 3【中】D7 台账写"待定"，实际已经选了 B —— 声明态与运行态不符

**证据对照**

| 真源 | 说的中转区在哪 |
|---|---|
| `AGENTS.md` P1/P3 落盘目标 | `D:\Work\AI自成长引擎\草稿区\L1_经验\` |
| `经验沉淀SOP.md` §一 | `D:\Work\AI自成长引擎\草稿区\{L1_经验,L2_SOP,L3_Skill}\`（三级目录**已实际建好**） |
| `AI自成长引擎\docs\01-任务看板.md` **D7 结果栏** | 「**待定（未表态前保持现状不动）**」 |
| `AI自成长引擎\docs\03-方案构想.md` §2.2 / T8 / T6 | 仍写 `D:\记忆\_inbox\` |
| `AI自成长引擎\技能库\README.md` §五 | 把这件事列为「待定问题」 |

**即：D7 在配置层已经按选项 B 落地执行了，台账却仍声明"待定/保持现状"，方案文档仍写选项 A 的路径。**

这件事的讽刺之处在于——**「声明态与运行态不一致，导致文档给出假信号」正是自适应工作流引擎立项的头号论据**（见其 `docs\02-镜像站试点盘点.md` §四）。项目自己先得了一遍这个病。

**连带问题：L3 skill 现在有两个存放地**

| 存放地 | 来源 | 有无索引 |
|---|---|---|
| `D:\Work\AI自成长引擎\草稿区\L3_Skill\` | 新 SOP 规定 | ❌ 无索引、无状态列 |
| `D:\Work\AI自成长引擎\技能库\` | 09-19 已定口径 | ✅ 有 `README.md`（技能清单 + 状态列 + 安装五步闸门） |

这正是你上一轮明确反对的「新旧资产检索时分裂成两套」。

**建议修法**：SOP §一 的 L3 路径保留（作为写入缓冲区），但**必须补一条流转规则**：「L3 草稿提炼完成后，**移动**（非复制）到 `技能库\<skill-id>\`，并在 `技能库\README.md` §二 清单加一行」。同时把 D7 结果栏改成事实（已按 B 执行），或把 `草稿区\` 显式降级命名为"写入缓冲区"以免与"中转区真源"混淆。

---

### 问题 4【中】两份逐字相同的全局配置，无同步机制

**证据**：`C:\Users\郭永涛\AGENTS.md` 与 `C:\Users\郭永涛\.claude\CLAUDE.md` 均 1806B，**SHA256 完全相同**。

**后果**：违反 handbook 单写真源法则。将来任一侧单独修改，Claude Code 与 Antigravity CLI 的行为会**静默分叉**——而且分叉不会报错，只表现为"另一个工具怎么不守规矩了"，排查成本极高。汇报第 1 条把它描述为优点（"保持一致的微内核底座"），但**一致性目前靠人工维持，没有机制保障**。

**建议**：指定一份为真源（如 `AGENTS.md`），另一份只写一行指针 `见 C:\Users\郭永涛\AGENTS.md`；或加一个 hash 比对检查（可挂 Loomy 定时任务，周跑一次，不一致就报警）。

---

### 问题 5【中】"可回溯"目前是口头承诺——三个目录都没有 git

**证据**

```
D:\Work\api-gateway\.git                    → False
D:\Work\AI自成长引擎\.git                    → False
D:\Work\AI自成长引擎\草稿区\.git             → False
D:\Work\自适应工作流引擎\.git                → False
```

汇报称「草稿与正式库强隔离…**从根本上阻断模型幻觉对核心知识库的污染**」。隔离这一半成立（写入路径确实限死在草稿区）。但**可回溯这一半不成立**：草稿区没有版本追踪，误覆盖、误删、以及"谁在什么时候改了哪条经验"都无法回滚或审计。

而 `AI自成长引擎\docs\01-任务看板.md` 的 **D5「`_inbox` 是否进 git」建议值就是 A（进，可回溯）**，状态待定。

**建议**：D5 尽快拍板。若两个项目短期内仍不建 git 仓（守 A3/M0 纪律），至少让草稿区纳入某一现有仓的追踪，或加一个"写入前备份 + 每日 diff 快照"的最小机制。

---

### 问题 6【与本次汇报无关，但你必须知道】Work 库多出 510 MB 重复目录 + 24 个文件被删未提交

**先声明**：此项**不在进度汇报的任何一条里**，时间戳也对不上（路由配置写于 09-20 08:32，该目录建于 **09-20 13:29**）。因此**我不指认执行方做了这件事**，只报客观现状供你处置。

**6.1 约 510 MB 资产被整份复制了一次**

```
869 文件   509.8 MB   D:\Work\api-gateway\              ← 建于 2026-09-20 13:29，最后写 13:43
862 文件   509.8 MB   D:\Work\AI平台\apps\api-gateway\  ← 原有
```

四个抽样文件**两边都在**：`docs\V5-DEVLOG.md`(133737B)、`prototypes\v4-gpt-complete\ai-gateway-v4\js\app.js`、`v5.2-experiment\src\scene3d.js`、`对话记录\更新记录.md`(26724B)。目录构成：`data / docs / prototypes / v5 / v5.2-experiment / visual-hub / 对话记录` + `AGENTS.md`(3298B) + `README.md`(4535B)。体量大头是数百张 1–3 MB 的 PNG 预览图与场景素材（`retired-fog-arch`、`previews`、`versions\V5.2.*-说明\预览图` 等）。

新目录 **不是 git 仓**、**未登记进 `D:\Work\Home.md`**（Home.md 仍为 9 个子库）。

**违反 handbook 两条**：
- `40-安全红线`：「仓库内只有代码与文档；**成品资产一律不进 git**」——现状是 510 MB 成品资产存在两份
- 单写真源 / canonical path 唯一：「同一功能只认一份代码；其余是 archive/reference 副本」——现状是两份等重副本，**没有任何一份被声明为 canonical**

**6.2 同一 repo 有 24 个文件被删，未 staged 未 commit**

`git -C "D:\Work\AI平台" status --porcelain`：工作树删除 **24 条**；`git diff --stat` = **29 files changed, 93 insertions(+), 9191 deletions(-)**。

被删的是 `apps/api-gateway/services/`：`api_gateway.py`、`channels.py`、`capability_verify.py`、`catalog_routes.py`、`fault_domains.py`、`heartbeat.py`、`model_capabilities.default.json`、`pricing.py`、`quota.py`、`rate_limit.py`、`resource_config.py`、`start_api_gateway_3100.ps1`、`sync_cherry.py`、`upstream_outcome.py`、`watchdog_3100_alert.ps1`、`web/api_page.html`(2888 行)、5 个 favicon。

**★ 好消息：源码没丢。** 我一开始判断"服务代码被删且没搬走"，**查实后推翻了自己这个判断**：

| 文件 | 仍在的位置 |
|---|---|
| `api_gateway.py` | `D:\Work\AI平台\apps\search-gateway\services\`(91879B) · `D:\项目\ai-hub\search_gateway\services\`(102945B) · `D:\项目\_backup_search_gateway_20260905\…` |
| `channels.py` / `quota.py` / `rate_limit.py` / `watchdog_3100_alert.ps1` | 同上三处 |

且删除**未提交**，`git -C "D:\Work\AI平台" checkout -- apps/api-gateway/` 可 **100% 还原**。

> **处置建议**：在你决定 6.1 的 510 MB 重复目录怎么办之前，**先别在 ai-platform 仓里做任何 add / commit / checkout**。一旦 `git add -A && git commit`，这 24 个删除就会被固化进历史；一旦 `checkout`，13:29 之后对该目录的任何新改动又会被冲掉。

**6.3 同一批未提交改动里还有 3 个与本任务无关的修改**

| 文件 | 规模 | 内容 |
|---|---|---|
| `resource-ops\docs\操作手册\01_镜像站GPT操作手册.md` | +11 −3 | **每窗口 ≤12 轮 → ≤24 轮**，标注「2026-09-18 用户修订」，并加了「24 轮不是免死金牌，出现遗忘/混淆/答非所问立即要总结带总结新开」 |
| `apps\search-gateway\services\web\api_page.html` | +35 −59 | 前端页面改动（疑与最近主题提交 a03a642 同批） |
| `docs\design\ai-platform-obsidian-rfc.md` | 已修改 | 未细查 |

`apps\api-gateway\docs\README.md` 的 +9 行是补文档索引（V5-DEVLOG / V5.1-5.3 方案 / 前端演进复盘 / 对话记录），属正常增量。

---

### 问题 7【我方错误】我的契约编码了一条已过期规则，而记忆库与操作手册现在互相矛盾

**这是本次审查最有价值的一条发现，因为它完整复现了本项目的立项理由。**

| 真源 | 轮次纪律 |
|---|---|
| `D:\记忆\调度大脑记忆\反馈\feedback_gpt_rounds_discipline.md` | 「一个聊天窗口最多 **12** 轮对话」 |
| `D:\记忆\调度大脑记忆\索引.md` L48 | 「GPT 轮次纪律（一窗≤**12**轮/3轮一任务）」 |
| `D:\记忆\…\feedback_gpt_mirror_subagent_flow.md` L14 | 「每窗口 ≤**12** 轮」 |
| `D:\Work\AI平台\resource-ops\docs\操作手册\01_镜像站GPT操作手册.md` §9.3 | 「每窗口 ≤**24** 轮（**2026-09-18 用户修订：12 → 24**）」 |
| 我写的 `contracts\gpt-mirror-extended-review.yaml` → `gates.turn_budget` | `assert: "window_turns <= 12 …"` ← **错** |

**我错在哪**：写契约时以记忆条目为依据，**没有跨真源比对同一个常量**。记忆库三处都说 12，操作手册已在 09-18 按你修订改为 24，我照记忆写了 12 → **契约现在会在我实际允许的窗口内误判 `break`，把还能继续的会话掐断**。

**这条错的教训要回写进设计（比改数字更重要）**：
- `00-项目总览` §四 ③ 的「撞车检查」目前只查**相似资产**（去重），**没查同一常量在不同真源里的取值冲突**。本次证明必须补一条：**契约里每个数值型/枚举型断言都要标注 `source` 路径**，探针除了核对外部世界，还要**核对内部多真源的一致性**。
- 这实际上是给「三件套」加了第四件：**跨真源常量一致性校验**。

**建议修法**：契约 `gates.turn_budget` 改为 `window_turns <= 24`，`assert` 加 `note: 2026-09-18 用户修订 12→24，source=resource-ops/docs/操作手册/01_镜像站GPT操作手册.md §9.3`；同时**记忆库那三处要不要同步改 24，需你决定**——记忆库是你的 canonical，我不擅自改（handbook 亦规定记忆正文由你手写）。

---

## 三、优先级处置建议

| 顺序 | 事项 | 为什么这个顺序 | 动作量 |
|---|---|---|---|
| **1** | 修哑路由（问题 1） | 影响**每一个后续会话**，且失效是静默的 | 改 AGENTS.md/CLAUDE.md 各 1 行 |
| **2** | 裁定 L2/L3 确认权归谁（问题 2） | 设计层矛盾，不裁就会长期拧着，且会决定 SOP 怎么写 | 你选甲或乙 |
| **3** | **冻结 ai-platform 仓**，再决定 510 MB 重复目录（问题 6） | 不紧急，但**越晚越难收拾**；一旦 commit 就把删除固化进历史 | 你先定 canonical |
| **4** | 契约 `turn_budget` 12→24 + 补 `source` 字段规范（问题 7） | 我的错，且直接导致闸门误判 | 改契约 + 加一条设计规范 |
| **5** | SOP 与技能库统一 L3 落点（问题 3） | 防止技能资产分裂成两套 | SOP 补一句流转规则 + D7 改成事实 |
| **6** | 两份全局配置加同步机制（问题 4） | 慢性风险 | 一份改指针，或加 hash 周检 |
| **7** | D5 拍板 git 追踪（问题 5） | 让"可回溯"变成事实 | 你拍板 |

---

## 四、我方（立项会话）自查结论

本次审查同时查出了**我自己**的两处问题，如实记录，不粉饰：

1. **契约 `turn_budget` 写了过期值 12**（问题 7）——根因是我只信单一真源、未做跨真源常量比对。**修法已写进本项目设计规范（§二 四件套），这比改对那个数字更重要。**
2. **第一轮 `project.yaml` 从 handbook 模板复制时留下两条幽灵路径**（`docs/02-资产清单.md`、`docs/03-规格与规范.md` 指向不存在的文件），是上一轮 `Test-Path` 才抓出来的。**教训已写进交接文档 §三.2 第 8 条：从模板复制元数据后必须逐条 Test-Path。**

> 这两条恰好说明：**"从模板/记忆直接抄，不做存在性与一致性校验" 是本次全部问题的共同根因**——问题 1（抄了一个想当然的路径）、问题 7（抄了一个过期常量）、我方两条（抄了模板占位）都是同一个病。建议把「**引用即校验**」（任何写入文档或配置的路径/常量，落盘前必须 Test-Path 或跨真源比对）升级为两个项目的共同铁律，写进各自 `00-项目总览` 的边界节。

---

## 五、五项裁定与执行记录（2026-09-20 用户拍板 → 2026-09-21 执行完毕）

用户以选择题形式裁定五个问题，**全部选了推荐项**。执行结果与验证证据如下：

| # | 裁定 | 已执行动作 | 验证证据 |
|---|---|---|---|
| **Q1** | 哑路由两份都修，**一份当真源** | ① `AGENTS.md` P2 路径 → `docs\00-项目总览.md` + `docs\03-方案构想.md`（指向既有文件，**不新建**，避免 `00-` 编号撞车）② `.claude\CLAUDE.md` 全文改为**指针文件**（只写真源路径 + 为什么不再复制正文 + 维护规则） | 两文件 SHA256 **已由相同变为不同**（真源 vs 指针），分叉风险消除 |
| **Q2** | **只管 L1，L2/L3 仍要点头** | ① `AGENTS.md` 硬约束 2 重写：静默范围**仅限 L1**，并明写「L2/L3 不静默（2026-09-20 用户裁定）…不得以'不加戏'为由跳过」② `经验沉淀SOP.md` §三 重写为 4 条，含闸门要求 ③ 上游 `技能库\README.md` §四 五步闸门保持一致 | 三处口径一致；冲突消除 |
| **Q3** | **只还原 24 个删除，重复目录先留着** | `git -C "D:\Work\AI平台" checkout -- apps/api-gateway/services/` | 还原后 `git status`：**删除项 24 → 0**；`api_gateway.py`/`channels.py`/`quota.py`/`rate_limit.py`/`watchdog_3100_alert.ps1`/`start_api_gateway_3100.ps1`/`web\api_page.html` 全部「已还原」；`diff --stat` 由 9191 deletions 降为 **66 deletions**（仅剩 `api_page.html` 那 59 行等正常改动）。`D:\Work\api-gateway\`（509.8MB）**未动一分** |
| **Q4** | **契约和记忆库都改成 24** | 四处同步：① 契约 `gates.turn_budget` → `<= 24`，并新增 `source`（指向手册 §9.3）/ `revision` / `caveat` ② 记忆库 `feedback_gpt_rounds_discipline.md`（description + 正文 + 新增「修订记录」保留 12 的旧值痕迹 + `modified` 时间戳）③ `feedback_gpt_mirror_subagent_flow.md` 第 2 条 ④ `索引.md` 索引行。另同步：`03-方案构想.md` 契约样例、`技能库\...\SKILL.md` 闸门表与禁止事项 | 全库 `grep` 复查：`D:\Work` 与 `D:\记忆` 中**残留的「12 轮」只剩 3 处，全部是有意保留的记录**——本报告 §二问题7（审计原文）、`02-镜像站试点盘点.md` §二（09-19 快照，已在 §八 追加更正说明）、`AI精听训练器` 历史交接档案 |
| **Q5** | **统一进技能库，草稿区加每日快照** | ① `经验沉淀SOP.md` §一 新增「L3 流转规则」三步（**移动**到 `技能库\` → 登记一行 → 状态流转），并声明技能库是 L3 唯一归口、两处并存视为未完成流转 ② 新建 `scripts\快照草稿区.ps1`（打包 `草稿区`+`技能库` 到 `_快照\`，保留 14 份，空目录写 `.EMPTY.txt` 以区分"没产出"与"没跑"，只删自己产出的过期快照、绝不递归删源目录，退出码供定时任务判成败）③ 建 Loomy 定时任务「AI自成长引擎草稿区每日快照」每日 23:30 | 脚本**实跑通过**：退出码 `0`，产出 `草稿区_20260921_114920.zip`(2991B) + `技能库_20260921_114920.zip`(9549B) + `快照日志.txt`；开包核验 zip 内**目录层级完整保留**（`loomy-learned-gpt-mirror-extended-review\SKILL.md`），未压平 |

### 5.1 执行中新发现的第九个问题（本轮自查所得）

跑快照时发现**草稿区已在真实产出**——P1 路由确实在工作，已落 3 条 L1（09-20 14:39 两条、20:56 一条）。逐条审查后：

| 草稿 | 引用即校验 | 撞车检查 | 结论 |
|---|---|---|---|
| `20260920_obsidian-canvas-board.md` | ✅ 引用的 `项目演进大白板.canvas` 经 `Test-Path` 确认真实存在 | 无同题真源 | **合格** |
| `20260920_powershell-json-arg-space-truncation-field-id.md` | 无外部路径 | ❌ 与既有 `context\feishu-bitable-field-id.md` **同题** | 撞车 |
| `20260920_lark-cli-home-redirect-not-configured.md` | 路径均真实 | ❌ 与既有 `context\lark-cli-home-redirect.md` **同题** | 撞车 |

**首轮撞车率 2/3 ≈ 67%。** 说明捕获环节缺「落盘前先查既有真源」这一步——正是 §二 问题 7 的同一类病（只看单一来源）。

**处置**（未删任何草稿）：两条撞车草稿各补一段 `relates_to` + **增量说明**，并写明"应增补进既有真源、不可平行并存"。之所以保留而非删除，是因为它们**都比既有真源更详细**（多出引号转义、竖线字段名、`record-get` 复核、`--prefix` 具体值、静默失败特征），是有价值的增量，问题只在于没被合并回去。
**注**：Loomy 自身记忆目录（`…\share\memory\`）按规矩不由我写入，故只在草稿侧标注，合并动作留给提纯环节。
**已内建防复发**：撞车检查前置写进 `经验沉淀SOP.md` §三.3、`03-方案构想.md` §2.2、`技能库\README.md` §五 ★节，并列入 T9 验收标准。

### 5.2 另一发现：M1 抢在 M0 前面跑了

全局路由只实现了「P1 信号触发 → 静默落 L1」，**没有实现「会话末建议固化清单 → 用户勾选」**。结果：M1 的捕获环节已真实运行，M0 的确认环节一次都没跑过——而上游台账原写「M0 没走通就不进 M1」。
**处置**：不强回退（自动产出已在积累，回退是浪费），但在上游看板 §一 加了 ⚠️ 实况记录，并把「补跑一次带 L2/L3 产出的真实会话」设为**进 M2 的前置条件**——否则 Q2 裁定保留的人工闸门没有落地载体。同时 D2（固化触发时机）现状与建议值不一致，已标为待定待裁。

### 5.3 遗留未处置（需你后续拍板）

| 项 | 现状 |
|---|---|
| ★ **ai-platform 内部两本手册对同一常量口径不一**（本轮终验新查出） | ~~`docs\runbooks\GPT镜像站送审流程.md` 第 82 行仍写 ≤12 轮~~ → **✅ 已修复（2026-09-23 用户点头）**：L82 改为 ≤24 轮并补修订注记与真源指向（手册 §9.3），两本操作真源口径已统一为 24；连带把「GPT 出现遗忘/混淆即提前收窗」的告诫同步了进去 |
| 509.8MB 重复目录 `D:\Work\api-gateway\` | **仍原封保留**（Q3 只授权还原删除）。需你决定 canonical 是哪一份、是否登记进 `Home.md`、仓内 `apps\api-gateway\` 是改指针还是保留 |
| ai-platform 仓 6 个 M 文件未提交 | 含我方 2 处失效标注 + 无关的 4 处（`search-gateway\...\api_page.html`、`obsidian-rfc.md`、`01_镜像站GPT操作手册.md`、`api-gateway\docs\README.md`）。**未替你提交**，等你决定是否入库 |
| 上游 D1 / D2 / D4 / D6、下游 D8 | 仍待你表态 |
| 「引用即校验」升级为两仓共同铁律 | 已写入 `AGENTS.md` 硬约束 3 + SOP §三.3（本轮执行时顺手做了）；两仓 `00-项目总览` 的边界节**尚未同步**，等你点头再补 |

---

**相关文档**：`docs\00-项目总览.md`（上游架构）· `docs\01-任务看板.md`（D1~D7 + §七 复盘）· `docs\经验沉淀SOP.md`（沉淀规程，已修订）· `技能库\README.md`（技能归口与撞车检查）· `scripts\快照草稿区.ps1`（每日兜底）· `D:\Work\自适应工作流引擎\docs\02-镜像站试点盘点.md` §八（轮次纪律更正）· `双引擎立项收口交接_给执行Agent_20260919.md`（上一轮交接）
