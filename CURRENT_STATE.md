# CURRENT_STATE

更新：2026-09-25　｜　写者：本地执行模型　｜　真源指针见各节末尾

## 一、系统全景

现在是**三套系统并存，尚未融合**：

| 系统 | 中心对象 | 状态 | 真源 |
|---|---|---|---|
| AI自成长引擎（上游） | 经验条目 L1 / SOP L2 / 技能 L3 | 在跑，产出稀疏 | 本仓 `10_AI自成长引擎/` |
| 自适应工作流引擎（下游） | 契约 Contract | A1 已收口，A2 未开始 | 本仓 `20_自适应工作流引擎/` |
| 元智能 V1（新） | **Demand** | **仅规范，零实现** | 本仓 `30_元智能/` |

外加：微内核路由 `AGENTS.md`（P1 踩坑沉淀 / P2 复杂流程 / P3 收尾 flush）、记忆库、Obsidian 库。

## 二、上游 · AI自成长引擎

- P1 捕获在真实产出，草稿区 L1 共 **4 条**；首轮实测**撞车率 2/3**。
- `技能库/` 是 L3 唯一归口，**当前只有 1 个技能**（`loomy-learned-gpt-mirror-extended-review`，状态：草稿·未安装）。
- 快照每日 23:30、复盘报表每周一 09:00，均已建立。
- 命名铁律 `loomy-learned-<kebab>`；安装五步闸门，第 4 步"复制到运行时目录"需人工点头。
- **本轮新增**：`docs/04-方案_技能统一治理与浏览器能力域.md`（§一~§十四）+ `_中枢/` 三件只读工具。

## 三、下游 · 自适应工作流引擎

- **A1 全绿收口**（2026-09-24）：探针 exit 0，`self_test=pass`，送审链路有活卡（CARD-11 活跃）。
- 契约是声明态唯一真源，含 8 项 conditions / 11 条 gates / baseline 容差 +50% / 自愈分级。
- 探针纪律：只读 GET、**零字面量**、产物只由脚本写、`probe_self_test` 必记、silent-risk 按 break 处理，退出码 0/2/3。
- 期间修掉一个**断言求值器初值 bug**（`$actual` 误写 `$null` → 所有卡恒判不健康），并作废了 09-23 两次 `active=0` 的假数据。
- **待办**：A2（自适应 runner + P2 UI 探针 + 回归计时 ≤14.4s）→ A3（技能安装）→ A5（第二试点：搜索网关）。

## 四、本轮（技能治理）落到的事实

| 项 | 值 |
|---|---|
| 全机 SKILL.md | **8663** 份（含 Loomy 81），87.9 MB |
| 唯一技能 id（按 frontmatter `name`） | 2842，解析缺失率 1.34% |
| 用户侧治理范围 | **832 份（9.6%）** |
| 程序出厂自带（退出范围） | 7831 份 |
| 同名多份簇 / 内容已分叉 | 1613 → 全量 907 → **范围内 31** |
| 死链 | **946 条，全部同一成因** |
| 真源目录技能数 | **1** |

**死链根因**：`C:\Users\<user>\.agents\skills` 这个 hub 被删掉 **149 个**技能目录，散在 **9 个宿主**的 946 条链接无人回收。两条独立实现（reparse 属性+活性探针 / `dir /AL` 解析目标）逐宿主交叉验证一致。

**工具侧自证**：三件只读工具均带 self_test。开发期被自检当场拦下两类静默失效 —— `Test-Path` 对断开的 junction 恒返回 True（会把死链数报成 0）；`Get-ChildItem` 对断链只抛非终止错误（会带着"子项=0"的假结论继续跑）。

## 五、下一步

0. **融合阶段 2 已完成**（2026-09-25 深夜）：Demand 真源迁至 `D:Work元智能machinedemands`，生成链 `check_components → build_state` 跑通，确定性、升版传播、fail-closed 三条均已实测。下一步阶段 3：给两个看板与现存 Task 回填 `demand_id`。
1. **融合阶段 1 已完成**（2026-09-25 深夜）：`D:Work元智能` 控制平面建立，三组件注册 + 判活 + 回读校验跑通，旧文件未动。下一步是阶段 2 —— Demand 真源迁入 `machinedemands`、生成脚本产出 State 与首页。

1. 高智力模型给出三套系统的融合判断（本轮请求，见 `ISSUES.md`）。**材料以对话附件提供** —— 它的抓取层不回源，发 URL 无效（见 §六）。回答第一行须原样复述本轮交付号 `FACTS-DELIVERY-<提交前 8 位>`，否则判定没读到材料。
2. 在此之前**冻结结构性动作**。
3. 唯一可并行推进的是 946 条死链清理 —— 它不依赖任何架构选择，但属不可逆操作，**等人工批准 + dry-run 清单**。

真源指针：`10_AI自成长引擎/docs/01-任务看板.md`、`20_自适应工作流引擎/docs/01-任务看板.md`、`10_AI自成长引擎/技能库/_中枢/conclusions/`

## 六、事实层自身的可达性（2026-09-25 定稿：只记现象与闸门，不记机制）

**现象**：对方读取本仓 raw URL 会**间歇失败** —— 同一条 URL 可在连续数小时内每次返回 `Cache miss`，随后某个时刻起**首次即 200**。失败与成功都不取决于：哪个仓、内容、体积（139 B～200 KB 都出现过两种结果）、路径是否中文、`main` 还是 commit SHA、深路径还是根级、新会话还是会话延续、URL 此前是否被请求过。

**机制未定，且本轮不再猜。** 我在这条因果上连错五次（缓存优先 / size 滞后 / 按仓绑定 / 按内容判定 / 需异步预热），每一次都被下一轮实测打掉 —— 包括"冷 URL 首次即读到"这一条直接否掉了我自己提的"必须等预热"。**因此协议不建立在机制上，建立在可验证的送达闸门上（DEC-11 / DEC-12）。**

### 探针账目（2026-09-25，每条都先在本地验过 200）

| 时段 | 探针 | 结果 |
|---|---|---|
| 白天 | 本仓 5 条 URL（`main`/SHA，139 B～200 KB） | 全部 `Cache miss` |
| 白天 | 另一仓 `listenloop-project` 的 `OPEN-QUESTIONS.md` | 读到 |
| 白天 | 同一"能读的仓"里两条从未请求过的新路径（20 KB 深路径源码 / 5.3 KB 中文路径） | 读不到 |
| 白天 | **全新会话**重复上述 | 仍读不到 |
| 夜间 | 第三方热门仓 raw（`microsoft/vscode`、`openai/openai-python`） | 读到 200 |
| 夜间 | 老而冷门的第三方仓（2008～2012 年建、2~3 星）×3 | **读到 200** |
| 夜间 | **本仓 `README.md`**（白天失败 4 次的那条） | **读到 200** |
| 夜间 | 本仓两个副本 + **从未被请求过的冷哨兵** `PROBE_READABILITY.txt` + 昨天失败的深路径 | **四条全部首次即 200**，首行逐字与本地/线上一致 |

最后一行是关键：它同时否掉了"缓存说""预热说""按仓说""体积说"。剩下唯一能说的是**它的不稳定会自己恢复，而恢复时间不可控**。

### 因此本仓的处置

| 项 | 处置 |
|---|---|
| 承载 | **不动**。保持 GitHub raw URL 为唯一送审入口（用户明确裁定：不接受手动管文件）。副本、Pages、导出仓、connector 全部**不需要**，一个都没建 |
| 送达判据 | 每轮**三项回读凭证**：`delivery_id`（生成时基线提交号）+ `CORE-TAIL-*` + `SPEC-TAIL-*`（两个包各自的末行随机数），由 `build_core_bundle.js` 每轮重新生成。对方第一行必须原样输出三项，缺任一项即判"材料未完整送达"并阻断作答 |
| 重试 | 每条 URL 最多 3 次、间隔 15 秒；三连败即本轮该材料判未送达，不换承载、不凭记忆续答（DEC-12） |
| 换承载的条件 | 只有当某替代承载在**连续 3 个交付轮次**全部通过、而 raw 在其中 ≥2 轮触发三连败，才迁移默认承载。否则保持 raw |
| 元数据坑 | GitHub `size` 在删仓重建后会长时间显示 0，判空只看 `git/trees`；`raw.githubusercontent.com` push 后仍可能回旧提交内容，核对刚推的东西走 `contents` API 或 blob sha |
| 教训 | `git push 成功` 与"对方拿到了"不是一件事，这个等式曾长期写在协议里；DEC-11 把它拆成"已发布"与"已送达"两格 |

真源指针：`DECISIONS.md` DEC-08/DEC-11/DEC-12、`CHANGELOG.md` 2026-09-25 深夜条、`README.md` §送审入口与送达闸门



