# CURRENT_STATE

更新：2026-09-25　｜　写者：本地执行模型　｜　真源指针见各节末尾

## 一、系统全景

现在是**三套系统并存，尚未融合**：

| 系统 | 中心对象 | 状态 | 真源 |
|---|---|---|---|
| AI自成长引擎（上游） | 经验条目 L1 / SOP L2 / 技能 L3 | 在跑，产出稀疏 | 本仓 `10_AI自成长引擎/` |
| 自适应工作流引擎（下游） | 契约 Contract | A1 已收口，A2 未开始 | 本仓 `20_自适应工作流引擎/` |
| 元智能 V1（新） | **Demand** | **仅规范，零实现** | 本仓 `30_元智能/` |

外加：微内核路由 `AGENTS.md`（P1 踩坑沉淀 / P2 复杂流程 / P3 收尾 flush）、记忆库、Obsidian 库。

## 二、上游 · AI自成长引擎

- P1 捕获在真实产出，草稿区 L1 共 **4 条**；首轮实测**撞车率 2/3**。
- `技能库/` 是 L3 唯一归口，**当前只有 1 个技能**（`loomy-learned-gpt-mirror-extended-review`，状态：草稿·未安装）。
- 快照每日 23:30、复盘报表每周一 09:00，均已建立。
- 命名铁律 `loomy-learned-<kebab>`；安装五步闸门，第 4 步"复制到运行时目录"需人工点头。
- **本轮新增**：`docs/04-方案_技能统一治理与浏览器能力域.md`（§一~§十四）+ `_中枢/` 三件只读工具。

## 三、下游 · 自适应工作流引擎

- **A1 全绿收口**（2026-09-24）：探针 exit 0，`self_test=pass`，送审链路有活卡（CARD-11 活跃）。
- 契约是声明态唯一真源，含 8 项 conditions / 11 条 gates / baseline 容差 +50% / 自愈分级。
- 探针纪律：只读 GET、**零字面量**、产物只由脚本写、`probe_self_test` 必记、silent-risk 按 break 处理，退出码 0/2/3。
- 期间修掉一个**断言求值器初值 bug**（`$actual` 误写 `$null` → 所有卡恒判不健康），并作废了 09-23 两次 `active=0` 的假数据。
- **待办**：A2（自适应 runner + P2 UI 探针 + 回归计时 ≤14.4s）→ A3（技能安装）→ A5（第二试点：搜索网关）。

## 四、本轮（技能治理）落到的事实

| 项 | 值 |
|---|---|
| 全机 SKILL.md | **8663** 份（含 Loomy 81），87.9 MB |
| 唯一技能 id（按 frontmatter `name`） | 2842，解析缺失率 1.34% |
| 用户侧治理范围 | **832 份（9.6%）** |
| 程序出厂自带（退出范围） | 7831 份 |
| 同名多份簇 / 内容已分叉 | 1613 → 全量 907 → **范围内 31** |
| 死链 | **946 条，全部同一成因** |
| 真源目录技能数 | **1** |

**死链根因**：`C:\Users\<user>\.agents\skills` 这个 hub 被删掉 **149 个**技能目录，散在 **9 个宿主**的 946 条链接无人回收。两条独立实现（reparse 属性+活性探针 / `dir /AL` 解析目标）逐宿主交叉验证一致。

**工具侧自证**：三件只读工具均带 self_test。开发期被自检当场拦下两类静默失效 —— `Test-Path` 对断开的 junction 恒返回 True（会把死链数报成 0）；`Get-ChildItem` 对断链只抛非终止错误（会带着"子项=0"的假结论继续跑）。

## 五、下一步

1. 高智力模型给出三套系统的融合判断（本轮请求，见 `ISSUES.md`）。**材料以对话附件提供** —— 它的抓取层不回源，发 URL 无效（见 §六）。回答第一行须原样复述本轮交付号 `FACTS-DELIVERY-<提交前 8 位>`，否则判定没读到材料。
2. 在此之前**冻结结构性动作**。
3. 唯一可并行推进的是 946 条死链清理 —— 它不依赖任何架构选择，但属不可逆操作，**等人工批准 + dry-run 清单**。

真源指针：`10_AI自成长引擎/docs/01-任务看板.md`、`20_自适应工作流引擎/docs/01-任务看板.md`、`10_AI自成长引擎/技能库/_中枢/conclusions/`

## 六、事实层自身的可达性（2026-09-25 结案）

**结论：高智力模型的网页抓取层不会回源。一条 URL 读得到读不到，唯一变量是"这条 URL 此前有没有进过它的缓存"** —— 与哪个仓、什么内容、文件多大、路径是否中文、用 `main` 还是 commit SHA，全部无关。详见本节末探针表。

而"对方能否稳定读到事实层"这条，从来不在任何验收项里。

| 项 | 现状 |
|---|---|
| 仓库可见性 | public，97 个 blob，`truncated=false`，匿名可读 |
| 本地侧 11 种 URL 形式实测 | **全部 HTTP 200**（raw ×3 / `github/raw` 跳转 / blob 网页 / 仓库首页 / `contents` API / `git/trees` / repo 元数据）。**注意这只证明"仓库不挡"，不证明"对方读得到"** |
| GitHub `size` 元数据 | **2026-09-25 已自愈为 343**，错误的 `empty=true` 与 `size=0` 均已消失（此前建议"判空只看 `git/trees`"仍然成立） |
| 副本入口 | `ALL_CORE_DOCS.md`（约 40 KB）/ `ALL_SPEC_DOCS.md`（约 200 KB）**对它的抓取层无效**，但仍是**附件上传**的载体，故保留 |
| 唯一有效途径 | ① 对话内上传文件／粘贴文本（本轮即用）② GitHub connector（走授权管道，长期解，待用户决定） |
| 无效并已放弃 | 换 SHA 链接、开 Pages、另建导出仓、等搜索索引收录（本仓 09-25 已在索引里，照样读不到） |

### 对方侧探针（2026-09-25，逐条实测）

| 探针 | URL 特征 | 对方结果 | 本地对照 |
|---|---|---|---|
| A | 本仓 · `ALL_SPEC_DOCS.md` · 约 200 KB · ASCII 路径 · **修复后新建的 URL** | **读不到**，返回 `Cache miss`，未取到 HTTP 状态 | 200 / 203490 B |
| B | **另一仓** `listenloop-project` · `OPEN-QUESTIONS.md` · 9.5 KB | 读到，200，首行逐字一致 | 200 / 9509 B |

**定案的是 D 轮，不是 B 轮。** D1/D2 两条新 URL 都放在"对方声称读得到"的那个仓里，一条是 20 KB 深路径 Flutter 源码（`code/lib/widgets/anki/anki_review_dialog.dart`，任何评审都不会读它），一条是 5.3 KB 的中文路径 md，**两条都读不到**。所以 B 的成功不代表"未缓存也能抓"，只代表**那条 URL 它早见过**（该仓确实是此前送审的对象）。我先前写下的"B 否掉了缓存优先假设"是错的，已撤。

补齐的账：

| # | 仓 | URL 形态 | 对方 | 本地 |
|---|---|---|---|---|
| P1 | facts | SHA 固定 / 43 KB | **读不到** | 200 |
| P2 | facts | SHA 固定 / 200 KB | **读不到** | 200 |
| D1 | listenloop | `main` / 20 KB / 深 ASCII 路径 | **读不到** | 200 |
| D2 | listenloop | `main` / 5.3 KB / **中文路径** | **读不到** | 200 |

变量已被穷举掉：仓、内容、体积（139 B 到 200 KB）、路径语言、分支名 vs SHA —— 全都不决定结果。唯一留下的是**这条 URL 有没有在它的缓存里**。

### 由此，本仓这侧做过的工程全部无效

新建的 URL 更不可能在缓存里，所以以下都白做（高智力模型建议的"另建公开导出仓"也在其中，已劝止）：SHA 固定链接、GitHub Pages 换域名、导出仓、根级 ASCII 单文件入口当抓取目标用、等 GitHub 搜索收录（本仓 09-25 已在索引内，照样读不到）。

两个副本**保留**，但用途变了：不再指望它被抓取，而是作为**附件上传的载体**。`PROBE_READABILITY.txt`（139 B，零项目内容）留在仓里当长期连通性标尺。

有效途径只剩两条：① **对话内上传文件／粘贴文本**（本轮即用，副本已在用户 `Downloads`）；② **GitHub connector**（走授权数据管道，理论可读任意新文件，是长期唯一解，属对方账号侧动作，待用户决定）。

> 协议缺的那一条：现在的同步判据是 `git push 成功`，需要的是 **"对方对这一轮交付物的回读确认"**。本轮我先后写过四条因果（缓存优先 / size 滞后 / 按仓挡 / 按内容挡），前三条都被后续探针打掉 —— 全部是被"push 完就当对方拿到了"放过去的。这就是 `ISSUES.md` Q8。


真源指针：`README.md` §抓取失败的备用入口、`REDACTION.md`、`CHANGELOG.md` 2026-09-25 条、`ISSUES.md` Q8


