【交付号】FACTS-DELIVERY-81b333fc   （= 生成本包时的基线提交号，每轮重新生成都会变）
【粘贴说明】这是 meta-intelligence-facts 仓库的第一个材料包：治理层 7 个文件的完整内容，按阅读顺序拼接。
配套第二个包 ALL_SPEC_DOCS.md（规范层 55 个文件）。两个包合起来覆盖全部阅读清单。
读取方式：直接访问本仓 raw URL（公开）。失败按《作答要求》第 0 条重试，最多 3 次、间隔 15 秒。
仓库地址：https://github.com/201650545/meta-intelligence-facts
若需要仓库内其他文件，请明确说出文件名，我会补贴。

==========================================================================

########## 文件开始：README.md ##########
# 元智能事实层 · meta-intelligence-facts

> 本仓是**本地执行模型与高智力模型之间的共享事实层**。
> 目标：另一个模型刚加入，只读少量核心文件，就能明白"这个需求是什么、系统做到哪、发生了什么、还有什么问题、下一步该讨论什么"。
> **不要靠 git commit 历史推测系统变化** —— commit 是执行历史，`CURRENT_STATE.md` 与 `REPORT_TO_STRATEGIST.md` 才是事实入口。
>
> **本仓角色（2026-09-25 起，DEC-16）**：这里是**掩码后的共享投影**，不是跨系统语义真源。Demand、DECISIONS、ISSUES、CHANGELOG 的原件按裁定迁往 `D:\Work\元智能\machine\`；迁移完成前本仓仍是这些文件的实际写入处，但每次改动都应视作"投影刷新"而不是"写真源"。

## 分工约定

| 位置 | 职责 |
|---|---|
| 本地文件系统 | 执行。结构由执行模型自管，不为可读性设计 |
| **本仓（GitHub）** | **同步事实**。AI 与 AI 之间的共享层 |
| 高智力模型 | 复杂判断、架构、策略 |
| Obsidian | 面向人的 Human View |

协作链路固定：需求变化 → 本地执行 → 验证 → 更新本仓 → 高智力模型读本仓 → 给判断 → 落实到本地 → 再验证 → 再更新本仓。

## 送审入口与送达闸门（2026-09-25 定稿）

**送审就是两条 raw URL，本仓是唯一承载：**

```
https://raw.githubusercontent.com/201650545/meta-intelligence-facts/main/ALL_CORE_DOCS.md
https://raw.githubusercontent.com/201650545/meta-intelligence-facts/main/ALL_SPEC_DOCS.md
```

前者是治理层 7 个文件（约 46 KB），后者是规范层 55 个文件（约 200 KB），合起来就是全部阅读清单。两个副本由脚本从本仓**已掩码**文件拼接生成，改任一源文件后必须用 `build_core_bundle.js` / `build_spec_bundle.js` 重新生成。

**但这个通道会间歇失败，且机制未知。** 实测：同一条 URL 可在数小时内每次返回 `Cache miss`，之后某个时刻起首次即 200；成败与仓库、内容、体积（139 B～200 KB 两端都出现过两种结果）、中文路径、`main` 还是 SHA、新会话还是会话延续、这条 URL 此前有没有被请求过 —— **统统无关**。完整探针账目见 `CURRENT_STATE.md` §六。那一天我给出过五条因果（缓存优先 / size 滞后 / 按仓绑定 / 按内容判定 / 需异步预热），全部被后续实测否掉，所以这里不写机制，只写闸门：

| 闸门 | 规定 |
|---|---|
| 重试 | 每条 URL 最多 3 次、间隔 15 秒；三连败即判该材料"未送达"，阻断作答，不换承载 |
| 送达确认 | 对方回答第一行须原样输出**三项凭证**：`FACTS-DELIVERY-*`（CORE 首行）+ `CORE-TAIL-*`（CORE 末行）+ `SPEC-TAIL-*`（SPEC 末行）。缺任一项 = 材料未完整送达，本轮不作答 |
| 凭证生成 | 每轮由 `build_core_bundle.js` 随机生成，只存本地 `.delivery_nonces.json`（不入库）。没读到材料就写不出凭证 |
| 换承载 | 只有当某替代承载在连续 3 个交付轮次全部通过、而 raw 在其中 ≥2 轮触发三连败，才迁移默认承载；否则保持 raw |

> 大小只写约数：`ALL_CORE_DOCS.md` 内嵌了本文件，写死字节数会自指（改数字 → 副本变大 → 数字又不对）。
> `PROBE_READABILITY.txt`（139 B，零项目内容）是长期连通性哨兵，复测同一件事用它，不必再造材料包。
> **`git push` 成功只代表"已发布"**，不代表"对方已送达" —— 后者由三项凭证判定（DEC-08 v2 / DEC-11 / DEC-12）。


## 阅读顺序（给高智力模型）

1. **`REPORT_TO_STRATEGIST.md`** ← 本轮要你看的那份，先读它
2. `DEMAND.md` — 长期需求与最近变化
3. `CURRENT_STATE.md` — 三套系统现在各自到哪
4. `ISSUES.md` — 需要你判断的问题（带编号，回复编号即可）
5. `DECISIONS.md` — 已定事项，**不必重新讨论**
6. `VERDICTS.md` — 外部裁定的**逐字原文**存档（上面所有摘要的出处）
7. `LOCAL_DRAFT_FUSION_PLAN.md` — 第 1 轮基线（已被裁三处，见其顶部）
8. `LOCAL_DRAFT_ENTRY_LAYOUT.md` — **第 2 轮基线**：融合后的物理形态草案，待反驳
9. `CHANGELOG.md` — 本阶段实际改了什么、为什么、结果如何

## 被融合的三套系统在哪

| 目录 | 是什么 | 状态 |
|---|---|---|
| `10_AI自成长引擎/` | 上游：经验分层沉淀 L1/L2/L3、技能库、五步安装闸门、快照与周报 | 在跑 |
| `20_自适应工作流引擎/` | 下游：契约（声明态真源）+ 只读探针 + drift 投影 + 自愈分级 | A1 收口，A2 待办 |
| `30_元智能/` | 新到的规范包：以 Demand 为中心的元智能 V1 实施规范，**尚未落地** | 待评审 |

## 本轮请求

**请给出把这三套融合成一个系统的方案。** 具体要回答的问题在 `ISSUES.md`，背景与已定约束在 `DECISIONS.md`。

## 两点知情

- **本仓内容已做类型化掩码**：内部服务域名、账号卡标识、本机 profile 令牌、本地端口被替换为 `<MIRROR_HOST>`、`CARD-nn`、`LOCAL_BROWSER_PROFILE` 等占位符。同一原值在全仓恒映射到同一占位符，跨文档可对照；**映射表只在本地，不在本仓**。详见 `REDACTION.md`。
- **原始文件未被修改**。本仓是三套系统的**只读投影副本**，唯一真源仍在本地。改动请勿直接写在这里。
########## 文件结束：README.md ##########

==========================================================================

########## 文件开始：REPORT_TO_STRATEGIST.md ##########
# REPORT_TO_STRATEGIST

阶段：2026-09-24 · 技能治理与三系统融合前置　｜　写者：本地执行模型
补记：2026-09-25 · 材料入口修复（见 §2 末条与 §3 末两行），问题本身未变

## 0. 材料怎么到手（2026-09-25 定稿）

**读这两个链接，失败就重试，每条最多 3 次、间隔 15 秒：**

```
https://raw.githubusercontent.com/201650545/meta-intelligence-facts/main/ALL_CORE_DOCS.md
https://raw.githubusercontent.com/201650545/meta-intelligence-facts/main/ALL_SPEC_DOCS.md
```

**这个通道会间歇失败，我不再给原因。** 一整天里我在这条因果上连错五次：缓存优先 → size 滞后 → 按仓绑定 → 按内容判定 → 需异步预热，每一次都被下一轮实测打掉。最后一枪是夜里那轮：老而冷门的第三方仓、你从未被请求过的本仓哨兵文件、白天失败过四次的本仓 README，**全部首次即 200**。所以能写的只有现象：同一条 URL 可连续数小时失败，之后某个时刻突然全通，恢复时间不可控。

承载不动（你上一轮建议的另建导出仓我没做，Pages 也没开，全部不需要）。改的是**判据**：`git push 成功 = 已发布`，`三项回读凭证齐 = 已送达`。三项是 `FACTS-DELIVERY-*`（CORE 首行）、`CORE-TAIL-*`（CORE 末行）、`SPEC-TAIL-*`（SPEC 末行），每轮随机重生成 —— 末行凭证专治"只摸到头部就当读完了 200 KB"。你 22:20 那一轮已经正确输出过交付号，机制可用。

## 1. 本阶段目标

两件：(a) 把"这台机器 skill 很混乱"从主观感受变成可复现事实并定出治理面；(b) 在动结构之前，为三套系统融合提供决策所需的事实底座。

## 2. 实际完成内容

- 三件**只读**工具落地（普查器 / 范围投影器 / 死链枚举器），共用一份配置解析真源 `lib_contract.ps1`，零硬编码判据，全部判据外置在 `audit.config.yaml`。
- 全机 8663 份 SKILL.md 入册，逐条给出身份键、分类、范围判定与证据文件路径。
- 按用户新裁定政策（**只管用户侧资产，程序出厂自带者退出范围**）重建治理口径。
- 死链逐条定性，根因定位到单一事故。
- 方案文档 `10_AI自成长引擎/docs/04-方案_...md`（§一~§十四），含两次自我更正记录。
- 本事实层建仓：84 个文件，含三套系统核心文档，全量类型化掩码。
- **（2026-09-25 补）** 两个根级 ASCII 合并副本 `ALL_CORE_DOCS.md`（约 40 KB，治理层 7 文件）/ `ALL_SPEC_DOCS.md`（约 200 KB，规范层 55 文件），由脚本从本仓**已掩码**文件拼接，作为抓取通道的稳定入口。

## 3. 验证结果

| 验证 | 结果 |
|---|---|
| 普查可复现 | 两轮逐项一致（8663 / 907 / 946） |
| 死链双实现交叉 | reparse+活性探针 vs `dir /AL` 解析目标，**逐宿主计数全部一致** |
| 与第三方工具对账 | 独立 bash `find` 通道在 `.trae` 上复现 149/190，与工具吻合 |
| self_test | 三件工具均 pass；开发期两次真实拦下自伤性 bug 并 exit 3 拒发结论 |
| 脱敏 | **23 类**规则复扫：掩码目标 21 类 **0 命中**；余 2 类为有意保留项（本机用户名 225 次全在路径内、散文 0 次；"私有化"类别词若干行）。详见 `REDACTION.md` |
| 越界检查 | 全程只读；未删/未移/未改任何被检对象；未提交任何人的既有仓库 |
| **（补）仓库可达性** | 匿名实测 11 种 URL 形式全 200：raw ×3 连测、`github/raw` 跳转、blob 网页、仓库首页、`contents` API、`git/trees?recursive=1`（97 blob / `truncated=false`）、repo 元数据 |
| **（补）扫描器计数单位** | 原把"命中行数"标成"处"，导致对外报数偏小（220 vs 225）。已改为行数与次数分别输出，并用第二个独立计数实现交叉验证，两者同为 225 |
| **（补 2）对方侧读取探针（约 15 条，只记现象）** | 白天本仓与旧仓全部 `Cache miss`（含 SHA 固定、139 B～200 KB、中文路径、新会话）；夜里同样的 URL 加上老而冷门的第三方仓、从未被请求过的哨兵文件**全部首次即 200**，首行逐字与本地/线上一致。变量已全部穷举并排除，不可归因 |


## 4. 当前系统状态

三套系统并存、零融合代码。治理面经两次收窄后很小：**832 份在范围、31 个可治理分叉簇、946 条死链**。而真源目录只有 **1 个**技能，Loomy 却在持续自动长出 learned 技能（本阶段从 9 涨到 10）。

## 5. 新发现的问题或风险

1. **状态真源已有四处重叠**：记忆库 `STATE`、上游 `01-任务看板`、下游 `runtime/*.drift.json`，加上本协议要求的 `CURRENT_STATE.md`。不先定映射就落地，会精确复现我们刚花两小时治好的那个病（副本静默分叉）。
2. **规范包自相矛盾的风险点**：它一方面要求"机器层必须有稳定 ID / 索引 / 归档契约"，另一方面把"文件如何组织"完全下放给执行模型。今晚 946 条死链正是"批量建立引用、无人回收"的产物 —— 这个口子不堵，同型事故会重演。
3. **自动产出与人审闸门天然冲突**：Loomy 持续生成 learned 技能，而体系要求"安装需点头"。两者没有既定接口，目前靠运气。
4. **便宜模型的判定可信度未被列为验收项**：规范包 V1 唯一的验证问题问的是"需求能否不依赖用户管文件而持续运行"，没问"执行模型能否可靠判定"。而本阶段两次事故都是**判定器坏了却产出合理假结论**——同型风险最高。
5. **敏感面**：该体系的事实描述必然含第三方服务域名、账号卡标识、企业私有化域名。现有密钥扫描器一条都不命中。本仓已按类型掩码，但映射表留在本地，长期需固化成 push 前强制闸门。
6. **（2026-09-25 新增，同日五次更正后定稿）协作链路的送达没有任何验收项，而我在这条上连错五次因果。** 现象是对方读取 raw URL 间歇失败且不可归因（见 §0 与 `CURRENT_STATE.md` §六）。要点不在机制，而在协议原来把 `git push 成功` 当成"事实已同步" —— 现已拆成"已发布"与"已送达"两格，后者由三项回读凭证判定（DEC-08 v2 / DEC-11 / DEC-12）。教训已入库：**给对方读的文档里，未经验证的因果不能写成结论；写现象和不相信任的判据，比写一个漂亮机制安全。**
   附带实测到：**`git push --force` 不能清除已公开的游离提交**（按 SHA 仍返回 200），只能删仓重建 —— 已写进 `REDACTION.md` 教训二。今后不再为刷新内容而删仓。

## 6. 仍未确定的事项

见 `ISSUES.md` 的 Q1–Q7。其中 Q1（融合后谁是状态唯一真源）是其余问题的前置。

## 7. 建议的下一步

**先定 Demand，再定真源归属，最后才谈结构合并。** 我的倾向是不做"三合一重写"，而是让元智能补上位：它只提供 Demand 层与 Human View 标准，两套引擎继续作为各自的执行/沉淀真源，`CURRENT_STATE.md` 退化为"摘要 + 指针"而非新的状态所有者。

**在 Q1 有答案前，建议冻结一切结构性改动**；唯一可并行的是 946 条死链清理（不依赖架构选择）。

## 8. 需要高智力模型判断的问题

`ISSUES.md` 共 7 条，请按编号回答。**Q1 / Q2 / Q5 / Q6 必须给明确结论**（不许"取决于情况"），Q3 / Q4 / Q7 可给倾向 + 判据。另需你帮着定义 `DEMAND.md` 的 **D-003**。

> 更正：本节原写"Q1/Q2/Q3 是本轮的核心"，与 `PROMPT_FOR_GPT.md` 和 `ISSUES.md` 的口径（Q1/Q2/Q5/Q6 必答）不一致，已按后两者统一。Q3 降为次级，因为它只影响人的阅读体验，不影响系统能否跑。
########## 文件结束：REPORT_TO_STRATEGIST.md ##########

==========================================================================

########## 文件开始：DEMAND.md ##########
# DEMAND

> **原件位置（2026-09-25 阶段 2 起）**：`D:Work元智能machinedemandsD-00x.yaml`。本文件是**投影**，改需求请改那边，改这里会被下次生成覆盖。

> 格式：让 ______ 长期保持 ______，并在 ______ 变化时自动重新评估。

## D-001 · 个人 AI 作业体系的可控性

让本机所有 AI Agent 的作业体系长期保持**单一真源可追溯、引用不悬空、规则与技能不分叉**，并在新增 Agent 宿主、程序版本更新、或引入新规范包时自动重新评估。

**why**：用户单人运维，判断带宽是唯一稀缺资源。体系一旦分叉，表现不是报错而是"另一个工具不守规矩了"，排查成本极高。

**success_state**：任一时刻能回答"X 的真源在哪、谁在引用它、引用是否有效"；新增复杂度必须自证降低了长期维护成本。

**constraints**：不可逆操作与共享状态变更需逐次人工批准；技能安装必须人审点头；文档结论必须可回指证据。

**当前版本变化（2026-09-24）**：新增一条明确边界 —— **治理范围只含用户侧资产（自产 + 主动装 + 工具代装），程序出厂自带者退出范围，只登记不治理**。

## D-002 · 送审链路的可用性与判断质量

**原件**：`D:\Work\元智能\machine\demands\D-002.yaml`（本节为投影，2026-09-26 手工同步，CT-08 完成后改生成）

**statement**：让"向高智力模型送审方案"这条链路长期保持有活卡可发、档位确实生效、结论确实被读到，并在账号池轮换、站点改版、档位文案漂移时自动重新评估。

**why**：这是体系里唯一的外部智力来源。链路断了不会报警，只会静默降级成"用便宜模型的答案"。

**constraints**：判对方故障前必须先自证读取侧；发问前必须核对模型档位，禁止用默认档提问；上下文走 GitHub 投影，不在聊天窗口贴长文；**送达判据是三项回读凭证齐，不是 `git push` 成功**；每条链接最多重试 3 次、间隔 15 秒，三连败即判未送达并阻断作答。

**v1 → v2 的变化（2026-09-26）**：新增后两条约束（来自 CT-06 实验前的同一天实测），并把两类读取侧失效写进 `observed_drift`：镜像站在对话末尾留空的 assistant 占位节点、长对话离屏节点被虚拟化清空 `innerText`。二者都曾让提取脚本报"空回复"，实际是读取侧缺陷而非对方故障。

**已观测到的漂移事实**：站点侧 `isHealthy` 与 `message` 不同义（存在"受限但仍活跃"的卡）；池页对无浏览器指纹的纯 GET 返回 1 字节 SPA 壳，HTML 正则发现路径**永久无效**；`raw.githubusercontent.com` 同一 URL 可连续数小时失败后突然全通，机制未定。

**status**：active　**version**：2　**changed_at**：2026-09-26

## D-003 · 三套系统融合（2026-09-25 由外部裁定定义，本地采纳）

**statement**：让一个长期 Demand 在元智能、执行验证引擎与经验沉淀引擎之间长期保持**统一语义、明确归属、可追溯联动且无重复可写真源**，并在任一子系统的契约、职责边界或能力发生变化时自动重新评估三者映射。

**success_state**：任一 active Demand 都能沿一条确定链路回答 —— 当前 Demand 是什么 → 当前 State 是什么 → 哪些 Task/Experiment 正在执行 → 哪个原生系统提供证据 → 哪些结果已沉淀为可复用经验；同一事实不存在两个可独立修改的正式真源；任何视图都能回指原生证据；任一子系统可被替换而不改变 Demand 的语义。

**constraints**：不新增第五份可写状态真源；保留已验证的下游契约/探针机制与上游人审、经验沉淀机制；元智能只拥有跨系统语义，不吞并各系统的执行真源；用户不需要管理机器层文件结构；新增对象、字段与自动化必须通过"降低认知成本、服务 Demand、降低长期维护成本、有真实证据"四道 Gate。

**recheck_on**：任一子系统更换其 SSOT/契约；Demand 模型或稳定 ID 规则变化；State 投影与原生事实出现无法自动消解的不一致；同一事实再次双写；新的执行/沉淀系统加入或旧系统退出；Human View 无法从 Current State 确定性生成；一次真实 Demand 变化无法完整传播到 Task/Experiment、State 与 Human View。

刻意不与 D-001（全机 AI 资产治理）、D-002（送审链路可用性）重叠：D-003 只管**三套系统之间的语义融合与职责边界**。

**status**：active　**version**：1　**changed_at**：2026-09-25　**来源**：`ISSUES.md` Q8 轮次高智力模型作答，本地采纳并登记为 DEC-13

## D-004 · 经验资产的质量与可复用性（2026-09-26 新增，原件在控制平面）

**statement**：建立经验资产从原始想法、策略抽象、实施骨架到真实复用与衰减退出的闭环，使沉淀结果可追溯、分层正确、质量可验证，并能在后续真实任务中产生可证明的复用价值。

**success_state**：一条经验能完整经历 来源保留 → 候选抽取 → L2/L3 判层 → 生成登记 → 真实任务复用 → 结果反馈 → 衰减/保留/淘汰；错误抽取、错层、无效复用都可被发现，且不会静默污染正式经验资产。

**constraints**：原始想法不得因抽象而丢失；不得凭模型臆测补经验；正式晋升继续遵守既有人审与验证闸门；不负责资产身份/权限/真源/分叉治理（归 D-001）；不吸收系统融合问题（归 D-003）。

**recheck_on**：L2/L3 定义变化；抽取或复用误判明显增加；衰减规则改变；经验资产格式或承载方式改变；上游引擎被替换；真实复用结果持续证明现有质量判据失效。

**status**：active　**version**：1　**changed_at**：2026-09-26　**来源**：`VERDICTS.md` 第 4 轮裁定 1，登记为 DEC-19
########## 文件结束：DEMAND.md ##########

==========================================================================

########## 文件开始：CURRENT_STATE.md ##########
# CURRENT_STATE

更新：2026-09-25　｜　写者：本地执行模型　｜　真源指针见各节末尾

## 一、系统全景

现在是**三套系统并存，尚未融合**：

| 系统 | 中心对象 | 状态 | 真源 |
|---|---|---|---|
| AI自成长引擎（上游） | 经验条目 L1 / SOP L2 / 技能 L3 | 在跑，产出稀疏 | 本仓 `10_AI自成长引擎/` |
| 自适应工作流引擎（下游） | 契约 Contract | A1 已收口，A2 未开始 | 本仓 `20_自适应工作流引擎/` |
| 元智能 V1（新） | **Demand** | **仅规范，零实现** | 本仓 `30_元智能/` |

外加：微内核路由 `AGENTS.md`（P1 踩坑沉淀 / P2 复杂流程 / P3 收尾 flush）、记忆库、Obsidian 库。

## 二、上游 · AI自成长引擎

- P1 捕获在真实产出，草稿区 L1 共 **4 条**；首轮实测**撞车率 2/3**。
- `技能库/` 是 L3 唯一归口，**当前只有 1 个技能**（`loomy-learned-gpt-mirror-extended-review`，状态：草稿·未安装）。
- 快照每日 23:30、复盘报表每周一 09:00，均已建立。
- 命名铁律 `loomy-learned-<kebab>`；安装五步闸门，第 4 步"复制到运行时目录"需人工点头。
- **本轮新增**：`docs/04-方案_技能统一治理与浏览器能力域.md`（§一~§十四）+ `_中枢/` 三件只读工具。

## 三、下游 · 自适应工作流引擎

- **A1 全绿收口**（2026-09-24）：探针 exit 0，`self_test=pass`，送审链路有活卡（CARD-11 活跃）。
- 契约是声明态唯一真源，含 8 项 conditions / 11 条 gates / baseline 容差 +50% / 自愈分级。
- 探针纪律：只读 GET、**零字面量**、产物只由脚本写、`probe_self_test` 必记、silent-risk 按 break 处理，退出码 0/2/3。
- 期间修掉一个**断言求值器初值 bug**（`$actual` 误写 `$null` → 所有卡恒判不健康），并作废了 09-23 两次 `active=0` 的假数据。
- **待办**：A2（自适应 runner + P2 UI 探针 + 回归计时 ≤14.4s）→ A3（技能安装）→ A5（第二试点：搜索网关）。

## 四、本轮（技能治理）落到的事实

| 项 | 值 |
|---|---|
| 全机 SKILL.md | **8663** 份（含 Loomy 81），87.9 MB |
| 唯一技能 id（按 frontmatter `name`） | 2842，解析缺失率 1.34% |
| 用户侧治理范围 | **832 份（9.6%）** |
| 程序出厂自带（退出范围） | 7831 份 |
| 同名多份簇 / 内容已分叉 | 1613 → 全量 907 → **范围内 31** |
| 死链 | **946 条，全部同一成因** |
| 真源目录技能数 | **1** |

**死链根因**：`C:\Users\<user>\.agents\skills` 这个 hub 被删掉 **149 个**技能目录，散在 **9 个宿主**的 946 条链接无人回收。两条独立实现（reparse 属性+活性探针 / `dir /AL` 解析目标）逐宿主交叉验证一致。

**工具侧自证**：三件只读工具均带 self_test。开发期被自检当场拦下两类静默失效 —— `Test-Path` 对断开的 junction 恒返回 True（会把死链数报成 0）；`Get-ChildItem` 对断链只抛非终止错误（会带着"子项=0"的假结论继续跑）。

## 五、下一步

0. **融合阶段 3a 已完成**（2026-09-26）：两个看板 34 条目逐条判定，13 条无法归属被归因到 Demand 集合缺口；外部裁定新增 **D-004**、允许 `scope: local`、授权控制平面自持 `machineTASKS.yaml`。首页已能派生「下一步 / 活动任务 / 阻断项」。**CT-04 与 CT-09 已完成**：两个看板先建 git 基线（b7d37b4），再各追加一节机器可读 `task_bindings`（35 条，表格本体未动，删除行数 0）。首页已能派生跨系统活动任务：D-001 2／D-002 8／D-003 5／D-004 10。**第 5 轮（阶段 6~7 收口）裁定已入库**：DEC-22~26；`delivery_readback` 首次真实通过并转正，`fetch_retry_budget` 保持未验证；EXPERIMENTS 已纳入 State 输入（`[]` 与 `null` 语义定死）；入口切换真实前置是 3 项阻断（CT-07/CT-12/CT-13）。此前 **CT-11 已完成**：送达判据成为可执行校验 `verify_delivery.mjs`（自检 4/4 + 真实产物正负例），契约 v1→v2 加两条闸门（标 `verified_at: null` 待下一轮真实送审确认），skill 与看板 T11/T14 同步补判据。**CT-06 已实测通过**（D-002 v1→v2，五项判据全中、耗时 <1 秒，见 `EXPERIMENTS/EXP-20260926_demand-version-propagation.md`），并因此补上追加式重判队列 —— 原先标记会在下次构建归零，等于信号闪一下就没了。（2026-09-26）：见下条。（2026-09-25 深夜）：Demand 真源迁至 `D:Work元智能machinedemands`，生成链 `check_components → build_state` 跑通，确定性、升版传播、fail-closed 三条均已实测。下一步阶段 3：给两个看板与现存 Task 回填 `demand_id`。
1. **融合阶段 1 已完成**（2026-09-25 深夜）：`D:Work元智能` 控制平面建立，三组件注册 + 判活 + 回读校验跑通，旧文件未动。下一步是阶段 2 —— Demand 真源迁入 `machinedemands`、生成脚本产出 State 与首页。

1. 高智力模型给出三套系统的融合判断（本轮请求，见 `ISSUES.md`）。**材料以对话附件提供** —— 它的抓取层不回源，发 URL 无效（见 §六）。回答第一行须原样复述本轮交付号 `FACTS-DELIVERY-<提交前 8 位>`，否则判定没读到材料。
2. 在此之前**冻结结构性动作**。
3. 唯一可并行推进的是 946 条死链清理 —— 它不依赖任何架构选择，但属不可逆操作，**等人工批准 + dry-run 清单**。

真源指针：`10_AI自成长引擎/docs/01-任务看板.md`、`20_自适应工作流引擎/docs/01-任务看板.md`、`10_AI自成长引擎/技能库/_中枢/conclusions/`

## 六、事实层自身的可达性（2026-09-25 定稿：只记现象与闸门，不记机制）

**现象**：对方读取本仓 raw URL 会**间歇失败** —— 同一条 URL 可在连续数小时内每次返回 `Cache miss`，随后某个时刻起**首次即 200**。失败与成功都不取决于：哪个仓、内容、体积（139 B～200 KB 都出现过两种结果）、路径是否中文、`main` 还是 commit SHA、深路径还是根级、新会话还是会话延续、URL 此前是否被请求过。

**机制未定，且本轮不再猜。** 我在这条因果上连错五次（缓存优先 / size 滞后 / 按仓绑定 / 按内容判定 / 需异步预热），每一次都被下一轮实测打掉 —— 包括"冷 URL 首次即读到"这一条直接否掉了我自己提的"必须等预热"。**因此协议不建立在机制上，建立在可验证的送达闸门上（DEC-11 / DEC-12）。**

### 探针账目（2026-09-25，每条都先在本地验过 200）

| 时段 | 探针 | 结果 |
|---|---|---|
| 白天 | 本仓 5 条 URL（`main`/SHA，139 B～200 KB） | 全部 `Cache miss` |
| 白天 | 另一仓 `listenloop-project` 的 `OPEN-QUESTIONS.md` | 读到 |
| 白天 | 同一"能读的仓"里两条从未请求过的新路径（20 KB 深路径源码 / 5.3 KB 中文路径） | 读不到 |
| 白天 | **全新会话**重复上述 | 仍读不到 |
| 夜间 | 第三方热门仓 raw（`microsoft/vscode`、`openai/openai-python`） | 读到 200 |
| 夜间 | 老而冷门的第三方仓（2008～2012 年建、2~3 星）×3 | **读到 200** |
| 夜间 | **本仓 `README.md`**（白天失败 4 次的那条） | **读到 200** |
| 夜间 | 本仓两个副本 + **从未被请求过的冷哨兵** `PROBE_READABILITY.txt` + 昨天失败的深路径 | **四条全部首次即 200**，首行逐字与本地/线上一致 |

最后一行是关键：它同时否掉了"缓存说""预热说""按仓说""体积说"。剩下唯一能说的是**它的不稳定会自己恢复，而恢复时间不可控**。

### 因此本仓的处置

| 项 | 处置 |
|---|---|
| 承载 | **不动**。保持 GitHub raw URL 为唯一送审入口（用户明确裁定：不接受手动管文件）。副本、Pages、导出仓、connector 全部**不需要**，一个都没建 |
| 送达判据 | 每轮**三项回读凭证**：`delivery_id`（生成时基线提交号）+ `CORE-TAIL-*` + `SPEC-TAIL-*`（两个包各自的末行随机数），由 `build_core_bundle.js` 每轮重新生成。对方第一行必须原样输出三项，缺任一项即判"材料未完整送达"并阻断作答 |
| 重试 | 每条 URL 最多 3 次、间隔 15 秒；三连败即本轮该材料判未送达，不换承载、不凭记忆续答（DEC-12） |
| 换承载的条件 | 只有当某替代承载在**连续 3 个交付轮次**全部通过、而 raw 在其中 ≥2 轮触发三连败，才迁移默认承载。否则保持 raw |
| 元数据坑 | GitHub `size` 在删仓重建后会长时间显示 0，判空只看 `git/trees`；`raw.githubusercontent.com` push 后仍可能回旧提交内容，核对刚推的东西走 `contents` API 或 blob sha |
| 教训 | `git push 成功` 与"对方拿到了"不是一件事，这个等式曾长期写在协议里；DEC-11 把它拆成"已发布"与"已送达"两格 |

真源指针：`DECISIONS.md` DEC-08/DEC-11/DEC-12、`CHANGELOG.md` 2026-09-25 深夜条、`README.md` §送审入口与送达闸门
########## 文件结束：CURRENT_STATE.md ##########

==========================================================================

########## 文件开始：DECISIONS.md ##########
# DECISIONS

已正式确定的判断。**列出者请勿重新讨论**，要推翻请显式写明推翻理由并升版本。

| # | 决策 | 定于 | 理由 | 可推翻条件 |
|---|---|---|---|---|
| DEC-01 | 治理范围**只含用户侧资产**（自产 + 主动装 + 工具代装）；程序出厂自带者退出范围，只登记不治理 | 2026-09-24 | 用户裁定。物理去重全部收益是 87.9MB 与目录观感，代价是版本静默错配 + 集中后爆炸半径从 1 变 N | 若某程序提供官方技能目录重定向能力，可重评 |
| DEC-02 | 判据优先级：出厂标记 → 自产 → 工具代装 → 声明根 → 默认**范围外** | 2026-09-24 | 宁可漏判，也不把程序资产误纳进来后遭误删。出厂标记排在声明根之前，因"自带不可动"是下限，不该被一条清单条目绕过 | 出现证据可判别的新类别时增补 |
| DEC-03 | 技能身份键用 frontmatter `name:`，目录名仅兜底 | 2026-09-24 | 目录名同时虚增（包装目录 `.../skills/` 被当成一个叫 `skills` 的技能，聚出 193 份假簇）与漏报（Anthropic `docx` → 本地 `loomy-docx` 这类改名副本识别不出） | 无 |
| DEC-04 | 政策层与事实层分离：范围判定放在**投影器**（秒级），不放在普查器（6 分钟） | 2026-09-24 | 政策会反复调，重扫全盘代价不对称 | 无 |
| DEC-05 | 配置解析器**单一真源** `lib_contract.ps1`，多脚本 dot-source 共用 | 2026-09-24 | 治理方案自身不能犯它要治理的错（两份副本静默分叉） | 无 |
| DEC-06 | 判活必须用"穿过链接"的活性探针；**禁用** `Test-Path` 判 junction | 2026-09-24 | `Test-Path` 与 `[IO.Directory]::Exists` 对断开的 junction 一律返回 True，用它判活会把死链数报成 0（实踩） | 无 |
| DEC-07 | `Get-ChildItem` 类命令不得用于"靠异常判定"的场景 | 2026-09-24 | 它只抛非终止错误，`try/catch` 接不住，会带着"子项=0"的假结论继续跑 —— silent-risk 的标准形态 | 无 |
| DEC-08 | GitHub 作为 **AI 间共享事实层**；每阶段至少同步 `CURRENT_STATE` / `CHANGELOG` / `REPORT_TO_STRATEGIST` | 2026-09-24 | 用户裁定。commit 是执行历史，不是事实入口；不让高智力模型靠 diff 猜系统变化 | **v2（2026-09-25）**：本行只管"已发布"，"已送达"另判，见 DEC-11 |
| DEC-09 | 本仓内容**必须经类型化掩码**后才可推 | 2026-09-24 | 现有密钥扫描器不覆盖内部域名/账号卡/profile/端口（实测 0 命中，即完全漏检） | 若仓库永久私有且威胁模型收窄，可放宽 |
| DEC-10 | 上游引擎的**快照**取代 git 作为该仓的可回溯机制 | 更早，沿用 | 该仓裁定不进 git；每日 23:30 快照保留 14 份 | 若需要跨机协作则重评 |
| DEC-11 | **送达判据 = 三项回读凭证**（`delivery_id` + `CORE-TAIL-*` + `SPEC-TAIL-*`），不是 `git push` 成功 | 2026-09-25 | 高智力模型裁定并已由本地实现。同一 URL 实测可连续数小时失败后突然成功，机制未知 → 只能靠可验证的送达闸门，不能靠架构改动。凭证由 `build_core_bundle.js` 每轮随机生成，读不到就写不出 | 若某轮换承载（Pages／导出仓／附件），新承载须当场通过同三项回读才算切换成功；若首行 `delivery_id` 命中的是**上一轮**而尾凭证不符，判为"读到旧版本"（`raw.githubusercontent.com` push 后有 CDN 滞后），等 10 分钟重跑同一链接，不重发材料也不换承载 |
| DEC-12 | 读取失败的重试上限 **3 次、间隔 15 秒**；3 次仍失败即判"材料未完整送达"并**阻断作答**，不换承载、不凭记忆续答 | 2026-09-25 | 高智力模型裁定。无限重试无意义（已观测到数小时级故障），但一次失败也不足以否掉整轮 | 若同类三连败在连续 3 个交付轮次里出现 ≥2 轮，才启动换承载评估 |
| DEC-13 | **Demand 由元智能层持有**，上游/下游只持 `demand_id` 指针；不新增第五份可写真源 | 2026-09-25 | 高智力模型驳回本地草案"在上游新建 `demands/<id>.yaml`"：会让子系统成为整体语义的所有者 | 若三套系统被证明不共享任何长期目标，Demand 失去上位价值 |
| DEC-14 | 自动产物**先进隔离候选区**，任何改变运行时能力/正式技能库/共享真源的晋升与安装**必须人工批准** | 2026-09-25 | 高智力模型裁定。生成与生效分离，既保留自动学习又不破坏五步安装闸门第 4 步 | 出现可证明等价于人审的机器策略时，须先作为 Experiment 验证，不得直接取消人审 |
| DEC-16 | 元智能**控制平面** `D:\Work\元智能\` 是跨系统语义真源：Demand、DECISIONS、ISSUES、CHANGELOG 的 machine 层原件都在它里面；公开事实层仓与 Obsidian 首页一律是**生成投影**。证据层**禁止 junction/symlink**，改用机器生成的索引页 | 2026-09-25 | 高智力模型裁定（第 2~3 轮）。946 条死链已证明「批量链接、无人回收」是故障源，再造一层链接等于引入第三种失效模式；且既有规则是 Obsidian 只解释与索引，不反向充当运行事实源 | 若三套执行系统真的共享同一发布周期与依赖图，且实验证明 monorepo 降低维护成本又不破坏任何原生 SSOT，才重审 |
| DEC-17 | 生成事务边界 = 本地真源 → 事实层投影 → CORE/SPEC 副本 → 掩码扫描（**扫描失败即阻断**）；`git commit/push` 由 Agent 执行但**保留人工批准发布**。首页分列「本地状态」与「外部送审状态」：投影过期或发布失败只阻断外部裁定，**不否定本地 State** | 2026-09-25 | 高智力模型裁定（第 3 轮 c）。GPT 只能读公开投影，投影过期就会拿到假裁定，所以外部送达必须与本地状态分开显示 | 若公开仓升格为本地 canonical 仓库且同时撤销原本地存储，可合并两格；不得双持 |
| DEC-18 | **第一版范围**：必须有 `project.yaml` + Demand 真源 + 单一生成脚本 + State/首页生成 + 失败显式失效 + 生成后回读 + 输入指纹（先用排序后输入文件的 SHA-256，完整 `input_hash` 规则后补）。**可后补**：`state_dirty` 事件总线、10 分钟 watchdog、独立 `schemas\` 与 `adapters\` 目录。过渡期靠「执行 Agent 每次收尾必须生成一次并回读产物」兜底 | 2026-09-25 | 高智力模型裁定（第 3 轮 a）。单人系统，控制平面不能比业务重 | 一旦出现「有程序写真源但不走这条收尾规则」的实例，立刻补 watchdog |
| DEC-19 | 新增 **D-004「经验资产的质量与可复用性」**，不并入 D-001 | 2026-09-26 | 高智力模型裁定（第 4 轮）。上游九条核心任务无处归属，暴露的是 Demand 集合缺一块，不是判定失误；塞进 D-001 会把「治理」与「质量」混成垃圾桶。边界判据：问「这条经验对不对、有用吗、可复用吗」归 D-004；问「这份资产唯一吗、可控吗、可追踪吗」归 D-001 | 只有 D-001 正式升版并把经验质量纳入自己的独立 success_state、且 D-004 不再有独立生命周期与指标时，才合并 |
| DEC-20 | Task 归属规则修正：`demand_id` **只允许真实 Demand ID**；系统内部建设改用 `scope: local` + `system_id` + `demand_id: null`。局部 Task 不进任何 Demand 的 `active_tasks`，只有真正阻塞时才以 `blocking_dependency` 出现 | 2026-09-26 | 高智力模型裁定（第 4 轮）。「所有 Task 必须挂 Demand」是过强约束，硬挂等于逼执行模型伪造归属 —— 那正是 946 死链式的静默污染 | 一旦某 local 任务开始改变跨系统契约、用户级能力或某 Demand 的成功条件，必须转 global 并绑真实 Demand |
| DEC-21 | 控制平面自持原生 Task 台账 `D:\Work\元智能\machine\TASKS.yaml`（阶段 3~7 等，绑 D-003/D-004），**不把两个引擎看板的条目抄进来** | 2026-09-26 | 高智力模型裁定（第 4 轮）。判据：谁实际拥有执行与完成判定，谁持有该 Task 的原生记录。不违反「不新增第五份可写状态真源」—— 它不是第二份 Current State，也不复制他系统 Task；违规的是把下游 Task 抄一份形成双写 | 若未来建立全系统唯一 Task 服务并完成迁移、旧台账退役，则并入；在此之前不跨系统借看板 |
| DEC-22 | 切默认入口的前置条件固定为四项：CT-05（普通 Task/Decision 变化也要走通 State→首页）、T15/T16 退出 active blocker、State 已知缺口消失（见 DEC-26）、切换与回滚脚本实测通过。**切换不移除任何旧路径**：`Home.md` 原地改成生成的兼容入口并指向新首页，Obsidian 默认打开文件指向新首页 | 2026-09-26 | 高智力模型裁定（第 5 轮 P1）。规范只要求 Obsidian 有一个默认 Human View，没要求物理移动旧页面；移动才会制造断链 | 切换后出现旧链接失效、首页比 State 旧、生成失败仍展示正常状态、或任一 active 全局 Task 无可靠归属 → 立即 rollback |
| DEC-23 | 去旧写口顺序固定 **CHANGELOG → DEMAND → DECISIONS → ISSUES**（ISSUES 最后）。生成频率分两层：Agent 收尾刷 machine 并重生成四份投影；**准备送审时无条件再生成一次**，再出 CORE/SPEC、掩码扫描、freshness 校验，全过才允许 commit/push | 2026-09-26 | 高智力模型裁定（第 5 轮 P2）。ISSUES 是外部模型的实时输入，生成时机错一轮就会让它回答旧问题；CHANGELOG 只是已发生事项，迁移错误即时影响最小 | 某文件存在 machine 原件未承载、而公开旧文件独有的有效事实 → 停止迁移，先把事实迁回 machine，不许靠继续双写解决 |
| DEC-24 | 迁移期双写的机器判据：**machine 永远胜，公开层绝不反向合并**。每个投影登记 `source / source_sha256 / projection_sha256 / generator_version / build_id`；每次收尾与每次 pre-push 执行"读源算哈希 → 临时目录重渲染 → 比对期望与实际投影"，两条件任一失败即 exit 非 0 并禁止打包 push。不一致时先把异常版本复制到 `runtime/divergence/<时间戳>/` 留证，再用 machine 真源覆盖 | 2026-09-26 | 高智力模型裁定（第 5 轮 P3）。这套比对还能区分两类错误：源变了投影没变=投影陈旧；源没变而实际≠重渲染=公开层被手改 | 公开仓被正式升格为 canonical 并同时撤销 machine 原件，才允许反转权威方向；当前架构下绝不双向同步 |
| DEC-25 | **第一版不加 manifest**（文件数/字节数/SHA-256）。触发条件：出现一次可复现案例——三项本轮 nonce 全匹配，但中间内容缺失、串包或与本地 bundle 不一致。届时加独立 manifest，仍不替代三个 nonce | 2026-09-26 | 高智力模型裁定（第 5 轮 P5）。现在加只能覆盖一个尚未出现过的中段损坏故障模式，违反"不因假设性风险增加维护复杂度" | 发布或抓取通道自身提供可在接收侧核验的端到端内容哈希时，无需自建 |
| DEC-26 | **EXPERIMENTS 纳入 State 输入**。每份实验按既有 schema 补可解析字段（`id / demand_id / hypothesis / baseline / candidate / status`）；`planned|running` 进 `active_experiments`，`completed|cancelled` 不进，其结果进 `recent_changes`。语义固定：**`[]` 表示机器确认当前无活动实验，`null` 表示生成器不知道**，二者不得混用 | 2026-09-26 | 高智力模型裁定（第 5 轮 P4）。今天已真实产出两份实验，规范把 Experiment 定为 V1 一等对象且 State 明确含"当前实验"，继续写 null 等于用"不知道"冒充"没有" | 实验不再是一等运行对象且生命周期被另一正式对象完全取代，才移出输入；当前 V1 规范下不能省 |
| DEC-27 | 事件覆盖判据固定：State 必须记录 `changed_inputs` 与 `change_kind`，能区分本次变化来自 demand / task / decision / 实验 / 组件健康；归因不出来即视为生成器缺陷，不得当作"无变化" | 2026-09-26 | CT-12 实测：只改 `TASKS.yaml` 时归因精确为 `["tasks/TASKS.yaml"]`、kind 为 `task`，D-003 活动任务由 5 变 4、下一步自动换成 CT-05。DEC-22 要求两类事件都过，本条覆盖 Task 与 Decision 两类 | 若输入集合扩张到无法穷举哈希（例如新增未登记的真源），必须先扩登记再谈归因 |
| DEC-28 | Obsidian 侧的"三项目合并"定义为**视图合并**：目录一律不搬，项目簇成员集合 = `project.yaml.components`，与父 vault 还能索引到多少别的目录无关（`AI平台 / 逆天主题 / 通用规范` 是嵌套 vault，原地保留）。搜索与图谱若要限定项目簇，按注册路径过滤 | 2026-09-26 | 高智力模型裁定（第 6 轮 M1/M3）。三个项目本就在同一 vault 的平级子目录，用户要的是"看起来一个项目"；搬目录会把脚本、契约、技能库、绝对路径全拖进迁移面，而本机 946 条死链正是这类操作的后遗症 | 只有实测证明父 vault 读不了这三个已注册目录，或嵌套 vault 真的造成链接/索引冲突，才重审目录结构；"还能看到别的项目"不构成搬目录理由 |
| DEC-29 | 顺序固定：**先做 Obsidian 视图合并 → 再 CT-07 切默认入口 → 最后 CT-08 去旧写口**。`cutover.ps1` 职责不扩大：只管"默认去哪"和恢复，不创建/删除结构、不维护索引内容 | 2026-09-26 | 高智力模型裁定（第 6 轮 M5）。判据：执行 `-Apply` 之前用户已能手动打开新首页看到完整三项目视图。若在 Apply 时才需要同步搬目录或重建索引，说明职责混合，应停止 CT-07 而不是扩大 cutover | 同左 |
| DEC-30 | `D:\Work\Home.md` 改为**生成投影**，允许人工直接写入的内容为 **0**。数据源固定：`project.yaml`（身份/路径/类型/owner/入口）+ 组件健康探针（git 是否存在等可机器验证事实）+ Current State（阶段/风险/下一步）；`TASKS.yaml` 不直接驱动 Home，Task 先进 State 再由 State 投影，避免第二套状态计算 | 2026-09-26 | 高智力模型裁定（第 6 轮 M6）。实证：Home.md 漏登记元智能、两行"暂无 git 仓"已过期却无人发现 —— 人工索引页必然静默过期 | 只有 Home.md 被正式降级为纯个人笔记、不再承担系统入口时，才允许人写 |
| DEC-15 | **判定可信度进入 V1 完成条件**：会写 State、触发动作或出正式结论的判定器须有正负样本哨兵、数值范围与合计不变量、至少一种独立交叉验证；`self_test` 失败一律 fail-closed | 2026-09-25 | 规范主循环本就要求"结果可靠吗"；实测已出现"结构完整但语义错误"的产出 | 明确只作草稿、不进 State/Decision/动作的输出可降到非阻断验证 |


## 与既有仓库约定的衔接（不重复讨论）

- 技能 id 命名沿用 `loomy-learned-<kebab>`，**不另起命名体系**（否则新旧检索分裂）
- 技能安装走五步闸门，第 4 步需人工点头
- 撞车检查必须在**落盘之前**，命中同题走增补（标 `relates_to`）不建平行条目
- **引用即校验**：写入文档/配置的路径需 `Test-Path`，数值常量需跨真源比对并标 `source`
- 规则真源是 `AGENTS.md`，宿主侧一律用指针文件，不复制正文
########## 文件结束：DECISIONS.md ##########

==========================================================================

########## 文件开始：ISSUES.md ##########
# ISSUES

需要高智力模型判断的问题。请按编号回答。Q1 是其余问题的前置。

---

## 裁定状态（2026-09-25 本轮已全部裁定）

| # | 裁定要点 | 入库位置 |
|---|---|---|
| Q1 | 单一 **Current State 语义对象** + 多原生真源 + 单向生成；Markdown 与 Obsidian 都只是投影 | `VERDICTS.md` §Q1 |
| Q2 | Demand **归元智能层持有**，上下游只持 `demand_id` 指针 | **DEC-13** |
| Q3 | Human View 只治理"用户入口"，不合并、不改写两个技术看板；看板降为 L3 证据层 | `VERDICTS.md` §Q3 |
| Q4 | 自动产物先进隔离候选区；任何改变运行时能力/正式技能库的晋升必须人审 | **DEC-14** |
| Q5 | 判定可信度**进 V1 完成条件**，`self_test` 失败 fail-closed | **DEC-15** |
| Q6 | dry-run 清单 + 回滚清单，人工批准**该固定批次**后执行；清单与实际不符即停 | `VERDICTS.md` §Q6 |
| Q7 | 固化为阻断式 pre-push 闸门，但须先修掉"整行白名单跳过"盲区；顺序固定：改完 → 重生成副本 → 全仓扫 → 检查扫描器自身 → 全绿才 push | `VERDICTS.md` §Q7 |
| Q8 | "抓取层不回源"的判定被本轮实测推翻；改为只设闸门：三项回读凭证 + 每条 URL 最多 3 次重试 + 三连败即阻断作答 | **DEC-08 v2 / DEC-11 / DEC-12** |
| D-003 | 已定义（statement / success_state / constraints / recheck_on），不与 D-001、D-002 重叠 | `DEMAND.md` D-003 v1 |

**裁定原文逐字存档在 `VERDICTS.md` 第 1 轮**（本表是对它的转述，冲突以存档为准）。**下一轮题目待本地执行后新增。** 以下 Q1~Q8 正文保留作题目原貌；其中 Q8 关于"抓取层不回源"的前提**已被推翻**，只当实验记录读。

---

---

## Q1 · 融合后谁是"当前状态"的唯一真源【核心】　【已裁定】

现状四处重叠：记忆库 `STATE`（≤60 行硬约束）、上游 `01-任务看板`（T/D/M 编号）、下游 `runtime/*.drift.json`（探针产物）、以及新协议要求的 `CURRENT_STATE.md`。

**请选择或另给方案：**
- **A** `CURRENT_STATE.md` 只做"摘要 + 指回真源"，不持有状态（我倾向此项，成本最低）
- **B** 元智能的 `State` 对象升格为唯一真源，其余三处改为它的投影／索引
- **C** 记忆库 `STATE` 保持唯一真源，其余都是视图

判据请一并给出：什么信息有资格进入 State，什么必须留在原处。

## Q2 · Demand 层放不放在元智能？

`Demand`（长期需求）是三套系统里唯一**没有对应实现**的对象 —— 上游按项目组织，下游按契约组织，都不承载"跨年度的需求演进史"。

是把 Demand 作为元智能的独有价值落地（我倾向：这是它最该留的部分），还是认为契约文件已足够承担？

## Q3 · Human View 标准要不要反向治理现有看板？

元智能的 `HUMAN_VIEW_STANDARD` 明确禁止首页出现文件路径、JSON、原始日志、重试细节，并要求"只展示会改变理解或判断的信息"。

而现在两个 `01-任务看板.md` 都是高密度技术台账（13KB / 18.6KB），用户实际要读它才能知道系统到哪。**这是不是应当治理？** 若是，治理边界到哪（合并？分层？重写？）。

## Q4 · 自动产出与人审闸门如何接口？

Loomy 持续自动生成本地系统（本阶段 9 → 10），而体系要求技能安装必须人工点头。当前无既定接口。

- **A** 自动产出先进隔离区，满足条件才晋升到待审队列（我倾向）
- **B** 接受自动入册，但人审只把关"安装/启用"
- **C** 关闭自动生成

## Q5 · 便宜模型的判定可信度，是否列为 V1 完成条件？

规范包 V1 唯一的验证问题是"长期需求能否不依赖用户管文件而被持续理解执行"，**没有问"执行模型能否可靠判定"**。

本阶段两次事故都是判定器坏掉却产出看起来合理的假结论（`Test-Path` 对断链恒真；`@(管道|Measure-Object).Count` 恒为 1）。是否把"执行层判定必须有 self_test 且能自证"写进 V1 完成条件？我认为必须，但这会改变它的落地成本。

## Q6 · 946 条死链的清理授权边界

技术上单一动作即可（`cmd /c rmdir`，严禁 `Remove-Item -Recurse`）。但需要你确认：**先出 dry-run 清单再执行**，还是授权一次跑完？另，`lark-*` 公有云 17 个已被人删过，清理时不应顺手重建回去 —— 这个判断是否成立？

## Q7 · 敏感事实的长期闸门

本体系的事实描述必然含第三方服务域名、账号卡标识、企业私有化域名。现有密钥扫描器 7 条规则**一条都不命中**这些。

本轮已按类型掩码（映射表仅存本地）。是否将其固化为 push 前强制闸门（扩规则 + 阻断式），并规定哪些目录允许含原值？

> 补充实测（2026-09-25，可直接作为你判断的输入）：现行规则集 23 类，对全仓 97 个文本文件复扫 —— **掩码目标 21 类 0 命中**；余 2 类是有意保留项（本机用户名 225 次**全在路径内**、散文 0 次；"私有化"类别词若干行）。另有三个已记录的盲区：白名单按整行跳过（跳过行数由扫描器每次打印，不写死）、规则集是枚举式（7→23 类的过程本身就是全漏的证明）、`real_name` 只匹配一个字面量。详见 `REDACTION.md`。
> 同时发现扫描器**把"命中行数"标成"处"**（220 行 vs 225 次），已修。若这套东西要做阻断式闸门，计数单位的歧义本身就是缺陷 —— 这一条请一并纳入你的判断。

## Q8 · 送审介质与"读取确认"该定成什么（2026-09-25，已结案到只剩选择）

**实测结论（七条探针，每条都先在本地验过 200 才发给你）**：你的网页抓取层**不会回源**。一条 URL 你读得到，唯一原因是它已在缓存里；读不到与哪个仓、什么内容、文件多大（139 B 到 200 KB 都试过）、路径是否中文、用 `main` 还是 commit SHA **全部无关**。定案那两条是新 URL 放在**你声称读得到的那个仓**里（一条 20 KB 深路径 Flutter 源码、一条 5.3 KB 中文路径 md），结果同样读不到。完整账目见 `CURRENT_STATE.md` §六。

**三条方案已被证伪，不必再讨论**：换 SHA 固定链接、另建公开导出仓、给本仓开 GitHub Pages。"等 GitHub 搜索索引收录"也无效——本仓当日已在索引内，照样抓不到。

**为什么这仍是 V1 的缺口**：规范包 V1 的验收只问"长期需求能否不依赖用户管文件而持续运行"，没有一条问"对方能否读到事实层"。而分工协议（本地执行 / GitHub 同步事实 / 高智力判断 / Obsidian 给人看）成立的前提正是这条；前提第一次被证伪，是靠人手工做对照实验发现的，没有任何检查会报。

**请判定三件事：**

1. **送审介质选哪个。** **A**｜每轮把合并副本作为**对话附件**上传（现在就能用；仓库退化为"人读的真源 + 附件的来源"）／**B**｜接 GitHub connector（走授权管道，可能可读任意新文件，需你账号侧动作）／**C**｜A 立即用、B 通了以后转 B。我倾向 **C**。
2. **"回读确认"的最小判据。** 目前把 `git push 成功` 当成"事实已同步"，这是错的。请给一个**便宜到每轮都跑得下去**的确认形式——例如要求你在回答开头原样回出指定文件的首行或本轮 nonce。注意判据必须是你**伪造不出来的**：本体系此前确实发生过"没读到材料却给出结构完整的作答"。
3. **仓库在协议里的角色要不要改写。** 若抓取层注定读不到新 URL，那"GitHub 负责同步事实"这句话需要限定：它同步的是**人与附件**的事实源，不是模型的抓取入口。这条要不要写进 `DECISIONS.md`？


---

**状态**：Q1~Q8 已于 2026-09-25 全部裁定，结论见顶部摘要表与 `DECISIONS.md` DEC-11 ~ DEC-15。

---

## 第 2 轮题目（2026-09-25 深夜新增）· 三套系统融合成**一个项目**的物理形态

基线是 `LOCAL_DRAFT_ENTRY_LAYOUT.md` v0.1（本地自拟，待反驳）。第 1 轮已定概念归属（Demand 归元智能层、State 是语义对象、视图不配平真源），本轮**只问目录/仓库/文件放哪、谁写谁生成、怎么迁移**。用户诉求一句话：变成一个项目，且他不想手工管文件。

**N1** 入口与执行层的物理边界：本地方案是"新建一个 `D:\Work\元智能\` 入口库（需求/状态/证据三块），三套引擎原地不动"。请判：**这个切法对不对**？还是应该真的把三套系统并成一个仓/一个库？给判据，别给折中话。

**N2 指针的活性**：证据层用 junction/symlink 指向原仓库。本机刚发生过 946 条死链事故（批量分发、无人回收）。请定：**谁负责周期性校验指针活性、多久一次、断了怎么报、报给谁**；以及如果 Obsidian 的搜索/反链/图谱不跟随链接，证据层该换成什么形态（生成索引页？复制快照？还是接受不可搜）。

**N3 Demand 台账的真源位置**：给人看的地方在 Obsidian，给 AI 写的地方在公开事实层仓 —— 两边都放就是双写。请判：**真源放哪一侧**，另一侧是投影还是指针；以及 DEC-13"元智能层持有 Demand"在物理上落成哪个路径才算成立。

**N4 生成器契约**：输入清单（Demand 台账 + 两个任务看板 + `runtime/*.drift.json` + DECISIONS/VERDICTS）→ 输出（`CURRENT_STATE.md` + 首页）。请给：**字段清单、确定性要求、生成失败时首页必须显示什么**（不允许把过期内容当现状展示）、以及"视图与真源冲突"的判定规则写成可执行判据。

**N5 触发的责任**：重生成靠手动、定时、还是 Agent 每次收尾必须跑？请给一个**不需要用户记得去点**的方案，并说明它失败时怎么被发现（这是第 1 轮"push 成功≠已送达"的同类病：跑成功 ≠ 页面新）。

**N6 迁移顺序与红线**：第一步的最小可逆动作是什么？哪些**绝对不许动**（老路径、各子库 git 结构、技能库物理位置）？存量任务要不要回填 `demand_id`，不回填的后果是什么？出现哪些信号应当直接回滚。

**必答顺序**：N1 → N3 → N4 → N5 → N2 → N6。每条给判据 + 作废条件；信息不足直说缺什么文件。

---

## 第 3 轮题目（2026-09-26 新增）· 阶段 6~7 收口：切入口与去旧写口

背景（本对话已定，不必重述）：控制平面为语义真源、事实层与 Obsidian 为投影（DEC-16）；Task 归属允许 `scope: local`（DEC-20）；控制平面自持 `TASKS.yaml`（DEC-21）；D-004 已立项；CT-06 已实测 Demand 升版传播成立并补了追加式重判队列；CT-11 把送达判据做成可执行校验并给契约加了 `delivery_readback` / `fetch_retry_budget` 两条闸门（现标 `verified_at: null`）。

现在要切默认入口（CT-07）并把事实层四份文件改成生成物（CT-08）。请判：

**P1 切入口的条件**：当前 CT-05（旁路稳定运行）没做、workflow T15/T16 两条机制外扩仍 unresolved。切默认入口需要同时满足哪些条件？切换动作本身怎么做才不断 Obsidian 现有链接（`Home.md` 与子库一览都指着老路径）？给"切了以后发现不对，怎么一步退回"的具体做法。

**P2 去旧写口的顺序**：`DEMAND.md`、`DECISIONS.md`、`ISSUES.md`、`CHANGELOG.md` 改成生成物，哪一份先改风险最低、哪一份必须最后改？生成频率怎么定（每次 push 前？每次收尾？）？特别注意：外部模型只能读投影，如果投影变成生成的，**送审材料的生成时机**会不会晚于本轮改动，导致我发给你的还是旧内容？这条怎么防。

**P3 迁移期双写**：同一份内容在 `machine\` 与事实层两处并存，直到 CT-08 完成。请给**可机器检查的判据**（不是"注意不要重复编辑"这种话），比如：什么校验、多久跑一次、发现两处内容不一致时以哪侧为准、另一侧怎么处置。

**P4 State 的字段缺口**：`active_experiments` 现在恒为 `null`，因为 EXPERIMENTS 没有条目结构可派生。要不要现在把 EXPERIMENTS 纳入生成输入（今天已新增两份）？给判据：什么条件下值得为它建结构化输入，什么条件下就该承认这个字段暂时派生不出来。

**P5 送达闸门的第二层**：本轮你按要求输出三项回读凭证，即视为该闸门首次真实通过。请再判一件事：要不要在第一版就加 manifest（两包文件数、字节数、SHA-256）？你上一轮说"manifest 是辅助完整性证据，不替代双包尾 nonce"——现在给结论：加、不加、还是等出现第一次"凭证齐但内容仍不对"再加。给判据和作废条件。

**必答顺序** P1 → P2 → P3 → P5 → P4。每条给判据 + 作废条件。回答第一行按《作答要求》第 0 条输出三项回读凭证。

---

## 第 4 轮题目（2026-09-26 新增）· Obsidian 里把三个项目合并成一个，怎么做

用户诉求原话：**"我想先把 Obsidian 中的三个项目合并在一起看，怎么做。"** 他要的是**看起来是一个项目**，不是把代码仓合并（DEC-16 已定不并仓、不搬执行层）。

### 本轮实测到的现状事实（不是推测，来自本地检查）

1. `D:\Work` **本身就是一个 Obsidian vault**（存在 `D:\Work\.obsidian`，含 `app.json / graph.json / workspace.json / core-plugins.json`）。三个项目是它下面的**平级子库目录**：`AI自成长引擎\`、`自适应工作流引擎\`、`元智能\`。所以"合并"不是跨 vault 合并，而是**同 vault 内的视图与入口合并**。
2. 但 `D:\Work` 里还**嵌套着别的 vault**：`AI平台\.obsidian`、`逆天主题\.obsidian`、`通用规范\.obsidian`；另有完全独立的 `D:\记忆\.obsidian`。
3. `D:\Work\Home.md` 有「子库一览」表格，登记了 `AI自成长引擎` 与 `自适应工作流引擎` 两行，**没有 `元智能` 这一行**——控制平面建了 git 与生成链，但 Obsidian 侧根本没登记。
4. 同一张表里两行描述写着"构想中，暂无 git 仓"，**已过期**：两个引擎现在都有 git，控制平面也有。
5. 控制平面首页是生成物 `D:\Work\元智能\generated\human\00_首页.md`，按 DEC-22 切换时 `Home.md` 要原地改成兼容入口。

### 请回答（必答顺序 M1 → M2 → M4 → M3 → M5 → M6）

**M1 合并形态选哪种。** 三条候选，请只选一条并给判据：
- **A** 物理聚合：把三个子库挪进一个父目录（如 `D:\Work\元智能\` 下），Obsidian 里天然是一个项目。
- **B** 保持平级 + 统一入口：目录不动，用一页"元智能项目簇"索引页 + 标签/图谱分组把它们在**视图上**合成一个。
- **C** 混合：入口与索引按 B 做，只把控制平面自己新建的目录归拢。
必须直说 A 的断链代价（本机有 946 条死链的前科）以及 B 的代价（用户仍然要在文件树里看到三个平级目录，"一个项目"只存在于入口页）。

**M2 入口怎么收。** 现在三个项目各有 README/Home，`Home.md` 是总入口。合并后**默认打开哪一页**、其余入口页保留还是降级为跳转页？给出"用户双击任何一处都不会撞到两套说法"的具体做法。

**M4 双链与附件。** 若选 A 或任何涉及移动的方案：Obsidian 的 `[[wikilink]]`、嵌入图片与附件、`graph.json`、以及外部（脚本/契约/技能库）写死的绝对路径，哪些会断？断链检查用什么判据（不能只 `Test-Path`，DEC-06）、多久跑一次、断了怎么回收。若选 B，请说明索引页怎么保证不变成第二份需要人维护的真相。

**M3 嵌套 vault 的影响。** `D:\Work` 里嵌套着 `AI平台`/`逆天主题`/`通用规范` 三个子 vault，`D:\记忆` 是独立 vault。请判：父 vault 会不会索引/搜索到嵌套 vault 的内容？合并方案要不要为此改结构？如果这条会影响 M1 的选择，直接说怎么选。

**M5 与 CT-07 / CT-08 的先后。** Obsidian 合并放在切入口之前还是之后？如果先做合并，`cutover.ps1 -Apply/-Rollback` 要管的东西会不会变多（现在它只管入口指向）？给一个明确顺序，不要"视情况而定"。

**M6 登记与过期。** `Home.md` 缺 `元智能` 行、且两行"暂无 git 仓"已过期。这类**人工维护的索引页过期**问题，按 DEC-24 的思路该怎么治：`Home.md` 是否应该成为生成投影？如果是，它的数据源是什么（`project.yaml`？`TASKS.yaml`？），哪些内容允许人写。
########## 文件结束：ISSUES.md ##########

==========================================================================

########## 文件开始：LOCAL_DRAFT_FUSION_PLAN.md ##########
# 融合方案 · 本地执行模型自拟草案 v0.1

> **性质**：本文件由**本地执行模型**自拟，**未经高智力模型评审**，不是决议。
> `ISSUES.md` 的原题保持不动，本草案不作为答案，只作为可反驳的基线。
> 若高智力模型的结论与此冲突，以高智力模型为准，本文件降级为 EXPERIMENTS 里的一条被否记录。

## 评审结论（2026-09-25 已评审，本文件按约定降级为基线记录）

**认同**："概念取元智能、机制复用下游、经验治理复用上游"这个映射方向成立。

**三处被驳回，本地采纳并已入库**：

1. **Demand 不能放在上游。** 草案写"在 `AI自成长引擎` 新增 `demands/<id>.yaml`"被否 —— 那会让子系统成为整体语义的所有者。改为元智能层持有 Demand 真源，上下游只持 `demand_id` 指针（**DEC-13**）。
2. **`CURRENT_STATE.md` 不等于状态真源，也不只是"摘要 + 指回真源"。** 唯一的是 **Current State 语义对象**，有明确字段、生成规则与失败规则；Markdown 与 Obsidian 都是它的投影。投影与原生真源冲突时判投影失效并重生，不许反向改真源配平视图。
3. **"只有第 1 步需要写代码"过于乐观。** 达到 V1 还需：生成式 Human View、State 校验、Demand 版本传播、实验生命周期、候选技能闸门、脱敏阻断闸门。草案的观察窗"至少覆盖一次真实需求变更"**必要但不充分** —— 最低验收须实际走完一次：Demand 升版 → 受影响 Task/Experiment 被重新判断 → State 重生成 → Human View 更新。

配套新裁定：自动产物先进隔离候选区、晋升须人审（**DEC-14**）；判定可信度进 V1 完成条件、`self_test` 失败 fail-closed（**DEC-15**）。

## 0. 我为什么先写这份

`REPORT_TO_STRATEGIST` 第 7 条建议"冻结结构性改动等 Q1"。冻结不等于空等 —— 先把可反驳的基线摆出来，能让评审方直接指出错在哪，而不是从零构造。

**硬约束**（沿用 `DECISIONS.md`，不重述理由）：不新增第五份状态真源；新增复杂度须自证降低长期维护成本；不可逆动作需人工点头；不引入常驻服务。

## 1. 三套系统其实是一个系统的三层，只是各自长出来的

| 层 | 承担者 | 已验证程度 |
|---|---|---|
| **概念层**：Demand 为中心、Human View 标准、六道 Gate、变更分级 L1–L4 | 元智能 V1 | **零实现**，纯规范 |
| **执行层**：契约=声明态真源、只读探针、drift 投影、self_test、退出码熔断 | 自适应工作流引擎 | **经过真实事故验证**（A1 收口） |
| **沉淀层**：L1/L2/L3 分层、撞车检查、引用即校验、快照、周报 | AI自成长引擎 | 在跑，但产出稀疏（技能库至今 1 个） |

**关键判断：融合的技术底座应该是下游那套"契约 + 探针 + self_test"，不是元智能的 schema。**

理由有实证支撑：元智能的 `state.schema.json` 只有 4 个必填字段、类型几乎全是 `string`/`string[]`，没有 ID 规则、没有枚举、没有单调性约束；而它的 Phase 1 却要求"所有对象有稳定 ID"。规范层与机器层强度不匹配。反观下游契约有 8 项 conditions、11 条 gates、baseline 容差 +50%、self_heal 分级，且 `probe_self_test` 在真实运行中**两次拦住了假结论**（断言求值器初值 bug、`Test-Path` 对断链恒真）。

所以：**概念取元智能，机制取下游，纪律取上游。** 不是三选一。

## 2. 对象映射与归属（融合的核心）

| 元智能对象 | 上游对应 | 下游对应 | 融合后归属 | 是否新建 |
|---|---|---|---|---|
| **Demand** | 无 | 无 | **上游新增** `demands/<id>.yaml` | ✅ 唯一需要新建的对象 |
| State | 记忆库 `STATE` + `01-任务看板` | `runtime/*.runtime.json` | **不新建**：State = 由真源**投影生成** | ❌ |
| Task | 看板 T / D / M 序列 | A1–A6 里程碑 | 保留看板，Demand 只存**指针** | ❌ |
| Experiment | 技能库四态 + dry-run | 契约 `baseline.tolerance` + 回归计时 | **采用下游形态**（有指标、容差、self_test） | ❌ |
| Decision | 看板 §五 | 契约注释 | 真源仍在各仓看板；`DECISIONS.md` 只做**对外入口** | ❌ |
| Archive | `草稿区` → `_快照` 14 份 | `superseded_runners` | 保留上游快照机制 | ❌ |
| Skill / L3 | `技能库` + 五步闸门 | — | 保留上游 | ❌ |
| Escalation | `AGENTS.md` P1/P2/P3 | — | 保留；`REPORT_TO_STRATEGIST` 是它的**出口格式** | ❌ |

**结论：融合只需要新建一样东西 —— Demand。其余全部是映射，不是搬迁。**

这一条是本草案最重要的主张。如果评审方同意，融合的工作量从"三套系统重写"降为"加一个对象 + 一个投影器"。

## 3. 对 Q1（状态真源）的回答

**选 A：`CURRENT_STATE.md` 只做摘要 + 指回真源，不持有状态。**

具体形态：`CURRENT_STATE.md` 由脚本**单向投影生成**，输入三处真源 —— `demands/*.yaml` + 两仓 `01-任务看板` + 下游 `runtime/*.drift.json`。产物**禁止手改**，与 `runtime/*.json` 同级。

三条支撑：

1. 已经存在四处状态描述，再加第五份必分叉 —— 这不是推测，是本轮技能治理实测的结论（`skill-creator` 76 份 / 25 种内容）。
2. 元智能自己把 State 定义为"**现在的最小真实描述**，不是历史合集"，并禁止"把完整日志复制进 State"。**这个定义天然就是投影，不是存储。**
3. 投影器可以带 self_test：每个投影值必须能在真源里逐条回指 —— 这正是上游铁律「引用即校验」的机器化，本轮体检器已用它两次拦住假结论。

**反对 B（State 升为唯一真源）的理由**：需要迁移记忆库 `STATE`，而它有 ≤60 行 / ≤12KiB 硬约束和 pre-commit hook 拦截，且记忆库是**跨项目**的、State 是**单 Demand** 的，粒度不匹配。迁移成本远高于收益。

## 4. Demand 的最小落地形态

```yaml
id: D-001
statement: 让本机所有 AI Agent 的作业体系长期保持"单一真源可追溯、引用不悬空"
why: 单人运维，判断带宽是唯一稀缺资源；体系分叉不报错，只表现为"另一个工具不守规矩了"
success_state: 任一时刻能回答"X 的真源在哪、谁在引用它、引用是否有效"
constraints:
  - 不可逆操作需逐次人工批准
  - 技能安装必须人审点头
recheck_on: [新增 Agent 宿主, 程序版本更新, 引入新规范包]
status: active
version: 3
changed_at: 2026-09-24
links:            # 只存指针，不复制内容
  upstream_board: D:\Work\AI自成长引擎\docs\01-任务看板.md
  downstream_contract: D:\Work\自适应工作流引擎\contracts\gpt-mirror-extended-review.yaml
  memory_state: D:\记忆\长期记忆.md
  skills_library: D:\Work\AI自成长引擎\技能库\README.md
```

**Demand 只存定义与指针，不存状态。** 这一条同时回答了 Q1 和 Q2。

## 5. 对 Q3（Human View 要不要反向治理看板）的回答

**不合并、不改写看板。** 新增**一个** Obsidian 入口页，内容 100% 来自投影，禁止手写。

理由：受众不同。`01-任务看板` 是给执行者读的技术台账（T/D/M 编号、验收标准、证据行号）；Human View 是给你读的决策面（现在怎样 / 下一步 / 要不要我介入）。把两者合并会同时损害双方 —— 台账失去精度，视图失去可扫描性。

元智能的 `HUMAN_VIEW_STANDARD` 应当**只约束新增的入口页**，不追溯既有看板。

## 6. 对 Q4（自动产出 vs 人审闸门）的回答

选 **A 变体**：不关闭自动生成，加隔离区。

Loomy 自学的 `loomy-learned-*` 先进 `技能库\_inbox_learned\`，投影器每日列出「新增项 + 撞车检查结果 + 与既有技能的相似度」，你点头才晋升 `技能库\`。

这解决的是当前的真实矛盾：本会话内 learned 从 9 涨到 10，而五步闸门要求第 4 步人工点头 —— 两者现在靠运气不冲突，因为**没人晋升过**。一旦开始晋升就会撞上。

## 7. 对 Q5（self_test 是否入验收）的回答

**必须入。** 三条实测依据，全部发生在同一天：

| 事故 | 症状 | 若无 self_test 的后果 |
|---|---|---|
| 断言求值器 `$actual` 初值误写 `$null` | 所有卡恒判不健康 | 两次 `active=0` 被当真实数据写进台账 |
| `Test-Path` 对断开 junction 恒返回 True | 死链数报 0 | "没有死链"成为结论，946 条被永久掩盖 |
| `@(管道｜Measure-Object).Count` 恒为 1 | 分类计数全错 | 报出"ORPHAN 1／UNREACHABLE 1"而总数 946，看着自洽 |

共性：**判定器坏掉时，输出仍然结构完整、读起来合理**。所以规范里必须写明 —— 判定型脚本的 self_test 必须包含三件套：**已知样本哨兵 + 数值区间 + 合计自洽校验**。元智能 V1 目前完全没有这一层。

## 8. 迁移路径（4 步，每步可回滚，只有第 1 步需要写代码）

1. 建 `demands\` + 一个只读投影脚本，产出 `CURRENT_STATE.md`。**不改任何现有文件、不删任何东西。**
2. 跑满一个观察窗（我的判据：**至少覆盖一次真实需求变更**，不是"跑够 7 天"），验证投影与人工判断一致。这是唯一需要走 EXPERIMENT 模板八项的动作。
3. 一致后，才把 Obsidian 入口页切到投影产物。
4. 最后才评估是否把元智能的六道 Gate 并入 `handbook`（作为"新增复杂度准入"条款）。

**第 1 步可回滚**（删两个新文件即恢复原状）；第 3 步不可逆性最低但影响你的日常阅读，故排在验证之后。

## 9. 明确不做

- 不合并三个仓（合并会让"谁是真源"更难判断，而不是更容易）
- 不改记忆库 `STATE` 的语义与硬约束
- 不引入常驻服务（下游 `03-方案构想.md` §八 已明令禁止）
- 不重写任何已验证的探针与解析器（`lib_contract.ps1` 保持单一真源）
- 不追溯治理既有看板

## 10. 我需要高智力模型反驳的三点

1. **"State = 投影，Demand 不持有状态"** 是否成立？还是 Demand 必须持有自己的 State 才能跨会话稳定？
2. **元智能的六道 Gate 与 `handbook` 的安全红线/台账格式**是否冲突？冲突时谁优先？（我倾向 handbook 优先，因为它是跨项目 SSOT 且已被实际引用。）
3. **迁移第 2 步的观察窗判据**（"覆盖一次真实需求变更"而非固定天数）是否可操作？还是应该给一个明确的最短周期？
########## 文件结束：LOCAL_DRAFT_FUSION_PLAN.md ##########

==========================================================================

########## 文件开始：LOCAL_DRAFT_ENTRY_LAYOUT.md ##########
# 融合后的物理形态 · 本地执行模型自拟草案 v0.1（待反驳）

> **性质**：本地执行模型自拟，**未经评审**。`ISSUES.md` 第 2 轮题目（N1~N6）以本文为待反驳基线。
> **本轮范围**：只谈**目录/仓库/文件放在哪**，不谈概念层（概念归属已由第 1 轮定，见 `VERDICTS.md` 与 DEC-13~15）。
> 用户的原始诉求一句话：**把三个项目变成一个项目，并且他不想手工管文件。**

## 1. 现状盘点（实测）

| 层 | 现在的物理位置 | 可写性 |
|---|---|---|
| Obsidian 容器库 | `D:\Work\`，**各子库独立 git** | 人写 |
| 三套系统 | `D:\Work\AI自成长引擎\`、`D:\Work\自适应工作流引擎\`、（元智能规范在事实层仓 `30_元智能/`） | 各自执行真源 |
| 记忆库 | `D:\记忆\`（独立 vault，含 `调度大脑记忆/` 工具记忆） | 人+Agent 写 |
| 事实层（AI 间共享） | `D:\meta-intelligence-facts\`，含 `10_ / 20_ / 30_` 三个子目录 | 本地执行模型写，推公开仓 |
| 状态类产物 | 记忆库 `STATE`（≤60 行）、两个 `01-任务看板.md`、`runtime/*.drift.json`、`CURRENT_STATE.md` | **四处各自可写，互不同步** |

**病灶**：不是"文件夹太多"，是**同一类事实有四个可写处**，且没有任何一处写着"我们到底在追求什么"。

## 2. 我的方案：合"看的那一层"，不合"跑的那一层"

```
D:\Work\元智能\                 ← 唯一入口，新项目，独立 git
  00_首页.md                    ← 机器生成，人只看这一页（Human View）
  需求\                         ← Demand 台账：全系统唯一"人可写"的地方
    D-001_全机AI资产治理.md
    D-002_送审链路可用性.md
    D-003_三系统融合.md
  状态\                         ← 机器生成，只读；人改它即视为污染
    CURRENT_STATE.md
  证据\                         ← 三块，全是指针，不复制正文
    自成长.md    → D:\Work\AI自成长引擎
    工作流.md    → D:\Work\自适应工作流引擎
    规范.md      → D:\meta-intelligence-facts\30_元智能
```

三套引擎**原地不动**（代码、技能、契约、探针、看板一律不搬）。理由：搬进 Obsidian 会让"看的地方"变成"写的地方"，违背既有规则"Obsidian 只解释与索引，不反向充当运行事实源"；且批量改路径正是本机 946 条死链的成因。

## 3. 数据流（谁生成谁）

```
需求\（人写） → 生成器 → 状态\CURRENT_STATE.md → 00_首页.md
                     ↑
   两个看板 + runtime/drift + DECISIONS/VERDICTS（只读输入）
```

- 生成器只读上面这些，**唯一写出** `状态\` 与 `00_首页.md`。
- 首页只放四类：现在在哪 / 下一步 / 风险 / 等你决定。路径、JSON、日志、重试细节默认隐藏。
- 视图与原生事实冲突时，判视图过期并重生成；**禁止反向修改真源去配平视图**。

## 4. 我不确定的三点（就是要问的）

1. **指针 vs 复制**：Obsidian 对 junction / symlink 的实际行为我没测过 —— 全文搜索、反链面板、关系图谱到底跟不跟进去？如果不跟，"证据层"在 Obsidian 里就是死链，那还不如放一份**生成出来的索引页**（列路径与状态，不复制正文）。
2. **Demand 台账放哪**：`D:\Work\元智能\需求\` 是给人看的，但 AI 间共享真源在事实层仓。同一份内容两个位置就又是双写。要么台账真源在事实层、Obsidian 里是投影；要么相反。**我倾向真源在事实层**（Agent 读写都走它，Obsidian 只渲染给人看），但这条与 DEC-13"元智能层持有 Demand"怎么落地我没想清楚。
3. **谁来触发重生成**：手动跑脚本？定时？还是 Agent 每次收尾必须跑？触发不了的话，首页就会长期显示过期状态——那比现在更危险，因为它看起来像真的。

## 5. 我认为的"合上了"判据（请评审可否机器化）

改一次 Demand 的 `version`，十分钟内三件事自动成立：受影响的 Task/Experiment 被重新判断、`状态\CURRENT_STATE.md` 变了、`00_首页.md` 变了。做不到就是叠了层皮，不是融合。
########## 文件结束：LOCAL_DRAFT_ENTRY_LAYOUT.md ##########

==========================================================================

########## 作答要求（务必遵守） ##########
0. 回答的**第一行**必须原样输出本轮三项回读凭证，用竖线分隔：
   FACTS-DELIVERY-81b333fc | CORE-TAIL-383899E4 | SPEC-TAIL-6107381C
   CORE_TAIL 在本包最后一行，SPEC_TAIL 在第二个包的最后一行。三项缺任意一项即判"材料未完整送达"，本轮不要作答，只回这六个字并说明缺哪项。
   这是送达闸门，替代"我 push 了 = 你拿到了"这个错误等式；不许转述、不许凭上一轮记忆。
   每个链接最多试 3 次、间隔 15 秒；3 次仍失败就照抄工具返回的报错原文，不要概括成中文。
1. 按 ISSUES.md 的 Q1~Q8 编号逐条回答；Q1/Q2/Q5/Q6 必须给明确结论，不许"取决于情况"。
2. 每条结论必须给判据：什么信息有资格进 State、什么必须留在原处、冲突时谁优先。
3. 引用事实时必须写明出自上面哪个文件的哪一节。禁止拿仓库外的通用常识当作本项目的既定事实。
4. 若某个问题信息不足，直接说"信息不足"并列出还需要什么文件，不要猜。
5. 若你的结论与 DECISIONS.md 已定事项（DEC-01~DEC-11）冲突，必须明确指出冲突并说明为什么推翻，不要绕开。
6. LOCAL_DRAFT_FUSION_PLAN.md 是待反驳的基线，不是答案。请指出它哪里错了；若认同某条，说明认同依据。
   重点检验它的主张："三套系统是一个系统的三层——概念取元智能、机制取下游、纪律取上游，融合只需新建 Demand 一个对象"。
7. 若两个包只收到一个（缺 ALL_SPEC_DOCS.md 规范层），必须在回答开头写明"仅读到治理层，规范层缺失"，
   并对依赖规范层原文的 Q3/Q4/Q5/Q7 直接判"信息不足"，不要用本包的转述替代规范层原文。
8. 若本轮有新的融合裁定，请顺带检查 DECISIONS.md 新登记事项是否与你上一轮的回答自相矛盾。
9. 输出用中文，结构化，能表格化就不写长段落。你的结论需要可执行：给判据、给优先级、给"什么情况下这个结论作废"。


【本包尾凭证】CORE-TAIL-383899E4
