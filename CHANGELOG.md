# CHANGELOG

## 2026-09-25（深夜之三）· 融合阶段 1 落地：元智能控制平面建立

### 做了什么

用户批准"最小可逆动作"后，新建 `D:\Work\元智能\`（独立 git，本地提交 `dd493a8`，**无远端**）：

- `project.yaml` —— 三套系统的组件注册表（路径 / 类型 / SSOT 入口 / 判活 sentinel / owner），结构照 `VERDICTS.md` 第 3 轮 (b)。
- `runners/check_components.mjs` —— 唯一脚本：读注册表 → 逐组件判活 → 写 `runtime/source-health.json` → **回读校验** → 输出 `input_fingerprint`。
- `machine\demands\`、`generated-staging\` 建空目录，阶段 2 才用。

**旧文件一个都没搬、没改名、没删；用户入口没切。** 回滚 = 删掉这个目录。

### 结果

- 三个组件判活全部 `ALIVE`，`input_fingerprint 5319af36a90f…`。
- **先验证了它能判死**：把一条 sentinel 结构串改坏 → 正确输出 `DEAD growth structure_mismatch` 且退出码 1；还原后指纹回到原值 —— 同一输入必得同一指纹，确定性成立。
- 判活落地前逐个核过它给的 sentinel 字符串在真文件里确实存在（6 条全命中），没有照抄一个跑不通的注册表。

### 刻意没做（DEC-18 的可后补清单）

`state_dirty` 事件总线、10 分钟 watchdog、独立 `schemas\` 与 `adapters\` 目录、完整 `input_hash` 规则。过渡期靠"执行 Agent 每次收尾必须生成一次并回读产物"兜。


## 2026-09-25（深夜之二）· 融合物理形态裁定入库；本仓角色降为投影

### 做了什么

1. **由本地执行模型直接操作镜像站完成两轮对话**（opencli 会话 `gptreview`，同一对话窗口、自己新开的标签页，发问前逐次核对档位 = Extended）。第 2 轮问 N1~N6，第 3 轮问 a~d。裁定原文逐字存入 `VERDICTS.md`。
2. **新增 DEC-16 / DEC-17 / DEC-18**：控制平面为跨系统语义真源、本仓与 Obsidian 均为生成投影、证据层禁 junction；生成事务边界与"本地状态／外部送审状态"分列；第一版范围与可后补清单。
3. **本仓角色改写**：README 顶部按 DEC-16 明确"这里是掩码后的共享投影，不是跨系统语义真源"，原件迁移完成前本仓仍是实际写入处，但每次改动视作"投影刷新"而非"写真源"。
4. **修了一个共享脚本的假退化判据**（见下条），差点误报实例故障。

### 一次差点误判的读取事故

提取脚本连续两次报 `LOW_REPLY`（19 字、0 字），按 SOP 我该上报"镜像实例退化"。真实原因：**长对话里离屏的 assistant 节点被 React 虚拟化清空 `innerText`** —— 页面滚到底之后同一节点立刻读出 1433 字带表格的完整回答。已把"探测与提取前先滚到底"落进 `ai-hub-memory/scripts/gpt_wait_extract.mjs`（ab446fe），并在 `LOW_REPLY` 提示里加了"先自证读取侧"。

**教训**：判对方故障之前先证明我读对了 —— 这条用户 2026-08-29 就纠正过一次，今天以脚本修复的形式第二次落地。

## 2026-09-25（深夜）· 外部裁定全部入库 + 送达闸门换成三项凭证

### 发生了什么

高智力模型读完两个材料包（首行交付号 `FACTS-DELIVERY-c8613790` 输出正确，两个包均 HTTP 200），对 `ISSUES.md` Q1~Q8 与 `DEMAND.md` D-003 给出了完整裁定。同日白天那一整套"抓取层不回源"的结论**被它自己的实测推翻**：夜里同一批 URL 加上老而冷门的第三方仓、从没被请求过的本仓哨兵文件，**全部首次即 200**。

### 做了什么

1. **裁定入库**：Q2 → DEC-13（Demand 归元智能层持有）；Q4 → DEC-14（自动产物先进隔离候选区，晋升须人审）；Q5 → DEC-15（判定可信度进 V1 完成条件，`self_test` 失败 fail-closed）；Q8 → DEC-08 升 v2 + DEC-11 + DEC-12。Q1/Q3/Q6/Q7 的结论落在 `ISSUES.md` 顶部摘要表与本地草案的驳回标注里。
2. **D-003 定义完成**，`DEMAND.md` 由 draft v0 升为 active v1（statement / success_state / constraints / recheck_on 四段齐），并显式声明与 D-001、D-002 不重叠。
3. **本地草案降级**：`LOCAL_DRAFT_FUSION_PLAN.md` 顶部新增"评审结论"，登记三处被驳回点（Demand 归属、State 不等于 Markdown、"只有第 1 步要写代码"过于乐观 + 观察窗不充分）。
4. **送达闸门实现**：`build_core_bundle.js` 每轮生成 `FACTS-DELIVERY-*`（基线提交号）+ `CORE-TAIL-*` + 随机 `SPEC-TAIL-*`，尾部凭证写进两个包的**末行**，随机值只存本地 `.delivery_nonces.json`（不入库）。`build_spec_bundle.js` 从该文件取 SPEC 尾凭证。作答要求第 0 条与 `PROMPT` 硬约束同步改为"三项缺一即判未送达"。
5. **裁定原文补存档**：新增 `VERDICTS.md`，按轮次逐字保存高智力模型原文。此前只入库了转述结论，没有原始出处 —— 摘要一旦与原文冲突就无从复核。头部记录送达凭证与题目状态，末尾记录本地调整点（补了一条它未覆盖的 CDN 滞后判据）。
6. **可达性一节全部重写**（`CURRENT_STATE` §六、`README`、`REPORT` §0/§3/§5、`ISSUES` Q8）：**只记现象与探针账目，不再记机制**；承载维持 GitHub raw URL 不变，导出仓、Pages、connector 一个都没建。

### 结果与教训

用户明确裁定过：不接受手动管文件，只要"传 GitHub + GPT 去读"。这条现在成立 —— 仓库路线本来就是通的，代价只是一道重试与回读闸门。

**教训（这条比机制值钱）**：我在同一条因果上连错五次，每次都写了"结案"，每次都因为下一轮才补上对照组而作废。前五次的共同缺陷是**把一次观察当机制**。定稿版因此刻意不解释为什么，只规定失败时怎么办。写进对方报告的推测必须带"未验证"标记，否则对方会照它执行无效动作 —— 本轮它建议的"另建导出仓"就是这么来的。

## 2026-09-25 · 材料入口：先做修复，再用七条探针结案（抓取层不回源）+ 扫描器计数单位纠错

### 做了什么

1. 新增两个**根级 ASCII 合并副本**：`ALL_CORE_DOCS.md`（治理层 7 文件，约 40 KB）、`ALL_SPEC_DOCS.md`（规范层 55 文件，约 200 KB），由脚本从本仓**已掩码**文件拼接生成，不直接读本地真源。
2. `README.md` 增「抓取失败的备用入口」；`PROMPT_FOR_GPT.md` 第一步改为三途径（两个 raw URL → 目录树 → 只回复"无法访问仓库"），并加硬约束：只读到治理层时必须声明规范层缺失，依赖规范层的题（Q3/Q4/Q5/Q7）直接判"信息不足"。
3. 扫描器 `scan_facts.js` 修**计数单位**：原先一行内多次命中只记一条却标成"处"，现分别输出"命中行"与"出现次数"，并打印白名单跳过行数（不再静默）。
4. `REDACTION.md` 更正过期表述（原写"15 类规则复扫 0 命中"，实际规则已扩到 23 类且有 2 类有意保留项命中），新增「合并副本生成纪律」「已知盲区」两节，记录 force-push 不能清除已公开游离提交的教训。
5. `REPORT_TO_STRATEGIST.md` 增 §0（材料怎么读）、风险第 6 条（协作链路可读性未列入验收），并更正 §8 的核心问题口径。
6. **（结案后改写）** 上面第 1、2 条建立的"URL 入口"路线全部作废：`README.md` 与 `PROMPT_FOR_GPT.md` 改为**附件优先**，明写"不要再抓本仓任何 URL"；副本保留，用途改成附件载体；`ISSUES.md` Q8 从"要不要列为验收项"改写为"送审介质选型 + 回读确认最小判据 + 仓库角色要不要改写"三项。
7. 新增 `PROBE_READABILITY.txt`（139 B 零项目内容）作为**长期连通性哨兵**，以后要复测同一件事直接用它，不必再造材料包。
8. **回读确认落地**：`ALL_CORE_DOCS.md` 第一行加了本轮交付号 `FACTS-DELIVERY-<基线提交前 8 位>`，作答要求第 0 条与 `PROMPT` 硬约束都要求对方**第一行原样输出该号**，不一致即判定本轮没读到材料、回答作废。交付号每轮随生成自动变化，对方无法凭上一轮的记忆伪造。

### 为什么

高智力模型上一轮报 `Cache miss`、读不到本仓，却读得到旧仓 `listenloop-project`。整套协作协议的前提是"对方能读到事实层"，这个前提今晚第一次被证伪 —— 而它并不在元智能 V1 的验收项里。

### 结果

- 匿名实测 **11 种 URL 形式全部 HTTP 200**（raw 三连测 / `github/raw` 跳转 / blob 网页 / 仓库首页 / `contents` API / `git/trees?recursive=1` 返回 97 blob 且 `truncated=false` / repo 元数据）→ **仓库侧无故障**。当时推测成因在对方抓取通道缓存优先、本仓刚删仓重建、且目录名含中文 —— **此推测当日稍后被探针 B 否掉一半，见下一节。**
- 掩码目标 **21 类 0 命中**；有意保留 2 类经定量核对（本机用户名 225 次**全在路径内**，散文中 0 次；"私有化"类别词若干行）。两个独立计数实现交叉一致，均为 225。
- GitHub `size` 元数据当时仍滞后显示 0（错误的 `empty=true` 已消失）。已在 README 明示：**判空请看 tree 接口，不要看 `size`**。当日稍后 `size` 自愈为 343。

### 同日追加：对方侧探针（把我自己的成因写错了，就地更正）

| # | 对象 | 结果 |
|---|---|---|
| A | 本仓 `ALL_SPEC_DOCS.md`（`main`，约 200 KB） | 读不到 `Cache miss` |
| B | **另一仓** `listenloop-project/OPEN-QUESTIONS.md`（9.5 KB） | **读到**，首行逐字一致 |
| P1/P2 | 本仓两份副本，**commit SHA 固定**（43 KB / 200 KB） | 都读不到 |
| D1 | **另一仓** `code/lib/widgets/anki/anki_review_dialog.dart`（20 KB，深 ASCII 路径，任何评审都不会读它） | **读不到** |
| D2 | **另一仓** `docs/00_项目驾驶舱_可视化中心/03_WBS任务分解全景图.md`（5.3 KB，中文路径） | **读不到** |
| 哨兵 | 本仓 `PROBE_READABILITY.txt`（139 B，零项目内容） | 未测（D 轮出来后已无必要），文件留在仓里当长期标尺 |

**结案结论：对方抓取层不回源，能否读到只取决于"这条 URL 它是否见过"。** 定案的是 D 轮 —— 失败发生在它自己声称读得到的那个仓里，两条从未读过的新路径照样读不到。所以 B 那次"成功"是**缓存命中**，不是回源能力。变量已被穷举：仓、内容、体积（139 B～200 KB）、路径语言、`main` vs SHA，全都不决定结果。

**据此作废的方案**：commit SHA 固定链接、另建公开导出仓（对方建议的）、给本仓开 GitHub Pages、等 GitHub 搜索索引收录（本仓当日已在索引内，实测照样抓不到）。两个副本保留，但用途从"抓取入口"改成"附件载体"。

**本轮我写错并就地更正的因果链（三次）**：① "缓存优先所以未缓存即失败" —— 被 B 否掉；② "故障按仓绑定" —— 被 P1/P2 动摇、被 D1/D2 彻底否掉；③ 中间还怀疑"响应体大小上限" —— 被 43 KB 与 139 B 否掉。三次都因为**没先做完备对照就把推测写进对外报告**，而报告是对方的决策依据：它据此建议了"另建导出仓"这个无效动作。


### 过程中的自我纠错

| 错误 | 后果 | 拦下方式 |
|---|---|---|
| 扫描器把"命中行数"标成"处" | 对外报数偏小（220 vs 225）；若当阻断式闸门会给出错误数量 | 两个单位分别输出；另写第二个独立计数脚本交叉验证 |
| `REDACTION.md` 写"15 类 0 命中"，而规则已扩到 23 类 | 过期结论，与实测有命中直接矛盾 | 每次写完重扫；报数必须注明是哪几类、什么单位 |
| `REPORT` §8 说核心是 Q1/Q2/Q3，`PROMPT` 说必答 Q1/Q2/Q5/Q6 | 同一批材料内部自相矛盾，对方无从判断优先级 | 统一为 Q1/Q2/Q5/Q6，并在 REPORT 内留更正记录 |
| 首轮验证时把 `http=000` 当成 GitHub 故障 | 会误判成"仓库还是读不到" | 实为本机直连外网被掐；改走代理复测后 11/11 全 200 |
| **把未验证的因果写进对外报告**（先后三条：缓存优先 / size 滞后 / 按仓挡） | 报告是对方决策依据，错因果会让它去执行无效动作 —— 它确实据此建议了"另建公开导出仓" | 结案：因果必须有**可区分的对照实验**支撑；本轮 D 轮才是合格对照组（同一仓内两条从未读过的新路径）。三次因果已全部撤掉 |
| **拿"另一个仓的一个文件"当对照组**（B 探针） | 该文件所在仓正是它此前送审过的对象，URL 很可能已在缓存里 → 我据此错误否掉了第一条正确假设，多绕两轮 | 对照组必须证明"从未被读过"：改用深路径源码文件与中文路径 md（D1/D2），并优先选任何评审都不会读的对象 |
| 探针发给对方前只测"URL 通不通"，没测"这条 URL 是否已被它见过" | "读不到"和"读得到"两种结果都无法归因，四轮才收敛 | 以后同类实验先问：这条 URL 它见过没有？没有的话，成功与失败各说明什么 |

### 未做（有意）

- 未改任何问题的实质内容（Q1~Q7 / D-003 原文未动）。
- 未替高智力模型作答，未把任何结论预先写进 `DECISIONS.md`。
- 未清理 946 条死链（不可逆，仍等批准）。
- 未接 GitHub connector（那是对方侧动作，只能由用户决定）。

## 2026-09-24 · 技能治理 P0/P0.5 + 事实层建仓

### 做了什么

1. **三件只读工具**落地于 `10_AI自成长引擎/技能库/_中枢/scripts/`：全机普查器、范围投影器、死链枚举器。配置解析器抽为单一真源 `lib_contract.ps1`。
2. **全机入册**：8663 份 SKILL.md（含 Loomy 81），逐条给出身份键、A/B/C 诊断分类、范围判定与证据路径。
3. **按新政策重划治理面**：范围层 832 份，其余 7831 份登记不治理。
4. **死链定性**：946 条全部为"源已删"，根因是 hub 删除 149 个技能目录、9 个宿主链接未回收。
5. **方案文档**：新增 `docs/04-方案_技能统一治理与浏览器能力域.md`（§一~§十四），含两次实测更正记录。
6. **本事实层建仓**：三套系统核心文档 84 份 + 8 个事实层文件。

### 为什么

用户主观感受是"整个电脑很混乱、重复、垃圾"，但无人能给出不靠感觉的规模判断。目标是把它换成可复现事实，并在动结构之前先确定"到底有多少值得动"。

### 结果

- 治理面比直觉**小两个数量级**：真正该管的 832 份（9.6%），可治理分叉 31 簇，其中 8 簇同源（一次工具升级可收口）。
- 用户自产资产仅 **1 个**技能（真源目录），而程序侧在持续自动长出（本阶段 9 → 10）。**瓶颈不是清理存量，是产出根本没入册。**
- 死链是单一事故，非 946 项手工活；清理是 1 个脚本动作，属不可逆，待批准。
- 三套系统并存、零融合代码，且存在**四处状态真源重叠**风险 —— 已提交为 `ISSUES.md` 的 Q1。

### 过程中的自我纠错（记录以防重犯）

| 错误 | 后果 | 拦下方式 |
|---|---|---|
| 用目录名当技能身份键 | 虚增假簇、漏报改名副本 | 改用 frontmatter `name` 并加缺失率上限自检 |
| 用 `Test-Path` 判 junction 活性 | 死链数会被报成 0 | 改活性探针；哨兵强制检出已知断链 |
| `@(管道 \| Measure-Object).Count` | 三个分类计数恒为 1 | 改 `@(管道).Count` 并加"分类合计=总数"校验 |
| 产物文件名未加前缀 | 一次冒烟覆盖掉全量三份报表 | 产物名一律带 `$Prefix`，并用校验和复验隔离 |
| 粗估断链 351 条（只抽查 5 宿主外推） | 低估 2.7 倍 | 全查 18 宿主 + 双实现交叉验证 |

### 未做（有意）

- 未删除、移动、修改任何被检对象；未建 registry；未清死链；未改任何人的既有仓库；未提交 git。
