# CHANGELOG

## 2026-09-24 · 技能治理 P0/P0.5 + 事实层建仓

### 做了什么

1. **三件只读工具**落地于 `10_AI自成长引擎/技能库/_中枢/scripts/`：全机普查器、范围投影器、死链枚举器。配置解析器抽为单一真源 `lib_contract.ps1`。
2. **全机入册**：8663 份 SKILL.md（含 Loomy 81），逐条给出身份键、A/B/C 诊断分类、范围判定与证据路径。
3. **按新政策重划治理面**：范围层 832 份，其余 7831 份登记不治理。
4. **死链定性**：946 条全部为"源已删"，根因是 hub 删除 149 个技能目录、9 个宿主链接未回收。
5. **方案文档**：新增 `docs/04-方案_技能统一治理与浏览器能力域.md`（§一~§十四），含两次实测更正记录。
6. **本事实层建仓**：三套系统核心文档 84 份 + 8 个事实层文件。

### 为什么

用户主观感受是"整个电脑很混乱、重复、垃圾"，但无人能给出不靠感觉的规模判断。目标是把它换成可复现事实，并在动结构之前先确定"到底有多少值得动"。

### 结果

- 治理面比直觉**小两个数量级**：真正该管的 832 份（9.6%），可治理分叉 31 簇，其中 8 簇同源（一次工具升级可收口）。
- 用户自产资产仅 **1 个**技能（真源目录），而程序侧在持续自动长出（本阶段 9 → 10）。**瓶颈不是清理存量，是产出根本没入册。**
- 死链是单一事故，非 946 项手工活；清理是 1 个脚本动作，属不可逆，待批准。
- 三套系统并存、零融合代码，且存在**四处状态真源重叠**风险 —— 已提交为 `ISSUES.md` 的 Q1。

### 过程中的自我纠错（记录以防重犯）

| 错误 | 后果 | 拦下方式 |
|---|---|---|
| 用目录名当技能身份键 | 虚增假簇、漏报改名副本 | 改用 frontmatter `name` 并加缺失率上限自检 |
| 用 `Test-Path` 判 junction 活性 | 死链数会被报成 0 | 改活性探针；哨兵强制检出已知断链 |
| `@(管道 \| Measure-Object).Count` | 三个分类计数恒为 1 | 改 `@(管道).Count` 并加"分类合计=总数"校验 |
| 产物文件名未加前缀 | 一次冒烟覆盖掉全量三份报表 | 产物名一律带 `$Prefix`，并用校验和复验隔离 |
| 粗估断链 351 条（只抽查 5 宿主外推） | 低估 2.7 倍 | 全查 18 宿主 + 双实现交叉验证 |

### 未做（有意）

- 未删除、移动、修改任何被检对象；未建 registry；未清死链；未改任何人的既有仓库；未提交 git。
