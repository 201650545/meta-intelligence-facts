# DECISIONS

已正式确定的判断。**列出者请勿重新讨论**，要推翻请显式写明推翻理由并升版本。

| # | 决策 | 定于 | 理由 | 可推翻条件 |
|---|---|---|---|---|
| DEC-01 | 治理范围**只含用户侧资产**（自产 + 主动装 + 工具代装）；程序出厂自带者退出范围，只登记不治理 | 2026-09-24 | 用户裁定。物理去重全部收益是 87.9MB 与目录观感，代价是版本静默错配 + 集中后爆炸半径从 1 变 N | 若某程序提供官方技能目录重定向能力，可重评 |
| DEC-02 | 判据优先级：出厂标记 → 自产 → 工具代装 → 声明根 → 默认**范围外** | 2026-09-24 | 宁可漏判，也不把程序资产误纳进来后遭误删。出厂标记排在声明根之前，因"自带不可动"是下限，不该被一条清单条目绕过 | 出现证据可判别的新类别时增补 |
| DEC-03 | 技能身份键用 frontmatter `name:`，目录名仅兜底 | 2026-09-24 | 目录名同时虚增（包装目录 `.../skills/` 被当成一个叫 `skills` 的技能，聚出 193 份假簇）与漏报（Anthropic `docx` → 本地 `loomy-docx` 这类改名副本识别不出） | 无 |
| DEC-04 | 政策层与事实层分离：范围判定放在**投影器**（秒级），不放在普查器（6 分钟） | 2026-09-24 | 政策会反复调，重扫全盘代价不对称 | 无 |
| DEC-05 | 配置解析器**单一真源** `lib_contract.ps1`，多脚本 dot-source 共用 | 2026-09-24 | 治理方案自身不能犯它要治理的错（两份副本静默分叉） | 无 |
| DEC-06 | 判活必须用"穿过链接"的活性探针；**禁用** `Test-Path` 判 junction | 2026-09-24 | `Test-Path` 与 `[IO.Directory]::Exists` 对断开的 junction 一律返回 True，用它判活会把死链数报成 0（实踩） | 无 |
| DEC-07 | `Get-ChildItem` 类命令不得用于"靠异常判定"的场景 | 2026-09-24 | 它只抛非终止错误，`try/catch` 接不住，会带着"子项=0"的假结论继续跑 —— silent-risk 的标准形态 | 无 |
| DEC-08 | GitHub 作为 **AI 间共享事实层**；每阶段至少同步 `CURRENT_STATE` / `CHANGELOG` / `REPORT_TO_STRATEGIST` | 2026-09-24 | 用户裁定。commit 是执行历史，不是事实入口；不让高智力模型靠 diff 猜系统变化 | **v2（2026-09-25）**：本行只管"已发布"，"已送达"另判，见 DEC-11 |
| DEC-09 | 本仓内容**必须经类型化掩码**后才可推 | 2026-09-24 | 现有密钥扫描器不覆盖内部域名/账号卡/profile/端口（实测 0 命中，即完全漏检） | 若仓库永久私有且威胁模型收窄，可放宽 |
| DEC-10 | 上游引擎的**快照**取代 git 作为该仓的可回溯机制 | 更早，沿用 | 该仓裁定不进 git；每日 23:30 快照保留 14 份 | 若需要跨机协作则重评 |
| DEC-11 | **送达判据 = 三项回读凭证**（`delivery_id` + `CORE-TAIL-*` + `SPEC-TAIL-*`），不是 `git push` 成功 | 2026-09-25 | 高智力模型裁定并已由本地实现。同一 URL 实测可连续数小时失败后突然成功，机制未知 → 只能靠可验证的送达闸门，不能靠架构改动。凭证由 `build_core_bundle.js` 每轮随机生成，读不到就写不出 | 若某轮换承载（Pages／导出仓／附件），新承载须当场通过同三项回读才算切换成功；若首行 `delivery_id` 命中的是**上一轮**而尾凭证不符，判为"读到旧版本"（`raw.githubusercontent.com` push 后有 CDN 滞后），等 10 分钟重跑同一链接，不重发材料也不换承载 |
| DEC-12 | 读取失败的重试上限 **3 次、间隔 15 秒**；3 次仍失败即判"材料未完整送达"并**阻断作答**，不换承载、不凭记忆续答 | 2026-09-25 | 高智力模型裁定。无限重试无意义（已观测到数小时级故障），但一次失败也不足以否掉整轮 | 若同类三连败在连续 3 个交付轮次里出现 ≥2 轮，才启动换承载评估 |
| DEC-13 | **Demand 由元智能层持有**，上游/下游只持 `demand_id` 指针；不新增第五份可写真源 | 2026-09-25 | 高智力模型驳回本地草案"在上游新建 `demands/<id>.yaml`"：会让子系统成为整体语义的所有者 | 若三套系统被证明不共享任何长期目标，Demand 失去上位价值 |
| DEC-14 | 自动产物**先进隔离候选区**，任何改变运行时能力/正式技能库/共享真源的晋升与安装**必须人工批准** | 2026-09-25 | 高智力模型裁定。生成与生效分离，既保留自动学习又不破坏五步安装闸门第 4 步 | 出现可证明等价于人审的机器策略时，须先作为 Experiment 验证，不得直接取消人审 |
| DEC-16 | 元智能**控制平面** `D:\Work\元智能\` 是跨系统语义真源：Demand、DECISIONS、ISSUES、CHANGELOG 的 machine 层原件都在它里面；公开事实层仓与 Obsidian 首页一律是**生成投影**。证据层**禁止 junction/symlink**，改用机器生成的索引页 | 2026-09-25 | 高智力模型裁定（第 2~3 轮）。946 条死链已证明「批量链接、无人回收」是故障源，再造一层链接等于引入第三种失效模式；且既有规则是 Obsidian 只解释与索引，不反向充当运行事实源 | 若三套执行系统真的共享同一发布周期与依赖图，且实验证明 monorepo 降低维护成本又不破坏任何原生 SSOT，才重审 |
| DEC-17 | 生成事务边界 = 本地真源 → 事实层投影 → CORE/SPEC 副本 → 掩码扫描（**扫描失败即阻断**）；`git commit/push` 由 Agent 执行但**保留人工批准发布**。首页分列「本地状态」与「外部送审状态」：投影过期或发布失败只阻断外部裁定，**不否定本地 State** | 2026-09-25 | 高智力模型裁定（第 3 轮 c）。GPT 只能读公开投影，投影过期就会拿到假裁定，所以外部送达必须与本地状态分开显示 | 若公开仓升格为本地 canonical 仓库且同时撤销原本地存储，可合并两格；不得双持 |
| DEC-18 | **第一版范围**：必须有 `project.yaml` + Demand 真源 + 单一生成脚本 + State/首页生成 + 失败显式失效 + 生成后回读 + 输入指纹（先用排序后输入文件的 SHA-256，完整 `input_hash` 规则后补）。**可后补**：`state_dirty` 事件总线、10 分钟 watchdog、独立 `schemas\` 与 `adapters\` 目录。过渡期靠「执行 Agent 每次收尾必须生成一次并回读产物」兜底 | 2026-09-25 | 高智力模型裁定（第 3 轮 a）。单人系统，控制平面不能比业务重 | 一旦出现「有程序写真源但不走这条收尾规则」的实例，立刻补 watchdog |
| DEC-19 | 新增 **D-004「经验资产的质量与可复用性」**，不并入 D-001 | 2026-09-26 | 高智力模型裁定（第 4 轮）。上游九条核心任务无处归属，暴露的是 Demand 集合缺一块，不是判定失误；塞进 D-001 会把「治理」与「质量」混成垃圾桶。边界判据：问「这条经验对不对、有用吗、可复用吗」归 D-004；问「这份资产唯一吗、可控吗、可追踪吗」归 D-001 | 只有 D-001 正式升版并把经验质量纳入自己的独立 success_state、且 D-004 不再有独立生命周期与指标时，才合并 |
| DEC-20 | Task 归属规则修正：`demand_id` **只允许真实 Demand ID**；系统内部建设改用 `scope: local` + `system_id` + `demand_id: null`。局部 Task 不进任何 Demand 的 `active_tasks`，只有真正阻塞时才以 `blocking_dependency` 出现 | 2026-09-26 | 高智力模型裁定（第 4 轮）。「所有 Task 必须挂 Demand」是过强约束，硬挂等于逼执行模型伪造归属 —— 那正是 946 死链式的静默污染 | 一旦某 local 任务开始改变跨系统契约、用户级能力或某 Demand 的成功条件，必须转 global 并绑真实 Demand |
| DEC-21 | 控制平面自持原生 Task 台账 `D:\Work\元智能\machine\TASKS.yaml`（阶段 3~7 等，绑 D-003/D-004），**不把两个引擎看板的条目抄进来** | 2026-09-26 | 高智力模型裁定（第 4 轮）。判据：谁实际拥有执行与完成判定，谁持有该 Task 的原生记录。不违反「不新增第五份可写状态真源」—— 它不是第二份 Current State，也不复制他系统 Task；违规的是把下游 Task 抄一份形成双写 | 若未来建立全系统唯一 Task 服务并完成迁移、旧台账退役，则并入；在此之前不跨系统借看板 |
| DEC-22 | 切默认入口的前置条件固定为四项：CT-05（普通 Task/Decision 变化也要走通 State→首页）、T15/T16 退出 active blocker、State 已知缺口消失（见 DEC-26）、切换与回滚脚本实测通过。**切换不移除任何旧路径**：`Home.md` 原地改成生成的兼容入口并指向新首页，Obsidian 默认打开文件指向新首页 | 2026-09-26 | 高智力模型裁定（第 5 轮 P1）。规范只要求 Obsidian 有一个默认 Human View，没要求物理移动旧页面；移动才会制造断链 | 切换后出现旧链接失效、首页比 State 旧、生成失败仍展示正常状态、或任一 active 全局 Task 无可靠归属 → 立即 rollback |
| DEC-23 | 去旧写口顺序固定 **CHANGELOG → DEMAND → DECISIONS → ISSUES**（ISSUES 最后）。生成频率分两层：Agent 收尾刷 machine 并重生成四份投影；**准备送审时无条件再生成一次**，再出 CORE/SPEC、掩码扫描、freshness 校验，全过才允许 commit/push | 2026-09-26 | 高智力模型裁定（第 5 轮 P2）。ISSUES 是外部模型的实时输入，生成时机错一轮就会让它回答旧问题；CHANGELOG 只是已发生事项，迁移错误即时影响最小 | 某文件存在 machine 原件未承载、而公开旧文件独有的有效事实 → 停止迁移，先把事实迁回 machine，不许靠继续双写解决 |
| DEC-24 | 迁移期双写的机器判据：**machine 永远胜，公开层绝不反向合并**。每个投影登记 `source / source_sha256 / projection_sha256 / generator_version / build_id`；每次收尾与每次 pre-push 执行"读源算哈希 → 临时目录重渲染 → 比对期望与实际投影"，两条件任一失败即 exit 非 0 并禁止打包 push。不一致时先把异常版本复制到 `runtime/divergence/<时间戳>/` 留证，再用 machine 真源覆盖 | 2026-09-26 | 高智力模型裁定（第 5 轮 P3）。这套比对还能区分两类错误：源变了投影没变=投影陈旧；源没变而实际≠重渲染=公开层被手改 | 公开仓被正式升格为 canonical 并同时撤销 machine 原件，才允许反转权威方向；当前架构下绝不双向同步 |
| DEC-25 | **第一版不加 manifest**（文件数/字节数/SHA-256）。触发条件：出现一次可复现案例——三项本轮 nonce 全匹配，但中间内容缺失、串包或与本地 bundle 不一致。届时加独立 manifest，仍不替代三个 nonce | 2026-09-26 | 高智力模型裁定（第 5 轮 P5）。现在加只能覆盖一个尚未出现过的中段损坏故障模式，违反"不因假设性风险增加维护复杂度" | 发布或抓取通道自身提供可在接收侧核验的端到端内容哈希时，无需自建 |
| DEC-26 | **EXPERIMENTS 纳入 State 输入**。每份实验按既有 schema 补可解析字段（`id / demand_id / hypothesis / baseline / candidate / status`）；`planned|running` 进 `active_experiments`，`completed|cancelled` 不进，其结果进 `recent_changes`。语义固定：**`[]` 表示机器确认当前无活动实验，`null` 表示生成器不知道**，二者不得混用 | 2026-09-26 | 高智力模型裁定（第 5 轮 P4）。今天已真实产出两份实验，规范把 Experiment 定为 V1 一等对象且 State 明确含"当前实验"，继续写 null 等于用"不知道"冒充"没有" | 实验不再是一等运行对象且生命周期被另一正式对象完全取代，才移出输入；当前 V1 规范下不能省 |
| DEC-27 | 事件覆盖判据固定：State 必须记录 `changed_inputs` 与 `change_kind`，能区分本次变化来自 demand / task / decision / 实验 / 组件健康；归因不出来即视为生成器缺陷，不得当作"无变化" | 2026-09-26 | CT-12 实测：只改 `TASKS.yaml` 时归因精确为 `["tasks/TASKS.yaml"]`、kind 为 `task`，D-003 活动任务由 5 变 4、下一步自动换成 CT-05。DEC-22 要求两类事件都过，本条覆盖 Task 与 Decision 两类 | 若输入集合扩张到无法穷举哈希（例如新增未登记的真源），必须先扩登记再谈归因 |
| DEC-15 | **判定可信度进入 V1 完成条件**：会写 State、触发动作或出正式结论的判定器须有正负样本哨兵、数值范围与合计不变量、至少一种独立交叉验证；`self_test` 失败一律 fail-closed | 2026-09-25 | 规范主循环本就要求"结果可靠吗"；实测已出现"结构完整但语义错误"的产出 | 明确只作草稿、不进 State/Decision/动作的输出可降到非阻断验证 |


## 与既有仓库约定的衔接（不重复讨论）

- 技能 id 命名沿用 `loomy-learned-<kebab>`，**不另起命名体系**（否则新旧检索分裂）
- 技能安装走五步闸门，第 4 步需人工点头
- 撞车检查必须在**落盘之前**，命中同题走增补（标 `relates_to`）不建平行条目
- **引用即校验**：写入文档/配置的路径需 `Test-Path`，数值常量需跨真源比对并标 `source`
- 规则真源是 `AGENTS.md`，宿主侧一律用指针文件，不复制正文
