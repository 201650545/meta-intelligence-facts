# REPORT_TO_STRATEGIST

阶段：2026-09-24 · 技能治理与三系统融合前置　｜　写者：本地执行模型
补记：2026-09-25 · 材料入口修复（见 §2 末条与 §3 末两行），问题本身未变

## 0. 材料怎么到手（2026-09-25 结案，含我三次更正）

**请不要再尝试抓本仓的任何 URL。** 七条探针（每条我都在本地先验过 HTTP 200 才发给你）测出的结论是：**你的网页抓取层不回源**，一条 URL 你能读，只因为它已在缓存里。这与是哪个仓、什么内容、文件多大（139 B 到 200 KB 都试过）、路径是否中文、用 `main` 还是 commit SHA —— 全部无关。

定案的是 D 轮：我把两条**你声称读得到的那个仓**里的新路径发给你（`code/lib/widgets/anki/anki_review_dialog.dart`，20 KB Flutter 源码；`docs/00_项目驾驶舱_可视化中心/03_WBS任务分解全景图.md`，5.3 KB 中文路径），两条都读不到。

我在这条因果上一前一后写错了三次，都留在这儿，因为它们正好说明"为什么必须有回读确认"：

1. 先写"你的通道缓存优先、未缓存即失败" → 被 B（你读到了 `OPEN-QUESTIONS.md`）否掉；
2. 改口"故障按仓绑定" → 被 P1/P2（SHA 固定链接也失败）动摇；
3. 中间还怀疑响应体大小上限 → 被 43 KB 与 139 B 的失败否掉。

**B 那次"成功"其实是缓存命中，不是回源能力**，而我当时没验证就据此推翻了第一条判断，多绕了两轮。

据此，以下四种方案全部无效、我不再尝试：SHA 固定链接、**另建公开导出仓**（你建议的）、给本仓开 GitHub Pages、等 GitHub 搜索收录（本仓当日已在索引内）。

**本轮材料的到手方式改为：由我作为对话附件提供。** 两个根级合并副本 —— `ALL_CORE_DOCS.md`（约 40 KB，治理层 7 文件 + 作答要求）与 `ALL_SPEC_DOCS.md`（约 200 KB，规范层 55 文件），合起来就是全部阅读清单。仓库继续作为**真源与人读的入口**，只是不再指望被抓取。

若你收到的材料不完整，请只回复"材料不完整"并说明缺哪一部分，**不要在没读全的情况下作答**。另请回答 `ISSUES.md` Q8：送审介质选 A（附件）／B（GitHub connector）／C（先 A，B 通了转 B），以及你认为"回读确认"的最小判据该长什么样 —— 我这边能做到的形式是：请你在回答开头**原样复述**指定文件的首行或本轮 nonce。



## 1. 本阶段目标

两件：(a) 把"这台机器 skill 很混乱"从主观感受变成可复现事实并定出治理面；(b) 在动结构之前，为三套系统融合提供决策所需的事实底座。

## 2. 实际完成内容

- 三件**只读**工具落地（普查器 / 范围投影器 / 死链枚举器），共用一份配置解析真源 `lib_contract.ps1`，零硬编码判据，全部判据外置在 `audit.config.yaml`。
- 全机 8663 份 SKILL.md 入册，逐条给出身份键、分类、范围判定与证据文件路径。
- 按用户新裁定政策（**只管用户侧资产，程序出厂自带者退出范围**）重建治理口径。
- 死链逐条定性，根因定位到单一事故。
- 方案文档 `10_AI自成长引擎/docs/04-方案_...md`（§一~§十四），含两次自我更正记录。
- 本事实层建仓：84 个文件，含三套系统核心文档，全量类型化掩码。
- **（2026-09-25 补）** 两个根级 ASCII 合并副本 `ALL_CORE_DOCS.md`（约 40 KB，治理层 7 文件）/ `ALL_SPEC_DOCS.md`（约 200 KB，规范层 55 文件），由脚本从本仓**已掩码**文件拼接，作为抓取通道的稳定入口。

## 3. 验证结果

| 验证 | 结果 |
|---|---|
| 普查可复现 | 两轮逐项一致（8663 / 907 / 946） |
| 死链双实现交叉 | reparse+活性探针 vs `dir /AL` 解析目标，**逐宿主计数全部一致** |
| 与第三方工具对账 | 独立 bash `find` 通道在 `.trae` 上复现 149/190，与工具吻合 |
| self_test | 三件工具均 pass；开发期两次真实拦下自伤性 bug 并 exit 3 拒发结论 |
| 脱敏 | **23 类**规则复扫：掩码目标 21 类 **0 命中**；余 2 类为有意保留项（本机用户名 225 次全在路径内、散文 0 次；"私有化"类别词若干行）。详见 `REDACTION.md` |
| 越界检查 | 全程只读；未删/未移/未改任何被检对象；未提交任何人的既有仓库 |
| **（补）仓库可达性** | 匿名实测 11 种 URL 形式全 200：raw ×3 连测、`github/raw` 跳转、blob 网页、仓库首页、`contents` API、`git/trees?recursive=1`（97 blob / `truncated=false`）、repo 元数据 |
| **（补）扫描器计数单位** | 原把"命中行数"标成"处"，导致对外报数偏小（220 vs 225）。已改为行数与次数分别输出，并用第二个独立计数实现交叉验证，两者同为 225 |
| **（补 2）对方侧读取探针（七条，定案）** | 失败：本仓 `main`/SHA × 139 B/2.2 KB/4.4 KB/43 KB/200 KB 全读不到；**另一仓**两条从未读过的新路径（20 KB 深 ASCII、5.3 KB 中文路径）同样读不到。成功：仅 `OPEN-QUESTIONS.md` 一条，事后判定为**缓存命中**。→ 结论：抓取层不回源，能否读到只取决于该 URL 是否已被缓存；仓、内容、体积、路径语言、URL 形态均不构成变量。全部对照物我先在本地验过 200 |


## 4. 当前系统状态

三套系统并存、零融合代码。治理面经两次收窄后很小：**832 份在范围、31 个可治理分叉簇、946 条死链**。而真源目录只有 **1 个**技能，Loomy 却在持续自动长出 learned 技能（本阶段从 9 涨到 10）。

## 5. 新发现的问题或风险

1. **状态真源已有四处重叠**：记忆库 `STATE`、上游 `01-任务看板`、下游 `runtime/*.drift.json`，加上本协议要求的 `CURRENT_STATE.md`。不先定映射就落地，会精确复现我们刚花两小时治好的那个病（副本静默分叉）。
2. **规范包自相矛盾的风险点**：它一方面要求"机器层必须有稳定 ID / 索引 / 归档契约"，另一方面把"文件如何组织"完全下放给执行模型。今晚 946 条死链正是"批量建立引用、无人回收"的产物 —— 这个口子不堵，同型事故会重演。
3. **自动产出与人审闸门天然冲突**：Loomy 持续生成 learned 技能，而体系要求"安装需点头"。两者没有既定接口，目前靠运气。
4. **便宜模型的判定可信度未被列为验收项**：规范包 V1 唯一的验证问题问的是"需求能否不依赖用户管文件而持续运行"，没问"执行模型能否可靠判定"。而本阶段两次事故都是**判定器坏了却产出合理假结论**——同型风险最高。
5. **敏感面**：该体系的事实描述必然含第三方服务域名、账号卡标识、企业私有化域名。现有密钥扫描器一条都不命中。本仓已按类型掩码，但映射表留在本地，长期需固化成 push 前强制闸门。
6. **（2026-09-25 新增，同日三次更正后结案）协作链路有一个未列入验收的脆弱点：事实层的"可读性"取决于对方抓取层的缓存，而它不回源。** 详见 §0 与 `CURRENT_STATE.md` §六。要点不在机制细节，而在：**协议把 `git push 成功` 当成"事实已同步"，而真正的判据应当是"对方对这一轮交付物的回读确认"**。本轮我先后写过四条因果（缓存优先 / size 滞后 / 按仓挡 / 按内容挡），前三条都被后续探针打掉 —— 全都是被"push 完就当对方拿到了"放过去的，直到做完 D 轮对照才定下来。元智能 V1 的验收项里没有这一条，这就是 `ISSUES.md` Q8。
   附带实测到：**`git push --force` 不能清除已公开的游离提交**（按 SHA 仍返回 200），只能删仓重建 —— 已写进 `REDACTION.md` 教训二。今后不再为刷新内容而删仓。

## 6. 仍未确定的事项

见 `ISSUES.md` 的 Q1–Q7。其中 Q1（融合后谁是状态唯一真源）是其余问题的前置。

## 7. 建议的下一步

**先定 Demand，再定真源归属，最后才谈结构合并。** 我的倾向是不做"三合一重写"，而是让元智能补上位：它只提供 Demand 层与 Human View 标准，两套引擎继续作为各自的执行/沉淀真源，`CURRENT_STATE.md` 退化为"摘要 + 指针"而非新的状态所有者。

**在 Q1 有答案前，建议冻结一切结构性改动**；唯一可并行的是 946 条死链清理（不依赖架构选择）。

## 8. 需要高智力模型判断的问题

`ISSUES.md` 共 7 条，请按编号回答。**Q1 / Q2 / Q5 / Q6 必须给明确结论**（不许"取决于情况"），Q3 / Q4 / Q7 可给倾向 + 判据。另需你帮着定义 `DEMAND.md` 的 **D-003**。

> 更正：本节原写"Q1/Q2/Q3 是本轮的核心"，与 `PROMPT_FOR_GPT.md` 和 `ISSUES.md` 的口径（Q1/Q2/Q5/Q6 必答）不一致，已按后两者统一。Q3 降为次级，因为它只影响人的阅读体验，不影响系统能否跑。

