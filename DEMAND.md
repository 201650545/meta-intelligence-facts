# DEMAND

> 格式：让 ______ 长期保持 ______，并在 ______ 变化时自动重新评估。

## D-001 · 个人 AI 作业体系的可控性

让本机所有 AI Agent 的作业体系长期保持**单一真源可追溯、引用不悬空、规则与技能不分叉**，并在新增 Agent 宿主、程序版本更新、或引入新规范包时自动重新评估。

**why**：用户单人运维，判断带宽是唯一稀缺资源。体系一旦分叉，表现不是报错而是"另一个工具不守规矩了"，排查成本极高。

**success_state**：任一时刻能回答"X 的真源在哪、谁在引用它、引用是否有效"；新增复杂度必须自证降低了长期维护成本。

**constraints**：不可逆操作与共享状态变更需逐次人工批准；技能安装必须人审点头；文档结论必须可回指证据。

**当前版本变化（2026-09-24）**：新增一条明确边界 —— **治理范围只含用户侧资产（自产 + 主动装 + 工具代装），程序出厂自带者退出范围，只登记不治理**。

## D-002 · 送审链路的可用性与判断质量

让"向高智力模型送审方案"这条链路长期保持**有活卡可发、档位确实生效、结论确实被读到**，并在账号池轮换、站点改版、档位文案漂移时自动重新评估。

**why**：这是体系里唯一的外部智力来源。链路断了不会报警，只会静默降级成"用便宜模型的答案"。

**已观测到的漂移事实**：站点侧 `isHealthy` 与 `message` 不同义（存在"受限但仍活跃"的卡）；池页对无浏览器指纹的纯 GET 返回 1 字节 SPA 壳，HTML 正则发现路径**永久无效**。

## D-003 · 三套系统融合（2026-09-25 由外部裁定定义，本地采纳）

**statement**：让一个长期 Demand 在元智能、执行验证引擎与经验沉淀引擎之间长期保持**统一语义、明确归属、可追溯联动且无重复可写真源**，并在任一子系统的契约、职责边界或能力发生变化时自动重新评估三者映射。

**success_state**：任一 active Demand 都能沿一条确定链路回答 —— 当前 Demand 是什么 → 当前 State 是什么 → 哪些 Task/Experiment 正在执行 → 哪个原生系统提供证据 → 哪些结果已沉淀为可复用经验；同一事实不存在两个可独立修改的正式真源；任何视图都能回指原生证据；任一子系统可被替换而不改变 Demand 的语义。

**constraints**：不新增第五份可写状态真源；保留已验证的下游契约/探针机制与上游人审、经验沉淀机制；元智能只拥有跨系统语义，不吞并各系统的执行真源；用户不需要管理机器层文件结构；新增对象、字段与自动化必须通过"降低认知成本、服务 Demand、降低长期维护成本、有真实证据"四道 Gate。

**recheck_on**：任一子系统更换其 SSOT/契约；Demand 模型或稳定 ID 规则变化；State 投影与原生事实出现无法自动消解的不一致；同一事实再次双写；新的执行/沉淀系统加入或旧系统退出；Human View 无法从 Current State 确定性生成；一次真实 Demand 变化无法完整传播到 Task/Experiment、State 与 Human View。

刻意不与 D-001（全机 AI 资产治理）、D-002（送审链路可用性）重叠：D-003 只管**三套系统之间的语义融合与职责边界**。

**status**：active　**version**：1　**changed_at**：2026-09-25　**来源**：`ISSUES.md` Q8 轮次高智力模型作答，本地采纳并登记为 DEC-13

