# 执行方案 · GPT-5.6 Sol 规划 + DeepSeek V4.1 Flash 执行 · 2026-09-22

> **本方案的任务**：完成《任务转交_给执行Agent_20260921.md》（下称"交接书"）§三 的 A1 收口——修探针 X1-X4 缺陷 → T6 首跑 → A-G 七项验收 → 契约 status 转 verified。
> **分工链路**：GPT-5.6 Sol（读 GitHub 做理解与规划）→ DeepSeek V4.1 Flash（DSH 新窗口执行）→ Claude 调度大脑（过程监督 + 四件套复核）→ GPT 镜像（验收审校，可选）。
> **交接书是唯一任务真源**；本方案只定"怎么分工、怎么交接、在哪停机"，不重复任务内容。

---

## 一、总流程（四阶段，两个停机点）

```
阶段0 GitHub同步 ──→ 阶段1 GPT规划 ──→ 阶段2 DSH执行 ──→ 阶段3 验收回路
     │                  │                  │
  密钥扫描           停机点①：          停机点②：
  建库推送           规划产出交郭老师     T6 首跑前需
  （无需批准）        过目后放行执行      外网 GET 许可
```

- **停机点①**：GPT-5.6 Sol 的规划产出（含修法确认 + 首跑风险清单）先给郭老师过目，批准后才进阶段 2
- **停机点②**：交接书 §3.3 明文规定——探针外网 GET（`<POOL_HOST>`、`<MIRROR_HEALTH_API>`，经代理 127.0.0.1:7890）**须郭老师明确许可**，许可到位才跑 T6；未许可前执行 Agent 只能完成到"修完 X1-X4 + §3.5 语法校验零错误"

## 二、阶段 0：GitHub 同步（Claude 执行，约 15 分钟）

1. **建库**：GitHub 新建 **私有** 仓库 `adaptive-workflow-engine`（必须私有——契约里含会员卡号发现规则、账号池 URL、有效期等私有账号信息，绝不入公开库）
2. **密钥/隐私扫描**（推送前逐项过）：
   - `contracts\gpt-mirror-extended-review.yaml`：核对 card_id_discovery 正则、account_pool URLs——私有库可接受，但确认无明文卡号/密码
   - `probes\probe_gpt_mirror.ps1`：确认无内嵌 token（ai-platform 曾有 CF token 内嵌被 push protection 拦截的前科）
   - `草稿区\`、`_快照\`、`runtime\`（如已存在）：**不入库**，写 .gitignore
3. **推送范围**：contracts\ + probes\ + docs\ + scripts\ + 技能库README + 交接书本体；`D:\Work\AI自成长引擎\` 上游项目**二期再推**（本次任务只读它，不需要进库）
4. 推完把仓库 URL 交给阶段 1

## 三、阶段 1：GPT-5.6 Sol 规划（ChatGPT 镜像，1 个新窗口）

- **输入**：仓库 URL（提示词强读）+ 一段任务背景（引用交接书 §一/§二/§三，贴关键原文）
- **要它产出三样**（只做规划，不改代码）：
  1. X1-X4 修法的技术确认（尤其 X1 的 PS 5.1 解析歧义修法是否稳妥、X3 顺带核查项的判断）
  2. 首跑风险清单：按 §3.3 A-G 逐条预判哪里最容易翻车（F 项网络归因误判是重点）
  3. 验收核对表：A-G 每项给出"执行时应采集什么证据"
- **轮次纪律**：本窗口 ≤24 轮（用户已裁定 12→24），每 3 轮闭环一个子任务；GPT 反问继续答即可不算新任务
- **产出落盘**：规划全文存 `D:\Work\自适应工作流引擎\docs\GPT规划_GPT56Sol_20260922.md`，摘要给郭老师过目 → **停机点①**

## 四、阶段 2：DSH 执行（DeepSeek V4.1 Flash，新窗口）

- **载体**：DeepSeek Harness 新窗口，模型用默认 `deepseek-flash`（=V4.1 Flash，官方 API，settings.yaml 已配好）
- **任务简报（写入 DSH 任务，自包含三段）**：
  1. **任务书指针**：本地路径 `D:\Work\自适应工作流引擎\任务转交_给执行Agent_20260921.md`，令其先只读该页
  2. **GPT 规划增补**：把阶段 1 产出的修法确认与风险清单贴给它（作为参考，不替代交接书）
  3. **硬约束原文 + 四件套要求**：交接书 §八 八条逐条贴入，§七 四项回报格式贴入；另加一条本方案补充——**"凡交接书标注『需用户许可/未答复』的事项（外网 GET、D2/D4/D6/D8、AGENTS.md 修改），一律停在询问，不得自行行动"**
- **执行顺序**：§三 X1→X2→X3（X4 顺带）→ §3.5 语法校验 → **停机点②**（若无外网许可则到此交半程回报）→ 有许可则 T6 首跑 → A-G 验收 → 契约转 verified + 台账 T6/T18 同步
- **过程监督（Claude）**：DSH 跑探针属本机操作无风险；但每个停机点我主动向郭老师要指令，不催不代办

## 五、阶段 3：验收回路

1. 执行 Agent 按 §七 交四件套（PASS/FAIL 表 / 变更清单 / 实测贴屏 / 已知偏差）
2. **Claude 复核**：对照交接书逐条核验（重点：B 项 `probe_self_test==pass`、D 项无字面量卡号、F 项无 network 误报 break、runtime 产物与 drift 计数一致性）
3. **GPT 镜像审校**（可选）：四件套 + 变更 diff 发镜像窗口做独立复核（新窗口、≤24 轮）
4. 全部通过 → 郭老师终验归档；任一 FAIL → 按交接书"缺一即返工"

## 六、明确不在本次范围（防跑偏）

- D2/D4/D6/D8 四裁定、主线二（AGENTS.md 补跑，需单独许可）、主线三（撞车检查内建）、A2 runner、第二试点——本次只做 A1 收口
- `D:\Work\api-gateway\`（509.8MB）：不删不挪不 git init
- 上游 `AI自成长引擎\`：只读

## 七、风险与回退

| 风险 | 对策 |
|---|---|
| 契约/脚本含隐私入 GitHub | 强制私有库 + 推前扫描（§二.2） |
| GPT 规划越权改代码 | 提示词写明"只出方案"；镜像本无本地执行能力，天然隔离 |
| DSH 执行偏航 | 硬约束原文贴入 + 停机点硬门 + Claude 过程监督 |
| 首跑遇网络失败误报 break | F 项是 GPT 规划重点 + 四件套必查项 |
| 上下文超长 | 交接书自包含，DSH 新窗口只读单页即可开工 |

## 八、当前状态与启动条件

- ⏳ **待郭老师**：①外网 GET 许可（停机点②前置）②D2/D4/D6/D8（不阻塞 A1，但 D6 关系首跑后的调度安排）③停机点①的规划放行
- ✅ 已就绪：DSH 环境、网关 :3100、GitHub 账号、本方案
- 启动顺序：郭老师说"开始" → Claude 跑阶段 0 → 开 GPT 窗口做阶段 1 → 停机点① → …
