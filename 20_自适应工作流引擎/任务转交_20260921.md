# 任务转交 · 给执行 Agent · 2026-09-21

> **接手请先只读本页**。本页自包含，不需要前一任 Agent 的会话上下文。
> **本任务不再由原 Agent（Loomy）执行**；所有后续操作由你（执行 Agent）完成，并按 §七 要求回报。
> **交接日期**：2026-09-21（原 Agent 最后一次状态核实见本节末尾）
> **管辖范围（用户明确限定）**：只管 `D:\Work\AI自成长引擎\`（上游·沉淀层）与 `D:\Work\自适应工作流引擎\`（下游·运行+自愈层）两个项目。ai-platform、记忆库、全局配置均属外部真源，只读+需授权。

| 速览 | |
|---|---|
| 立即要做的第一件事 | 修探针 `probes\probe_gpt_mirror.ps1` 的 3 处缺陷（含 1 个**语法错误，当前根本跑不起来**），再执行 T6 首跑 |
| 最大陷阱 | 探针脚本**从未成功运行过**，`runtime\` 目录至今不存在（见 §三） |
| 状态 | 用户已完成 5 项裁定（§五）；**D2 / D4 / D6 / D8 仍无答复，不得自行替用户拍板** |

---

## 一、项目背景

### 1.1 上游 · AI 自成长引擎（`D:\Work\AI自成长引擎\`）
沉淀层：对话中自动捕获经验 → 判定是否值得留 → 提炼成三级产物 → 固化 → 回放，带衰减与撞车检查。三级产物（决定生效路径）：

- **L1 经验条目**：落 `草稿区\L1_经验\`，纯静默（不加载不触发）
- **L2 工作流 SOP**：落 `草稿区\L2_SOP\`，**必须用户确认后才算定稿**
- **L3 skill**：先落 `草稿区\L3_Skill\` 作缓冲区，**唯一归档地 = 同目录 `技能库\<skill-id>\`**（提炼完成后须"移动"过去并在 `技能库\README.md` §二 清单登记一行）；L3 的"安装"（复制到 `D:\记忆\_Skills\` 才会真生效）须用户点头 + dry-run

### 1.2 下游 · 自适应工作流引擎（`D:\Work\自适应工作流引擎\`）
核心判断：问题不是"没有自动化"，而是自动化**静默过期并给出假信号**。
范式照搬 ai-platform 已验证的三件套——**声明态（只写期望，绝不写当前健康）/ 只读探针（产物禁手改）/ drift 投影（偏差**）：

1. 声明态与运行态分离；执行体从契约读参数，不得出现字面量域名/选择器/文案/数值
2. 契约：`contracts\gpt-mirror-extended-review.yaml`（workflow_id=`gpt-mirror-extended-review`，当前 status=`draft_pending_first_run`）
3. **drift 分级**：`info` 候选集内自填 / `warn` 候选集外出草稿 / `break` 前置不成立即熔断 / **`silent-risk`（成功但校验点不过=视为 break，绝不产出结论）**

试点 = GPT 镜像站（AI <POOL_NAME_CN>）Extended 送审，`evalB_ext.js` 于 2026-09-15 文案改版后三处硬编码静默断裂（仅加失效标注，未改代码）。

---

## 二、★ 当前最关键事实：探针从未成功运行

`probes\probe_gpt_mirror.ps1`（22,736 B）2026-09-21 由原 Agent 写完，**仅做过语法解析，从未执行**：

- `runtime\` 目录不存在（探针首次成功后才会 `New-Item` 出来）
- 契约 `status` 仍 `draft_pending_first_run`（本应首跑通过才转 `verified`）
- 原 Agent 选择"不挂定时任务"——探针调度频率 D6 用户未答
- **交接前终验**（PS5.1 解析器）实测：探针有 **1 个语法错误 + 2 处运行期隐患**，当前不可执行；已确认 runtime 未被写过

> 任何声称「drift 检测已能跑通 / 已产出 drift.json」的说法都与盘面矛盾，不要采信。

---

## 三、立即要做的第一件事：修 3 个缺陷 → 跑 T6 首探（A1 收口）

### 3.1 缺陷清单（均已定位到行，改前先读上下文核实行号是否漂移）

| # | 类别 | 位置现状 | 修成什么 |
|---|---|---|---|
| **X1** | **语法错误（阻塞执行）** | 探针 L382 `actual = "$days 天（已过期 $([-math]::Abs($days)) 天）"` → 报 `Missing type name after '['` | 插值子表达式里带一元负号触发 PS 解析歧义。改为先算 `$abs = [math]::Abs($days)`，再 `actual = "$days 天（已过期 $abs 天）"`。修完复跑 §3.5 语法校验 |
| **X2** | 死代码 | L206 `exitCode = 0`（行首丢了 `$`） | 删除该赋值，或恢复 `$exitCode = 0` |
| **X3** | 字段名笔误 | L421 `workflow_id = (Get-Cfg $C 'workflow_workflow_id_placeholder')` | `'workflow_id'`（契约顶层正是该字段）。顺带核查 `contract_version` 键是否真被 `Read-Contract` 扁平化为顶层标量 |
| X4（可选） | 无害 no-op | L379 `$selfTest.network_reachable = $selfTest.network_reachable` | 删除，避免误导 |

### 3.2 首跑

```powershell
powershell -ExecutionPolicy Bypass -File "D:\Work\自适应工作流引擎\probes\probe_gpt_mirror.ps1"
"退出码 $LASTEXITCODE"     # 0 无break · 2 有break(勿执行送审) · 3 自检失败(结果不可采信,勿当drift用)
```

产物（契约是唯一字段来源，**脚本内不得新增任何硬编码域名/卡号/正则/阈值**）：

1. `runtime\gpt-mirror.runtime.json` — 全量事实
2. `runtime\gpt-mirror.drift.json` — 偏差投影
3. stdout — 自检判定 + info/warn/break/silent-risk 计数（空跑也须留下自检记录）

### 3.3 验收标准（对应台账 T6、T18）**全部满足才算 A1 收口**

| 项 | 标准 |
|---|---|
| A | 退出码 ∈ {0,2,3} 且与 drift 内容逻辑一致 |
| B | `probe_self_test == pass`（**探针自身坏了 ≠ 站点变了**，这是本项目立身之本） |
| C | 产出 ≥1 条真实 drift。**预期 info**（`membership_expiry` 当前 2026-10-28 还剩约 37 天 > warn 阈值 14 天 → 走 info，不报 warn）；`active_card` 是 info 还是 break 取决于首跑真实网络，**如实记录，别为凑 drift 假报** |
| D | `carid` 全部走契约 `card_id_discovery`（html_regex 主 / 有限 enumerate 兜底），日志写明 method；脚本内无字面量卡号 |
| E | 只读核验：无 PUT/POST/点击痕迹（本探针仅 GET/`Method='Get'`） |
| F | **网络失败必须归因为本机/代理问题**，不许误判为"站点挂了"触发 break（代码分 `http_error` vs `network_error`，真机跑时确认无 `check_local_network` 项误报 break） |
| G | 首跑通过后改契约 `status: draft_pending_first_run → verified`，并同步台账 T6/T18 |

> 首跑前先与用户确认一次：**本探针要访问外部镜像站域名（契约 `conditions.account_pool.urls`、`active_card.endpoint`，代理 127.0.0.1:7890）。用户此前只授权了 D7 会员提醒，**探针外网首页 GET 属未明确许可的外部访问**，请取得许可后再跑。

### 3.4 首跑之后顺带修一个已知解析器局限（不阻塞跑通，但影响未来消费这些字段）

`Read-Contract` 对形如 `- {label: "...", weight: 100}` 的**内联 map 列表项**只取整串（`$val -match '^(.*?):\s*(.+)$'` 对 `{label: ...}` 会错拆为键 `{label`）。P1 不读 candidates，暂无功能损坏；但**下一步 A2 runner 若要消费 candidates，须先给该分支加 `TrimStart('{')` 后整体解析的分支处理**。

### 3.5 自检工具（修完随手跑，X1 同款校验）

```powershell
$e=$null; [void][System.Management.Automation.Language.Parser]::ParseFile("D:\Work\自适应工作流引擎\probes\probe_gpt_mirror.ps1",[ref]$null,[ref]$e); $e
```

---

## 四、后续两条主线（用户已知方向，按序推）

### 主线二（上游 M0 补跑，零成本，但**当前无实现载体**）
全局路由 `C:\Users\郭永涛\AGENTS.md` 目前只实现「P1 信号即时静默落 L1」，**「会话末 L2/L3 建议固化清单 → 用户勾编号」尚未实现**，而它是进 M2 的前置条件。补跑一次并实装（落点可能是技能库某 skill 或全局路由条款——属外部真源，**改 AGENTS.md 需单独许可**）。

> ⚠️ 与裁定 #2 的张力：用户裁定 L2/L3 必须留人工闸门（不许静默），但全局路由第 2 条「静默范围仅限 L1」只是写了要求，**清单功能本身还不存在**。先实现再谈"已保住闸门"，别让文档口径掩盖功能缺位。

### 主线三（上游 M1 加固，可独立做）
**撞车检查内建**：上游 `docs\01-任务看板.md` §七 2026-09-21 复盘显示，3 条 L1 里 2 条与既有真源同题（撞车率 2/3）——P1 落盘时未做"先比对既有真源"。修法（已写入 `docs\03-方案构想.md` §2.2 与 `经验沉淀SOP.md` §三）：
- **落盘前**先查 ①Loomy memory ②`D:\记忆\调度大脑记忆\` ③`技能库\`
- 命中同题 → 走增补：补 `relates_to: <既有真源路径>` + 写明本条 delta；**不新建平行条目**
- 已人工补标 2 条；目标是内建到捕获环节，不再靠人工
- 前置：**D4 周报仍未答复，别擅自建 Loomy 定时任务**

---

## 五、用户已裁定事项（勿重复问，照此执行）

| # | 决策 | 内容 |
|---|---|---|
| 1 | 载体（D1/D2） | **契约中立 YAML + Loomy skill/脚本起步，本机不装 n8n**（当前无任何 n8n 痕迹）。n8n/orchestrator 是 A6 远期导出，不是现在的选型；改 ai-platform 服务属外部真源，需单独许可 |
| 2 | 人工闸门 | **静默仅限 L1；L2 出草稿待确认，L3 必须点头 + dry-run** 才进 `技能库\` |
| 3 | 项目 skill vs 程序 skill | 项目 skill 一律先落 `技能库\`，**绝不直写 `D:\记忆\_Skills\`** |
| 4 | ai-platform 旧脚本 | 只加失效标注不改代码（已执行）；509.8 MB 重复目录 `D:\Work\api-gateway\` **按裁定原封不动** |
| 5 | 中转区 + git | L1/L2/L3 缓冲区统一落 `AI自成长引擎\草稿区\`；快照脚本 `scripts\快照草稿区.ps1`（定时任务 `schedule-1789962626722-v0u2w15` 每日 23:30） |
| 附 | 轮次纪律 | 统一 12→24 已四处落定（详见 `docs\02-镜像站试点盘点.md` §八） |

---

## 六、仍需用户答复（**不阻塞主线，不许自行拍板**）

- **D2** 沉淀触发时机（影响主线二能否定稿）
- **D4** 要不要每周自动复盘报表（M4/主线三 依赖，否则撞车率无人定期查看）
- **D6** 探针最终调度频率（先只手动跑，定稿后再设 daily/pre-execution）
- **D8** 第二个试点选哪个（A5）
- 附带遗留：`D:\Work\api-gateway\`（509.8 MB 重复目录，非 git）**canonical 归属未定 —— 不要删、不要挪、不要 git init** | ai-platform 6 个未提交 M 文件由用户自己处置 | 上游 D1 / D6 | 探针外网 GET 许可（§3.3 末）

---

## 七、完成后必须回报（四项，缺一即返工）

1. **模块 × 验收项 PASS/FAIL 表**：至少覆盖 X1–X4 修复、T6 首跑验收项 A–G、契约 status 转 verified、探针零回归解析
2. **变更文件清单**：绝对路径 + 每文件一句话
3. **验证结果**：贴 `probe_self_test` 判定、drift 计数、`runtime\` 实际产生的文件名，**不要只写"已验证"**；另贴 §3.5 解析器零错误输出
4. **已知偏差**：凡与本任务书不一致处，以及"探针首跑结论与文档假设不符"的点，必须如实列出，**不得静默改文档圆场**

回报可直接发郭老师，他会对照本页与 `D:\Work\AI自成长引擎\进度审查报告_微内核路由与双引擎_20260920.md` 逐条核验。

---

## 八、硬约束（违反即返工）

1. **不联网跑探针需单独许可**：探针外网 GET（`<POOL_HOST>`、`<MIRROR_HEALTH_API>`）属未明确许可访问；首跑前问一句
2. **不新造第 5 个存放地，不改 ai-platform / 记忆库 / 全局路由**：项目外真源只读+需许可
3. **不硬编码**：域名/卡号/正则/阈值一律从契约读
4. **不静默改执行真源**；**探针产物文件禁止手改**（唯一写者=探针）
5. **数值/枚举断言必须标 `source` 指向真源**；路径/数值落盘前先 `Test-Path`/跨真源比对
6. **禁止递归删除与改 git 历史**：`api-gateway` 目录不许删
7. **撞车检查前置到落盘前**：命中同题走增补 + `relates_to`，不新建平行条目
8. **`runtime\` 是探针产物目录**，人工只读，别手填假数据"凑 drift"

---

## 九、关键文件索引（绝对路径 + 定位）

### 下游 `D:\Work\自适应工作流引擎\`
- `contracts\gpt-mirror-extended-review.yaml`（15,749 B）— workflow_id=`gpt-mirror-extended-review`；含 8 类 conditions、gates（`turn_budget` 已 12→24 带 source）、skill_home/skill_install_to/skill_index、card_id_discovery
- `probes\probe_gpt_mirror.ps1`（22,736 B）— **待修 X1-X3 后首跑**；`runtime\` 首跑时生成
- `docs\00-项目总览.md` — A0-A4 分级、drift 四级、铁律三条
- `docs\01-任务看板.md`（L98 轮次更正）、`docs\02-镜像站试点盘点.md`（§八）、`docs\03-方案构想.md`（探针分层 P1/P2、契约 schema、自愈执行序、Windows 传参三坑）
- 待建：`runners\evalB_ext_adaptive.js`（A2/T9）、`probes\probe_gpt_mirror_ui.js`（A2/T10）、`runtime\`、`contracts\` 下 drift 历史

### 上游 `D:\Work\AI自成长引擎\`
- `docs\经验沉淀SOP.md` — L1/L2/L3 模板 + L3 流转规则（§一）+ 行为守则（§三）
- `docs\00-项目总览.md`（五段流水线、三级产物）`01-任务看板.md`（§六 决策日志、§七 撞车率复盘）`02-原始想法记录.md``03-方案构想.md`（§2.2 中转区与撞车检查）
- `技能库\README.md`（L3 唯一归档地 + 两目录分工 + 安装五步闸门）
- `草稿区\{L1_经验,L2_SOP,L3_Skill}\`（现有 3 条 L1，其中 2 条已补 relates_to）
- `scripts\快照草稿区.ps1` + `_快照\`（zip+日志）

### 外部真源（只读 + 需许可）
- `D:\记忆\调度大脑记忆\`（feedback/流程/索引，轮次纪律已改 24 带修订记录）；`D:\记忆\_Skills\` 程序 skill（**未装本项目 skill，安装需点头**）
- `D:\Work\AI平台\docs\runbooks\GPT镜像站送审流程.md` / `scripts\evalB_ext.js`（失效标注勿改代码）/ `resource-ops\docs\操作手册\01_镜像站GPT操作手册.md`（≤24 轮真源）
- `C:\Users\郭永涛\AGENTS.md`（微内核路由：P1/P2/P3、硬约束 1-3）；`.claude\CLAUDE.md` 仅指针

---

**原 Agent 最后核实**（2026-09-21，均实测非转述）：探针语法 1 错（L382）+ 2 隐患（L206/L421）、`runtime\` 尚不存在、契约 `draft_pending_first_run`、L3 未安装进 `D:\记忆\_Skills\`、探针脚本从未执行。

> 📘《镜像站试点盘点》§八、《审查报告》§五 记录了 12→24 裁定与探针首跑设计；《AI 自成长引擎任务看板》§六/§七 记录撞车与决策口径。
