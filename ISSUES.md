# ISSUES

需要高智力模型判断的问题。请按编号回答。Q1 是其余问题的前置。

---

## 裁定状态（2026-09-25 本轮已全部裁定）

| # | 裁定要点 | 入库位置 |
|---|---|---|
| Q1 | 单一 **Current State 语义对象** + 多原生真源 + 单向生成；Markdown 与 Obsidian 都只是投影 | `VERDICTS.md` §Q1 |
| Q2 | Demand **归元智能层持有**，上下游只持 `demand_id` 指针 | **DEC-13** |
| Q3 | Human View 只治理"用户入口"，不合并、不改写两个技术看板；看板降为 L3 证据层 | `VERDICTS.md` §Q3 |
| Q4 | 自动产物先进隔离候选区；任何改变运行时能力/正式技能库的晋升必须人审 | **DEC-14** |
| Q5 | 判定可信度**进 V1 完成条件**，`self_test` 失败 fail-closed | **DEC-15** |
| Q6 | dry-run 清单 + 回滚清单，人工批准**该固定批次**后执行；清单与实际不符即停 | `VERDICTS.md` §Q6 |
| Q7 | 固化为阻断式 pre-push 闸门，但须先修掉"整行白名单跳过"盲区；顺序固定：改完 → 重生成副本 → 全仓扫 → 检查扫描器自身 → 全绿才 push | `VERDICTS.md` §Q7 |
| Q8 | "抓取层不回源"的判定被本轮实测推翻；改为只设闸门：三项回读凭证 + 每条 URL 最多 3 次重试 + 三连败即阻断作答 | **DEC-08 v2 / DEC-11 / DEC-12** |
| D-003 | 已定义（statement / success_state / constraints / recheck_on），不与 D-001、D-002 重叠 | `DEMAND.md` D-003 v1 |

**裁定原文逐字存档在 `VERDICTS.md` 第 1 轮**（本表是对它的转述，冲突以存档为准）。**下一轮题目待本地执行后新增。** 以下 Q1~Q8 正文保留作题目原貌；其中 Q8 关于"抓取层不回源"的前提**已被推翻**，只当实验记录读。

---

---

## Q1 · 融合后谁是"当前状态"的唯一真源【核心】　【已裁定】

现状四处重叠：记忆库 `STATE`（≤60 行硬约束）、上游 `01-任务看板`（T/D/M 编号）、下游 `runtime/*.drift.json`（探针产物）、以及新协议要求的 `CURRENT_STATE.md`。

**请选择或另给方案：**
- **A** `CURRENT_STATE.md` 只做"摘要 + 指回真源"，不持有状态（我倾向此项，成本最低）
- **B** 元智能的 `State` 对象升格为唯一真源，其余三处改为它的投影／索引
- **C** 记忆库 `STATE` 保持唯一真源，其余都是视图

判据请一并给出：什么信息有资格进入 State，什么必须留在原处。

## Q2 · Demand 层放不放在元智能？

`Demand`（长期需求）是三套系统里唯一**没有对应实现**的对象 —— 上游按项目组织，下游按契约组织，都不承载"跨年度的需求演进史"。

是把 Demand 作为元智能的独有价值落地（我倾向：这是它最该留的部分），还是认为契约文件已足够承担？

## Q3 · Human View 标准要不要反向治理现有看板？

元智能的 `HUMAN_VIEW_STANDARD` 明确禁止首页出现文件路径、JSON、原始日志、重试细节，并要求"只展示会改变理解或判断的信息"。

而现在两个 `01-任务看板.md` 都是高密度技术台账（13KB / 18.6KB），用户实际要读它才能知道系统到哪。**这是不是应当治理？** 若是，治理边界到哪（合并？分层？重写？）。

## Q4 · 自动产出与人审闸门如何接口？

Loomy 持续自动生成本地系统（本阶段 9 → 10），而体系要求技能安装必须人工点头。当前无既定接口。

- **A** 自动产出先进隔离区，满足条件才晋升到待审队列（我倾向）
- **B** 接受自动入册，但人审只把关"安装/启用"
- **C** 关闭自动生成

## Q5 · 便宜模型的判定可信度，是否列为 V1 完成条件？

规范包 V1 唯一的验证问题是"长期需求能否不依赖用户管文件而被持续理解执行"，**没有问"执行模型能否可靠判定"**。

本阶段两次事故都是判定器坏掉却产出看起来合理的假结论（`Test-Path` 对断链恒真；`@(管道|Measure-Object).Count` 恒为 1）。是否把"执行层判定必须有 self_test 且能自证"写进 V1 完成条件？我认为必须，但这会改变它的落地成本。

## Q6 · 946 条死链的清理授权边界

技术上单一动作即可（`cmd /c rmdir`，严禁 `Remove-Item -Recurse`）。但需要你确认：**先出 dry-run 清单再执行**，还是授权一次跑完？另，`lark-*` 公有云 17 个已被人删过，清理时不应顺手重建回去 —— 这个判断是否成立？

## Q7 · 敏感事实的长期闸门

本体系的事实描述必然含第三方服务域名、账号卡标识、企业私有化域名。现有密钥扫描器 7 条规则**一条都不命中**这些。

本轮已按类型掩码（映射表仅存本地）。是否将其固化为 push 前强制闸门（扩规则 + 阻断式），并规定哪些目录允许含原值？

> 补充实测（2026-09-25，可直接作为你判断的输入）：现行规则集 23 类，对全仓 97 个文本文件复扫 —— **掩码目标 21 类 0 命中**；余 2 类是有意保留项（本机用户名 225 次**全在路径内**、散文 0 次；"私有化"类别词若干行）。另有三个已记录的盲区：白名单按整行跳过（跳过行数由扫描器每次打印，不写死）、规则集是枚举式（7→23 类的过程本身就是全漏的证明）、`real_name` 只匹配一个字面量。详见 `REDACTION.md`。
> 同时发现扫描器**把"命中行数"标成"处"**（220 行 vs 225 次），已修。若这套东西要做阻断式闸门，计数单位的歧义本身就是缺陷 —— 这一条请一并纳入你的判断。

## Q8 · 送审介质与"读取确认"该定成什么（2026-09-25，已结案到只剩选择）

**实测结论（七条探针，每条都先在本地验过 200 才发给你）**：你的网页抓取层**不会回源**。一条 URL 你读得到，唯一原因是它已在缓存里；读不到与哪个仓、什么内容、文件多大（139 B 到 200 KB 都试过）、路径是否中文、用 `main` 还是 commit SHA **全部无关**。定案那两条是新 URL 放在**你声称读得到的那个仓**里（一条 20 KB 深路径 Flutter 源码、一条 5.3 KB 中文路径 md），结果同样读不到。完整账目见 `CURRENT_STATE.md` §六。

**三条方案已被证伪，不必再讨论**：换 SHA 固定链接、另建公开导出仓、给本仓开 GitHub Pages。"等 GitHub 搜索索引收录"也无效——本仓当日已在索引内，照样抓不到。

**为什么这仍是 V1 的缺口**：规范包 V1 的验收只问"长期需求能否不依赖用户管文件而持续运行"，没有一条问"对方能否读到事实层"。而分工协议（本地执行 / GitHub 同步事实 / 高智力判断 / Obsidian 给人看）成立的前提正是这条；前提第一次被证伪，是靠人手工做对照实验发现的，没有任何检查会报。

**请判定三件事：**

1. **送审介质选哪个。** **A**｜每轮把合并副本作为**对话附件**上传（现在就能用；仓库退化为"人读的真源 + 附件的来源"）／**B**｜接 GitHub connector（走授权管道，可能可读任意新文件，需你账号侧动作）／**C**｜A 立即用、B 通了以后转 B。我倾向 **C**。
2. **"回读确认"的最小判据。** 目前把 `git push 成功` 当成"事实已同步"，这是错的。请给一个**便宜到每轮都跑得下去**的确认形式——例如要求你在回答开头原样回出指定文件的首行或本轮 nonce。注意判据必须是你**伪造不出来的**：本体系此前确实发生过"没读到材料却给出结构完整的作答"。
3. **仓库在协议里的角色要不要改写。** 若抓取层注定读不到新 URL，那"GitHub 负责同步事实"这句话需要限定：它同步的是**人与附件**的事实源，不是模型的抓取入口。这条要不要写进 `DECISIONS.md`？


---

**状态**：Q1~Q8 已于 2026-09-25 全部裁定，结论见顶部摘要表与 `DECISIONS.md` DEC-11 ~ DEC-15。

---

## 第 2 轮题目（2026-09-25 深夜新增）· 三套系统融合成**一个项目**的物理形态

基线是 `LOCAL_DRAFT_ENTRY_LAYOUT.md` v0.1（本地自拟，待反驳）。第 1 轮已定概念归属（Demand 归元智能层、State 是语义对象、视图不配平真源），本轮**只问目录/仓库/文件放哪、谁写谁生成、怎么迁移**。用户诉求一句话：变成一个项目，且他不想手工管文件。

**N1** 入口与执行层的物理边界：本地方案是"新建一个 `D:\Work\元智能\` 入口库（需求/状态/证据三块），三套引擎原地不动"。请判：**这个切法对不对**？还是应该真的把三套系统并成一个仓/一个库？给判据，别给折中话。

**N2 指针的活性**：证据层用 junction/symlink 指向原仓库。本机刚发生过 946 条死链事故（批量分发、无人回收）。请定：**谁负责周期性校验指针活性、多久一次、断了怎么报、报给谁**；以及如果 Obsidian 的搜索/反链/图谱不跟随链接，证据层该换成什么形态（生成索引页？复制快照？还是接受不可搜）。

**N3 Demand 台账的真源位置**：给人看的地方在 Obsidian，给 AI 写的地方在公开事实层仓 —— 两边都放就是双写。请判：**真源放哪一侧**，另一侧是投影还是指针；以及 DEC-13"元智能层持有 Demand"在物理上落成哪个路径才算成立。

**N4 生成器契约**：输入清单（Demand 台账 + 两个任务看板 + `runtime/*.drift.json` + DECISIONS/VERDICTS）→ 输出（`CURRENT_STATE.md` + 首页）。请给：**字段清单、确定性要求、生成失败时首页必须显示什么**（不允许把过期内容当现状展示）、以及"视图与真源冲突"的判定规则写成可执行判据。

**N5 触发的责任**：重生成靠手动、定时、还是 Agent 每次收尾必须跑？请给一个**不需要用户记得去点**的方案，并说明它失败时怎么被发现（这是第 1 轮"push 成功≠已送达"的同类病：跑成功 ≠ 页面新）。

**N6 迁移顺序与红线**：第一步的最小可逆动作是什么？哪些**绝对不许动**（老路径、各子库 git 结构、技能库物理位置）？存量任务要不要回填 `demand_id`，不回填的后果是什么？出现哪些信号应当直接回滚。

**必答顺序**：N1 → N3 → N4 → N5 → N2 → N6。每条给判据 + 作废条件；信息不足直说缺什么文件。

---

## 第 3 轮题目（2026-09-26 新增）· 阶段 6~7 收口：切入口与去旧写口

背景（本对话已定，不必重述）：控制平面为语义真源、事实层与 Obsidian 为投影（DEC-16）；Task 归属允许 `scope: local`（DEC-20）；控制平面自持 `TASKS.yaml`（DEC-21）；D-004 已立项；CT-06 已实测 Demand 升版传播成立并补了追加式重判队列；CT-11 把送达判据做成可执行校验并给契约加了 `delivery_readback` / `fetch_retry_budget` 两条闸门（现标 `verified_at: null`）。

现在要切默认入口（CT-07）并把事实层四份文件改成生成物（CT-08）。请判：

**P1 切入口的条件**：当前 CT-05（旁路稳定运行）没做、workflow T15/T16 两条机制外扩仍 unresolved。切默认入口需要同时满足哪些条件？切换动作本身怎么做才不断 Obsidian 现有链接（`Home.md` 与子库一览都指着老路径）？给"切了以后发现不对，怎么一步退回"的具体做法。

**P2 去旧写口的顺序**：`DEMAND.md`、`DECISIONS.md`、`ISSUES.md`、`CHANGELOG.md` 改成生成物，哪一份先改风险最低、哪一份必须最后改？生成频率怎么定（每次 push 前？每次收尾？）？特别注意：外部模型只能读投影，如果投影变成生成的，**送审材料的生成时机**会不会晚于本轮改动，导致我发给你的还是旧内容？这条怎么防。

**P3 迁移期双写**：同一份内容在 `machine\` 与事实层两处并存，直到 CT-08 完成。请给**可机器检查的判据**（不是"注意不要重复编辑"这种话），比如：什么校验、多久跑一次、发现两处内容不一致时以哪侧为准、另一侧怎么处置。

**P4 State 的字段缺口**：`active_experiments` 现在恒为 `null`，因为 EXPERIMENTS 没有条目结构可派生。要不要现在把 EXPERIMENTS 纳入生成输入（今天已新增两份）？给判据：什么条件下值得为它建结构化输入，什么条件下就该承认这个字段暂时派生不出来。

**P5 送达闸门的第二层**：本轮你按要求输出三项回读凭证，即视为该闸门首次真实通过。请再判一件事：要不要在第一版就加 manifest（两包文件数、字节数、SHA-256）？你上一轮说"manifest 是辅助完整性证据，不替代双包尾 nonce"——现在给结论：加、不加、还是等出现第一次"凭证齐但内容仍不对"再加。给判据和作废条件。

**必答顺序** P1 → P2 → P3 → P5 → P4。每条给判据 + 作废条件。回答第一行按《作答要求》第 0 条输出三项回读凭证。

---

## 第 4 轮题目（2026-09-26 新增）· Obsidian 里把三个项目合并成一个，怎么做

用户诉求原话：**"我想先把 Obsidian 中的三个项目合并在一起看，怎么做。"** 他要的是**看起来是一个项目**，不是把代码仓合并（DEC-16 已定不并仓、不搬执行层）。

### 本轮实测到的现状事实（不是推测，来自本地检查）

1. `D:\Work` **本身就是一个 Obsidian vault**（存在 `D:\Work\.obsidian`，含 `app.json / graph.json / workspace.json / core-plugins.json`）。三个项目是它下面的**平级子库目录**：`AI自成长引擎\`、`自适应工作流引擎\`、`元智能\`。所以"合并"不是跨 vault 合并，而是**同 vault 内的视图与入口合并**。
2. 但 `D:\Work` 里还**嵌套着别的 vault**：`AI平台\.obsidian`、`逆天主题\.obsidian`、`通用规范\.obsidian`；另有完全独立的 `D:\记忆\.obsidian`。
3. `D:\Work\Home.md` 有「子库一览」表格，登记了 `AI自成长引擎` 与 `自适应工作流引擎` 两行，**没有 `元智能` 这一行**——控制平面建了 git 与生成链，但 Obsidian 侧根本没登记。
4. 同一张表里两行描述写着"构想中，暂无 git 仓"，**已过期**：两个引擎现在都有 git，控制平面也有。
5. 控制平面首页是生成物 `D:\Work\元智能\generated\human\00_首页.md`，按 DEC-22 切换时 `Home.md` 要原地改成兼容入口。

### 请回答（必答顺序 M1 → M2 → M4 → M3 → M5 → M6）

**M1 合并形态选哪种。** 三条候选，请只选一条并给判据：
- **A** 物理聚合：把三个子库挪进一个父目录（如 `D:\Work\元智能\` 下），Obsidian 里天然是一个项目。
- **B** 保持平级 + 统一入口：目录不动，用一页"元智能项目簇"索引页 + 标签/图谱分组把它们在**视图上**合成一个。
- **C** 混合：入口与索引按 B 做，只把控制平面自己新建的目录归拢。
必须直说 A 的断链代价（本机有 946 条死链的前科）以及 B 的代价（用户仍然要在文件树里看到三个平级目录，"一个项目"只存在于入口页）。

**M2 入口怎么收。** 现在三个项目各有 README/Home，`Home.md` 是总入口。合并后**默认打开哪一页**、其余入口页保留还是降级为跳转页？给出"用户双击任何一处都不会撞到两套说法"的具体做法。

**M4 双链与附件。** 若选 A 或任何涉及移动的方案：Obsidian 的 `[[wikilink]]`、嵌入图片与附件、`graph.json`、以及外部（脚本/契约/技能库）写死的绝对路径，哪些会断？断链检查用什么判据（不能只 `Test-Path`，DEC-06）、多久跑一次、断了怎么回收。若选 B，请说明索引页怎么保证不变成第二份需要人维护的真相。

**M3 嵌套 vault 的影响。** `D:\Work` 里嵌套着 `AI平台`/`逆天主题`/`通用规范` 三个子 vault，`D:\记忆` 是独立 vault。请判：父 vault 会不会索引/搜索到嵌套 vault 的内容？合并方案要不要为此改结构？如果这条会影响 M1 的选择，直接说怎么选。

**M5 与 CT-07 / CT-08 的先后。** Obsidian 合并放在切入口之前还是之后？如果先做合并，`cutover.ps1 -Apply/-Rollback` 要管的东西会不会变多（现在它只管入口指向）？给一个明确顺序，不要"视情况而定"。

**M6 登记与过期。** `Home.md` 缺 `元智能` 行、且两行"暂无 git 仓"已过期。这类**人工维护的索引页过期**问题，按 DEC-24 的思路该怎么治：`Home.md` 是否应该成为生成投影？如果是，它的数据源是什么（`project.yaml`？`TASKS.yaml`？），哪些内容允许人写。

---

## 第 5 轮题目（2026-09-26 深夜）· 今天这一轮要做到什么程度

**不问方案，只问范围。** 现状实测（不是计划）：

- Demand 四条 D-001~D-004 都有定义与真源；State 派生出跨系统活动任务 **D-001 2／D-002 8／D-003 5／D-004 9**，另有 4 条 `scope: local`、2 条 unresolved（机制外扩未授权）。
- 控制平面 14 项任务：**9 完成、5 未完成** —— CT-05 事件覆盖扩展、CT-07 切默认入口、CT-08 去旧写口、CT-10 等授权、CT-13 cutover 脚本。阻断入口切换的是 CT-07 与 CT-13。
- 今天已经发生的事：融合从"只有一个意图"变成有 D-003/D-004 定义并跑通传播链；契约新增两条闸门，其中 `delivery_readback` **实际拦下了一次假通过**（对方把上一轮交付号写在首行）；Obsidian 合并定为"视图合并不搬目录"，项目簇页已生成可看，`Home.md` 子库表已改成生成投影，并因此纠正了两件被手填掩盖的事实——"各子库独立 git"是错的（12 个子库里 7 个归属同一个 `D:\Work` 仓），以及一个子库的入口页根本不存在。
- 现在是本地深夜，剩余精力有限，明天还要能接着干。

**请回答四条。不要复述现状，不要给路线图，不要"视情况而定"：**

**T1** 今天这一轮的**完成定义（DoD）**：一条可以判真/判假的标准，不是"尽量做完"。

**T2** 今天**必须做完**的清单（最多 3 项，"今天什么都不再做"是合法选项）与今天**明确不做**的清单，每条一句理由。

**T3** **停止线**：出现什么信号今天就该收手？收手时的最小收尾动作是什么，才不把系统留在半成品状态（我们现在有 State、投影、契约、看板四处要同步）？

**T4** 现在这套东西里，哪一处最可能**看起来完成了、其实没有**？给一个能机器判定或一次性能判的检查方法，不接受"注意检查"这类话。

---

## 第 8 轮题目（2026-09-27 00:20 新开窗口）· 仍是上面 T1~T4，但请带上这三条新事实

上一轮（第 7 轮）首行凭证抄的是旧值，按 DEC-11/12 判"材料未送达"，所以 T1~T4 至今没有一条被采信的裁定。**请就上面 T1~T4 作答**，不要复述现状、不要给路线图、每条给判据和作废条件。

作答时把这三条今晚新发生的事实算进 T1/T2/T4：

1. **送达闸门自身被查出缺陷（DEC-31）**：旧《作答要求》把两个包尾凭证的字面值原样印在**第一个包**里，因此只读 CORE 就能抄齐三项。也就是说，此前所有"pass"最多只证明第一个包被读到，没证明规范层 55 个文件被打开过。现已改为"只说去哪取 + 生成后自检包内唯一，违反即拒绝写包"。**这条本身就是 T4 的一个真实答案样本，请独立判断还有没有同类。**
2. **入口切换的两个阻断项进展不对称**：CT-14（项目簇索引 + Home 生成投影）已完成并 `--apply` 过，`Home.md` 子库表现在是机器生成。**CT-13（`cutover.ps1 -Apply/-Rollback`）今晚实现完了，但在线实测一次没跑。** 入口机制是实测出来的：`D:\obsidian\resources\obsidian.asar` 里有 `openBehavior").startsWith("file:")`，即 vault 配置 `D:\Work\.obsidian\app.json` 支持 `"openBehavior": "file:<相对路径>"`（`workspace.json` 走不通，Obsidian 运行时改它必被覆盖）。已过：沙箱 apply→rollback 往返、两件文件字节级还原、真 `Home.md` 哈希全程未变、Obsidian 正在运行时 Apply 被前置闸门拦下退出码 2。未过：**在线 Apply 从未执行**（本机 Obsidian 在跑，脚本按设计拒绝）。所以 DEC-22 四项前置里"切换与回滚脚本实测通过"这条**仍不成立**，CT-07 不许启动。这条也是 T4 的样本：脚本写完、离线测试全绿，跟"切换在本机真能成立"是两件事。
3. **本轮实测已经推翻过两条手填事实**：`Home.md` 原写"各子库独立 git 仓"，实为 12 个子库里 7 个归属同一个 `D:\Work` 仓；另一个子库的入口页根本不存在（真实死链）。这类"看起来登记了、其实没登记"的形态，请在 T4 里一并考虑。
